[package]
name = "qemu-bsp-listener"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
description = "Simple listener example using nros-qemu"

[[bin]]
name = "qemu-bsp-listener"
test = false
bench = false

[features]
default = []
# Use Docker network configuration (different IPs for container-based testing)
docker = ["nros-qemu/docker"]

[dependencies]
# Platform crate - all you need for QEMU bare-metal
nros-qemu = { path = "../../../packages/boards/nros-qemu" }

# Generated message types (from package.xml via cargo nano-ros generate)
std_msgs = { version = "*", default-features = false }

# Panic handler (required for no_std)
panic-semihosting = { version = "0.6", features = ["exit"] }

# phase-383 W9 — this workspace's root manifest is GENERATED by `nros build`
# from the packages it discovers plus the `[image.*]` table. It is build
# output like `build/` and `Cargo.lock`, and committing it would put a
# generated member list under review (RFC-0065 D3).
/Cargo.toml
/target/
/build/
/Cargo.lock

<?xml version="1.0"?>
<!-- Phase 269 W3 — talker half of the cross-process C E2E-safety demo.
     In-process delivery does not happen (same zenoh session; issue 0096), so the demo
     splits into two processes. This entry boots only the talker; the sibling
     `safety_listener.launch.xml` boots the C validated listener.
     Boot with `native_safety_talker_entry`. -->
<launch>
  <node pkg="c_safety_talker_pkg" exec="talker" name="talker"/>
</launch>

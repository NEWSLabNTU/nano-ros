# phase-331 W4 (RFC-0066) — the rust half of the E2E-safety workspace.
[workspace]
resolver = "2"
members = [
    "src/rust_safety_talker_pkg",
    "src/rust_safety_listener_pkg",
    "src/native_rust_safety_talker_entry",
    "src/native_rust_safety_listener_entry",
]
# West-built staticlib, not plain cargo.
exclude = ["src/zephyr_rust_safety_entry"]

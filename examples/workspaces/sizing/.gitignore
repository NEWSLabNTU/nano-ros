# phase-383 W10.a — the root manifest is GENERATED by `nros build` from the
# discovered packages plus the `[image.*]` table. Build output, like
# `build/` and `Cargo.lock`; committing it would put a generated member
# list under review (RFC-0065 D3).
/Cargo.toml
/generated/
/build/
/build-*/
/target/
/target-*/
/Cargo.lock

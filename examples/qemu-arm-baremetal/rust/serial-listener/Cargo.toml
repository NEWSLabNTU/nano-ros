[package]
name = "qemu-serial-listener"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
description = "Serial (UART) listener example using nros-board-mps2-an385"

[[bin]]
name = "qemu-serial-listener"
test = false
bench = false

[features]
default = []

[dependencies]
# Board crate with serial transport (no ethernet)
nros-board-mps2-an385 = { path = "../../../../packages/boards/nros-board-mps2-an385", default-features = false, features = ["serial", "rmw-zenoh"] }
# Phase 88.16.C — user diagnostics route through `nros-log`.
nros-log = { path = "../../../../packages/core/nros-log", default-features = false }

# nros Executor API (no link-tcp/link-udp — serial link comes from zpico-serial)
# Phase 204.1.L — sub-only node: lean cffi + the subscriber entity slot
# (service-server/client trampolines + buffers stay stubbed → stripped).
nros = { path = "../../../../packages/core/nros", default-features = false, features = ["rmw-cffi-lean", "rmw-entity-subscriber", "platform-bare-metal", "ros-humble"] }
nros-rmw-zenoh = { path = "../../../../packages/zpico/nros-rmw-zenoh", features = ["platform-bare-metal", "ros-humble"] }

# Generated message types (from package.xml via nros generate-rust)
std_msgs = { version = "*", default-features = false }

# Panic handler (required for no_std)
panic-semihosting = { version = "0.6", features = ["exit"] }

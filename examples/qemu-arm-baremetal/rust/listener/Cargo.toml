[package]
name = "qemu-bsp-listener"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
description = "Bare-metal nros::main!() BoardEntry listener for QEMU MPS2-AN385"

[[bin]]
name = "qemu-bsp-listener"
test = false
bench = false

# Self-contained standalone example (issue 0100): `lib.rs` IS the node
# (`nros::node!(ListenerNode)` emits `register`); `main.rs` is the
# `nros::main!()` Form-1 Entry dispatching to this same crate's `register`.
[lib]
crate-type = ["rlib"]

[package.metadata.nros.entry]
deploy = "qemu-mps2-an385"

[package.metadata.nros.node]
class = "qemu_bsp_listener::ListenerNode"
name = "qemu_bsp_listener"
default_namespace = "/"
dispatch = "deferred"

# Issue #48 cause 1 — net config that used to live in the hardcoded `Config`
# literal now rides here; `nros::main!()` bakes it into a `DeployOverlay` and
# the board applies it in `run_with_deploy`. Mirrors the old listener Config
# (ip 10.0.2.11, prefix 24 → netmask 255.255.255.0).
[package.metadata.nros.deploy.qemu-mps2-an385]
locator = "tcp/10.0.2.2:7450"
ip = "10.0.2.11"
gateway = "10.0.2.2"
netmask = "255.255.255.0"

[dependencies]
nros = { path = "../../../../packages/core/nros", default-features = false, features = ["alloc", "rmw-cffi", "ros-humble"] }
# Phase 244.D1 enabler — `board-entry` pulls the `nros_platform::BoardEntry`
# impl so `nros::main!(deploy = "qemu-mps2-an385")` can drive this board. Keeps
# its existing ethernet + rmw-zenoh defaults.
nros-board-mps2-an385 = { path = "../../../../packages/boards/nros-board-mps2-an385", features = ["board-entry"] }
# Linked RMW backend (force-linked; self-registers via the macro's
# `__register_linked_rmw()` on `target_os = "none"`).
nros-rmw-zenoh = { path = "../../../../packages/zpico/nros-rmw-zenoh", features = ["platform-bare-metal", "ros-humble"] }
# User diagnostics route through `nros-log`; the dispatcher is installed by the
# board BoardEntry boot scaffold — this crate only registers its Logger handle.
nros-log = { path = "../../../../packages/core/nros-log", default-features = false }
# Generated message types (from package.xml via `nros generate-rust`).
std_msgs = { version = "*", default-features = false }

# Bare-metal Cortex-M entry: `nros::main!()` emits `#[cortex_m_rt::entry]` for
# the `qemu-mps2-an385` deploy; the reset trampoline halts via `cortex_m::asm`.
cortex-m = { version = "0.7", features = ["critical-section-single-core"] }
cortex-m-rt = "0.7"
panic-semihosting = { version = "0.6", features = ["exit"] }

# Phase 204.3 — size-optimized profile: opt-level "z" + strip, inheriting
# release's lto/codegen-units. Build with `cargo build --profile size`.
[profile.size]
inherits = "release"
opt-level = "s"
debug = false
strip = true

[profile.release]
codegen-units = 1
debug = 2
debug-assertions = false
incremental = false
lto = "fat"
opt-level = "s"
overflow-checks = false

# Phase 208.F1 — empty `[workspace]` table makes this example a standalone
# workspace root. Without it, cargo walks up + adopts the example into the
# outer nano-ros workspace (in nested checkouts) or gets adopted by a user's
# workspace `members` glob when they copy this example out, breaking the
# README's "standalone copy-out template" promise.
[workspace]

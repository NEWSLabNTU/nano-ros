<?xml version="1.0"?>
<!-- Phase 244.D1 — empty launch placeholder (Form-1 self-bringup).

     The single node is resolved from this Entry pkg's own re-exported
     `register` (src/lib.rs → qemu_baremetal_e2e_pkg). A populated <launch>
     is exercised by the nav2-compat / multi-node fixtures, not this
     BoardEntry boot smoke. -->
<launch>
</launch>

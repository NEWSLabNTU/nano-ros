# Phase 212.K — patches moved to the example's Cargo.toml so the
# generated `std_msgs/cyclonedds` feature wiring (Phase 212.K) takes
# effect. Cargo also reads `[patch.crates-io]` from
# `.cargo/config.toml`, and when both are present the config-file
# patches SHADOW the Cargo.toml patches — which previously pinned the
# example to the pre-cyclonedds `generated/` tree.
#
# This file is intentionally left empty (rather than deleted) to
# preserve the per-example tracked-file shape; the active patches now
# live in `Cargo.toml`'s `[patch.crates-io]` block.

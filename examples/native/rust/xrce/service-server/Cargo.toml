[package]
name = "native-rs-xrce-service-server"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[[bin]]
name = "xrce-service-server"
path = "src/main.rs"

[dependencies]
nros = { path = "../../../../../packages/core/nros", default-features = false, features = ["std", "rmw-xrce", "xrce-udp", "platform-posix", "ros-humble"] }
example_interfaces = { version = "*", default-features = false }

[package]
name = "native-rs-action-server"
version = "0.1.0"
edition = "2024"
description = "Example ROS 2 action server using nros"

[dependencies]
nros = { path = "../../../../../packages/core/nros", default-features = false, features = ["std", "rmw-zenoh", "platform-posix"] }
example_interfaces = { version = "*", default-features = false }
heapless = "0.8"
log = "0.4"
env_logger = "0.10"

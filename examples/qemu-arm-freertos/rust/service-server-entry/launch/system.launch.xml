<?xml version="1.0"?>
<!-- Phase 212.N.7 step-2 — empty launch placeholder.

     The Component pkg's register() wrapper is a TODO stub right now
     (see ../service-server/src/lib.rs for the RuntimeCtx-vs-ComponentRuntime
     gap), so emitting <node> entries here would produce a run_plan
     body whose register() calls do nothing. Step-3+ lands the live
     registration path AND a populated <launch>.
-->
<launch>
</launch>

[workspace]

[package]
name = "nros-freertos-ffi"
version = "0.1.0"
edition = "2024"
publish = false
description = "Panic handler + global symbols for FreeRTOS C/C++ examples (no_std)"

[lib]
crate-type = ["staticlib"]

[dependencies]
panic-halt = "1"

[profile.release]
opt-level = "s"
lto = true

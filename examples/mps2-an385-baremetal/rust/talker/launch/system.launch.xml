<?xml version="1.0"?>
<!-- Empty launch placeholder (Form-1 self-bringup).

     The single node is resolved from this Entry pkg's own re-exported
     `register` (src/lib.rs -> talker_pkg). A populated <launch> is exercised by
     multi-node fixtures, not this single-talker example. -->
<launch>
</launch>

# px4/rust/uorb

Placeholder for the Rust PX4 uORB example. The entry point
`just px4 build-sitl` is already wired and points
EXTERNAL_MODULES_LOCATION at this directory; a copy-out user
example landing here is pending.

Until that example lands, see `examples/px4/cpp/uorb/` for the C++
EXTERNAL_MODULES_LOCATION integration smoke
(`nros-register-check`).

# Phase 157 — NuttX external-app Kconfig for the C service-server example.
# Sourced from `integrations/nuttx/Kconfig` via osource glob.

config NROS_EXAMPLE_SERVICE_SERVER_C
    tristate "nano-ros: C service-server (zenoh)"
    default n
    depends on NROS && NROS_C_API
    ---help---
        Hosts an example_interfaces/srv/AddTwoInts service on /add_two_ints.
        Built-in command name: `nuttx_c_service_server`.

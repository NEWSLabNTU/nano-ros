# Phase 157 — NuttX external-app Kconfig for the C action-client example.
# Sourced from `integrations/nuttx/Kconfig` via osource glob.

config NROS_EXAMPLE_ACTION_CLIENT_C
    tristate "nano-ros: C action-client (zenoh)"
    default n
    depends on NROS && NROS_C_API
    ---help---
        Sends an example_interfaces/action/Fibonacci goal on /fibonacci and streams feedback.
        Built-in command name: `nuttx_c_action_client`.

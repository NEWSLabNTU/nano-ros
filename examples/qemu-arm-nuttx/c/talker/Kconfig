# Phase 157 — NuttX external-app Kconfig for the C talker example.
# Sourced from `integrations/nuttx/Kconfig` via osource glob.

config NROS_EXAMPLE_TALKER_C
    tristate "nano-ros: C talker (zenoh)"
    default n
    depends on NROS && NROS_C_API
    ---help---
        Publishes std_msgs/msg/Int32 on /chatter via zenoh-pico.
        Built-in command name: `nuttx_c_talker`.

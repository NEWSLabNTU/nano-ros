# Phase 157 — NuttX external-app Kconfig for the CPP service-server example.
# Sourced from `integrations/nuttx/Kconfig` via osource glob.

config NROS_EXAMPLE_SERVICE_SERVER_CPP
    tristate "nano-ros: CPP service-server (zenoh)"
    default n
    depends on NROS && NROS_CPP_API
    ---help---
        Hosts an example_interfaces/srv/AddTwoInts service on /add_two_ints.
        Built-in command name: `nuttx_cpp_service_server`.

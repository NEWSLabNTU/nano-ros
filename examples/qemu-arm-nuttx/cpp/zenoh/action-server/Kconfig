# Phase 157 — NuttX external-app Kconfig for the CPP action-server example.
# Sourced from `integrations/nuttx/Kconfig` via osource glob.

config NROS_EXAMPLE_ACTION_SERVER_CPP
    tristate "nano-ros: CPP action-server (zenoh)"
    default n
    depends on NROS && NROS_CPP_API
    ---help---
        Hosts an example_interfaces/action/Fibonacci action on /fibonacci.
        Built-in command name: `nuttx_cpp_action_server`.

# Phase 157 — NuttX external-app Kconfig for the CPP service-client example.
# Sourced from `integrations/nuttx/Kconfig` via osource glob.

config NROS_EXAMPLE_SERVICE_CLIENT_CPP
    tristate "nano-ros: CPP service-client (zenoh)"
    default n
    depends on NROS && NROS_CPP_API
    ---help---
        Calls example_interfaces/srv/AddTwoInts on /add_two_ints and prints the result.
        Built-in command name: `nuttx_cpp_service_client`.

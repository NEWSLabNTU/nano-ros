# Phase 157 — NuttX external-app Kconfig for the CPP talker example.
# Sourced from `integrations/nuttx/Kconfig` via osource glob.

config NROS_EXAMPLE_TALKER_CPP
    tristate "nano-ros: CPP talker (zenoh)"
    default n
    depends on NROS && NROS_CPP_API
    ---help---
        Publishes std_msgs/msg/Int32 on /chatter via zenoh-pico.
        Built-in command name: `nuttx_cpp_talker`.

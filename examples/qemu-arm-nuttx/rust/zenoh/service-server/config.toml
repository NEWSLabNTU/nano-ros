# NuttX QEMU ARM service server — network configuration
# Used by nros-nuttx-qemu-arm board crate (Config::from_toml)

[network]
ip = "10.0.2.32"
gateway = "10.0.2.2"
prefix = 24

[zenoh]
locator = "tcp/10.0.2.2:7462"
domain_id = 0

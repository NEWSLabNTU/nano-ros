# NuttX QEMU ARM action client — network configuration
# Used by nros-nuttx-qemu-arm board crate (Config::from_toml)

[network]
ip = "10.0.2.33"
gateway = "10.0.2.2"
prefix = 24

[zenoh]
locator = "tcp/10.0.2.2:7472"
domain_id = 0

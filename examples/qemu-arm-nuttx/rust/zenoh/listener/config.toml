# NuttX QEMU ARM listener — network configuration
# Used by nros-board-nuttx-qemu-arm board crate (Config::from_toml)

[network]
ip = "10.0.2.31"
gateway = "10.0.2.2"
prefix = 24

[zenoh]
locator = "tcp/10.0.2.2:7452"
domain_id = 0

[package]
name = "threadx_linux_rs_action_client"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
description = "ThreadX Linux action client — Phase 212.L Component pkg"

[lib]
crate-type = ["rlib", "staticlib"]

[features]
# Phase 214.S.5 — `rmw-cyclonedds` parity row. The default deploy rmw is
# still selected via `[package.metadata.nros.deploy.<rtos>].rmw`; this
# row gives users `cargo build --features rmw-cyclonedds` as an
# alternative build target without changing the default. 1-entry shape
# mirrors S.4 (umbrella pulls the backend transitively).
# Phase 214.S.5.c — `rmw-zenoh` + `rmw-xrce` parity rows. Same shape
# as the cyclonedds row above; gives users `cargo build --features
# rmw-zenoh` / `--features rmw-xrce` alternative build targets without
# changing the default deploy rmw. Required by `just freertos
# build-examples` which invokes `cargo build --features rmw-zenoh`.
rmw-zenoh = ["nros/rmw-zenoh"]
rmw-xrce  = ["nros/rmw-xrce"]
rmw-cyclonedds = ["nros/rmw-cyclonedds"]

[dependencies]
nros = { path = "../../../../packages/core/nros", default-features = false, features = ["std", "rmw-cffi", "platform-threadx", "ros-humble"] }
# Phase 212.N.7 step-2 — Entry-pkg-callable `pub fn register(runtime: &mut RuntimeCtx)` wrapper.
nros-platform = { path = "../../../../packages/core/nros-platform", default-features = false, features = ["platform-threadx"] }
example_interfaces = { version = "*", default-features = false }

[package.metadata.nros.node]
class = "threadx_linux_rs_action_client::ActionClient"
name = "action_client"
default_namespace = "/"

[package.metadata.nros.deploy.threadx-linux]
board = "threadx-linux"
rmw = "zenoh"
domain_id = 0

[package.metadata.ament]
build_depend = ["example_interfaces"]
exec_depend = ["example_interfaces"]

[profile.release]
opt-level = "s"
lto = true

[workspace]

[patch.crates-io]
# === BEGIN nros-managed [patch.crates-io] ===
# Auto-generated by `nros ws sync`. Do not edit between
# the BEGIN/END markers — re-run sync instead.
action_msgs = { path = "generated/action_msgs" }
builtin_interfaces = { path = "generated/builtin_interfaces" }
example_interfaces = { path = "generated/example_interfaces" }
unique_identifier_msgs = { path = "generated/unique_identifier_msgs" }
nros-core = { path = "../../../../packages/core/nros-core" }
nros-serdes = { path = "../../../../packages/core/nros-serdes" }
# === END nros-managed [patch.crates-io] ===

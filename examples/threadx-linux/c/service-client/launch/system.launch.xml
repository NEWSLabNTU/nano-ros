<?xml version="1.0"?>
<!-- Phase 244 D6 — empty launch placeholder (mirrors threadx-linux/cpp).

     The threadx-linux generated runtime (`nros_threadx_codegen_system`)
     is a build-smoke stand-in: it dispatches each component's register
     trampoline but does not yet drive a live executor, so emitting
     <node> entries here is not required for the build-fixture role.
-->
<launch>
</launch>

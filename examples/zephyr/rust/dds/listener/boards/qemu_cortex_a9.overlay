/* Phase 92 — qemu_cortex_a9 DT overlay (listener)
 * See talker side for rationale. */

&gem0 {
    promiscuous-mode;
    /* Distinct MAC from talker — see talker's overlay for rationale. */
    local-mac-address = [02 00 00 00 00 02];
};

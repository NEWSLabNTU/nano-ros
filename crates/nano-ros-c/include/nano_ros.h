/**
 * nano-ros C API
 *
 * An rclc-compatible C interface for nano-ros, designed for embedded systems.
 * This header is auto-generated by cbindgen - DO NOT EDIT MANUALLY.
 *
 * Copyright 2024 nano-ros contributors
 * Licensed under Apache-2.0
 */


#ifndef NANO_ROS_H
#define NANO_ROS_H

/* Generated with cbindgen:0.26.0 */

/* Warning: this file is autogenerated by cbindgen. Don't modify this manually. */

#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>

/**
 * Maximum length of a zenoh locator string (e.g., "tcp/127.0.0.1:7447")
 */
#define MAX_LOCATOR_LEN 128

/**
 * Maximum length of a node name
 */
#define MAX_NAME_LEN 64

/**
 * Maximum length of a node namespace
 */
#define MAX_NAMESPACE_LEN 128

/**
 * Maximum length of a topic name
 */
#define MAX_TOPIC_LEN 256

/**
 * Maximum length of a service name
 */
#define MAX_SERVICE_NAME_LEN 256

/**
 * Maximum length of a type name (e.g., "std_msgs::msg::dds_::Int32_")
 */
#define MAX_TYPE_NAME_LEN 256

/**
 * Maximum length of a type hash (RIHS format)
 */
#define MAX_TYPE_HASH_LEN 128

/**
 * Maximum number of handles in an executor
 */
#define NANO_ROS_EXECUTOR_MAX_HANDLES 16

/**
 * Client state
 */
typedef enum nano_ros_client_state_t {
  /**
   * Not initialized
   */
  NANO_ROS_CLIENT_STATE_UNINITIALIZED = 0,
  /**
   * Initialized and ready
   */
  NANO_ROS_CLIENT_STATE_INITIALIZED = 1,
  /**
   * Shutdown
   */
  NANO_ROS_CLIENT_STATE_SHUTDOWN = 2,
} nano_ros_client_state_t;

/**
 * Handle type for executor
 */
typedef enum nano_ros_executor_handle_type_t {
  /**
   * No handle (empty slot)
   */
  NANO_ROS_EXECUTOR_HANDLE_NONE = 0,
  /**
   * Subscription handle
   */
  NANO_ROS_EXECUTOR_HANDLE_SUBSCRIPTION = 1,
  /**
   * Timer handle
   */
  NANO_ROS_EXECUTOR_HANDLE_TIMER = 2,
  /**
   * Service handle
   */
  NANO_ROS_EXECUTOR_HANDLE_SERVICE = 3,
  /**
   * Client handle
   */
  NANO_ROS_EXECUTOR_HANDLE_CLIENT = 4,
} nano_ros_executor_handle_type_t;

/**
 * Callback invocation mode
 */
typedef enum nano_ros_executor_invocation_t {
  /**
   * Only invoke callback when new data is available
   */
  NANO_ROS_EXECUTOR_ON_NEW_DATA = 0,
  /**
   * Always invoke callback (even with NULL data)
   */
  NANO_ROS_EXECUTOR_ALWAYS = 1,
} nano_ros_executor_invocation_t;

/**
 * Executor state
 */
typedef enum nano_ros_executor_state_t {
  /**
   * Not initialized
   */
  NANO_ROS_EXECUTOR_STATE_UNINITIALIZED = 0,
  /**
   * Initialized and ready
   */
  NANO_ROS_EXECUTOR_STATE_INITIALIZED = 1,
  /**
   * Currently spinning
   */
  NANO_ROS_EXECUTOR_STATE_SPINNING = 2,
  /**
   * Shutdown
   */
  NANO_ROS_EXECUTOR_STATE_SHUTDOWN = 3,
} nano_ros_executor_state_t;

/**
 * Node state
 */
typedef enum nano_ros_node_state_t {
  /**
   * Not initialized
   */
  NANO_ROS_NODE_STATE_UNINITIALIZED = 0,
  /**
   * Initialized and ready
   */
  NANO_ROS_NODE_STATE_INITIALIZED = 1,
  /**
   * Shutdown
   */
  NANO_ROS_NODE_STATE_SHUTDOWN = 2,
} nano_ros_node_state_t;

/**
 * Publisher state
 */
typedef enum nano_ros_publisher_state_t {
  /**
   * Not initialized
   */
  NANO_ROS_PUBLISHER_STATE_UNINITIALIZED = 0,
  /**
   * Initialized and ready
   */
  NANO_ROS_PUBLISHER_STATE_INITIALIZED = 1,
  /**
   * Shutdown
   */
  NANO_ROS_PUBLISHER_STATE_SHUTDOWN = 2,
} nano_ros_publisher_state_t;

/**
 * QoS durability policy
 */
typedef enum nano_ros_qos_durability_t {
  /**
   * Volatile - no persistence
   */
  NANO_ROS_QOS_DURABILITY_VOLATILE = 0,
  /**
   * Transient local - persist for late joiners
   */
  NANO_ROS_QOS_DURABILITY_TRANSIENT_LOCAL = 1,
} nano_ros_qos_durability_t;

/**
 * QoS history policy
 */
typedef enum nano_ros_qos_history_t {
  /**
   * Keep last N samples
   */
  NANO_ROS_QOS_HISTORY_KEEP_LAST = 0,
  /**
   * Keep all samples
   */
  NANO_ROS_QOS_HISTORY_KEEP_ALL = 1,
} nano_ros_qos_history_t;

/**
 * QoS reliability policy
 */
typedef enum nano_ros_qos_reliability_t {
  /**
   * Best effort delivery - no guarantees
   */
  NANO_ROS_QOS_RELIABILITY_BEST_EFFORT = 0,
  /**
   * Reliable delivery - retransmit if needed
   */
  NANO_ROS_QOS_RELIABILITY_RELIABLE = 1,
} nano_ros_qos_reliability_t;

/**
 * Service server state
 */
typedef enum nano_ros_service_state_t {
  /**
   * Not initialized
   */
  NANO_ROS_SERVICE_STATE_UNINITIALIZED = 0,
  /**
   * Initialized and ready
   */
  NANO_ROS_SERVICE_STATE_INITIALIZED = 1,
  /**
   * Shutdown
   */
  NANO_ROS_SERVICE_STATE_SHUTDOWN = 2,
} nano_ros_service_state_t;

/**
 * Subscription state
 */
typedef enum nano_ros_subscription_state_t {
  /**
   * Not initialized
   */
  NANO_ROS_SUBSCRIPTION_STATE_UNINITIALIZED = 0,
  /**
   * Initialized and ready
   */
  NANO_ROS_SUBSCRIPTION_STATE_INITIALIZED = 1,
  /**
   * Shutdown
   */
  NANO_ROS_SUBSCRIPTION_STATE_SHUTDOWN = 2,
} nano_ros_subscription_state_t;

/**
 * Support context state
 */
typedef enum nano_ros_support_state_t {
  /**
   * Not initialized
   */
  NANO_ROS_SUPPORT_STATE_UNINITIALIZED = 0,
  /**
   * Initialized and ready
   */
  NANO_ROS_SUPPORT_STATE_INITIALIZED = 1,
  /**
   * Shutdown
   */
  NANO_ROS_SUPPORT_STATE_SHUTDOWN = 2,
} nano_ros_support_state_t;

/**
 * Timer state
 */
typedef enum nano_ros_timer_state_t {
  /**
   * Not initialized
   */
  NANO_ROS_TIMER_STATE_UNINITIALIZED = 0,
  /**
   * Initialized and running
   */
  NANO_ROS_TIMER_STATE_RUNNING = 1,
  /**
   * Initialized but canceled
   */
  NANO_ROS_TIMER_STATE_CANCELED = 2,
  /**
   * Shutdown
   */
  NANO_ROS_TIMER_STATE_SHUTDOWN = 3,
} nano_ros_timer_state_t;

/**
 * Executor handle (union-like structure)
 */
typedef struct nano_ros_executor_handle_t {
  /**
   * Handle type
   */
  enum nano_ros_executor_handle_type_t handle_type;
  /**
   * Invocation mode (for subscriptions)
   */
  enum nano_ros_executor_invocation_t invocation;
  /**
   * Handle pointer (type depends on handle_type)
   */
  void *handle;
  /**
   * Flag indicating if handle has new data ready
   */
  bool data_ready;
} nano_ros_executor_handle_t;

/**
 * Support context structure.
 *
 * This is the main context for nano-ros, similar to rclc_support_t.
 * It manages the zenoh session and provides shared resources.
 */
typedef struct nano_ros_support_t {
  /**
   * Current state
   */
  enum nano_ros_support_state_t state;
  /**
   * Domain ID (ROS_DOMAIN_ID)
   */
  uint8_t domain_id;
  /**
   * Locator string storage
   */
  uint8_t locator[MAX_LOCATOR_LEN];
  /**
   * Locator string length
   */
  uintptr_t locator_len;
  /**
   * Opaque pointer to internal Rust context
   * This will hold a pointer to the zenoh session
   */
  void *internal;
} nano_ros_support_t;

/**
 * Executor structure.
 *
 * The executor manages a fixed array of handles and processes them
 * in the order they were added.
 */
typedef struct nano_ros_executor_t {
  /**
   * Current state
   */
  enum nano_ros_executor_state_t state;
  /**
   * Handle array
   */
  struct nano_ros_executor_handle_t handles[NANO_ROS_EXECUTOR_MAX_HANDLES];
  /**
   * Number of handles in use
   */
  uintptr_t handle_count;
  /**
   * Maximum handles (configured at init)
   */
  uintptr_t max_handles;
  /**
   * Timeout in nanoseconds for spin_some
   */
  uint64_t timeout_ns;
  /**
   * Pointer to support context
   */
  const struct nano_ros_support_t *support;
} nano_ros_executor_t;

/**
 * Return type for nano-ros C API functions.
 *
 * Compatible with rcl_ret_t for familiarity.
 */
typedef int nano_ros_ret_t;

/**
 * Subscription callback function type.
 *
 * # Parameters
 * * `data` - Pointer to received CDR-serialized message data
 * * `len` - Length of data in bytes
 * * `context` - User-provided context pointer
 */
typedef void (*nano_ros_subscription_callback_t)(const uint8_t *data,
                                                 uintptr_t len,
                                                 void *context);

/**
 * Node structure.
 *
 * Represents a ROS 2 node with a name and namespace.
 */
typedef struct nano_ros_node_t {
  /**
   * Current state
   */
  enum nano_ros_node_state_t state;
  /**
   * Node name storage
   */
  uint8_t name[MAX_NAME_LEN];
  /**
   * Node name length
   */
  uintptr_t name_len;
  /**
   * Namespace storage
   */
  uint8_t namespace_[MAX_NAMESPACE_LEN];
  /**
   * Namespace length
   */
  uintptr_t namespace_len;
  /**
   * Pointer to parent support context
   */
  const struct nano_ros_support_t *support;
  /**
   * Opaque pointer to internal Rust node
   */
  void *internal;
} nano_ros_node_t;

/**
 * Subscription structure.
 */
typedef struct nano_ros_subscription_t {
  /**
   * Current state
   */
  enum nano_ros_subscription_state_t state;
  /**
   * Topic name storage
   */
  uint8_t topic_name[MAX_TOPIC_LEN];
  /**
   * Topic name length
   */
  uintptr_t topic_name_len;
  /**
   * Type name storage
   */
  uint8_t type_name[MAX_TYPE_NAME_LEN];
  /**
   * Type name length
   */
  uintptr_t type_name_len;
  /**
   * Type hash storage
   */
  uint8_t type_hash[MAX_TYPE_HASH_LEN];
  /**
   * Type hash length
   */
  uintptr_t type_hash_len;
  /**
   * User callback function
   */
  nano_ros_subscription_callback_t callback;
  /**
   * User context pointer
   */
  void *context;
  /**
   * Pointer to parent node
   */
  const struct nano_ros_node_t *node;
  /**
   * Opaque pointer to internal Rust subscriber
   */
  void *internal;
} nano_ros_subscription_t;

/**
 * Timer callback function type.
 *
 * # Parameters
 * * `timer` - Pointer to the timer that triggered
 * * `context` - User-provided context pointer
 */
typedef void (*nano_ros_timer_callback_t)(struct nano_ros_timer_t *timer,
                                          void *context);

/**
 * Timer structure.
 */
typedef struct nano_ros_timer_t {
  /**
   * Current state
   */
  enum nano_ros_timer_state_t state;
  /**
   * Period in nanoseconds
   */
  uint64_t period_ns;
  /**
   * Last trigger time in nanoseconds
   */
  uint64_t last_call_time_ns;
  /**
   * User callback function
   */
  nano_ros_timer_callback_t callback;
  /**
   * User context pointer
   */
  void *context;
  /**
   * Pointer to parent support context
   */
  const struct nano_ros_support_t *support;
} nano_ros_timer_t;

/**
 * Service server callback function type.
 *
 * # Parameters
 * * `request_data` - Pointer to CDR-serialized request data
 * * `request_len` - Length of request data in bytes
 * * `response_data` - Pointer to buffer for CDR-serialized response
 * * `response_capacity` - Capacity of response buffer
 * * `response_len` - Output: actual length of response data written
 * * `context` - User-provided context pointer
 *
 * # Returns
 * * `true` if the request was handled successfully
 * * `false` if there was an error handling the request
 */
typedef bool (*nano_ros_service_callback_t)(const uint8_t *request_data,
                                            uintptr_t request_len,
                                            uint8_t *response_data,
                                            uintptr_t response_capacity,
                                            uintptr_t *response_len,
                                            void *context);

/**
 * Service server structure.
 */
typedef struct nano_ros_service_t {
  /**
   * Current state
   */
  enum nano_ros_service_state_t state;
  /**
   * Service name storage
   */
  uint8_t service_name[MAX_SERVICE_NAME_LEN];
  /**
   * Service name length
   */
  uintptr_t service_name_len;
  /**
   * Type name storage
   */
  uint8_t type_name[MAX_TYPE_NAME_LEN];
  /**
   * Type name length
   */
  uintptr_t type_name_len;
  /**
   * Type hash storage
   */
  uint8_t type_hash[MAX_TYPE_HASH_LEN];
  /**
   * Type hash length
   */
  uintptr_t type_hash_len;
  /**
   * User callback function
   */
  nano_ros_service_callback_t callback;
  /**
   * User context pointer
   */
  void *context;
  /**
   * Pointer to parent node
   */
  const struct nano_ros_node_t *node;
  /**
   * Opaque pointer to internal Rust service server
   */
  void *internal;
} nano_ros_service_t;

/**
 * Publisher structure.
 */
typedef struct nano_ros_publisher_t {
  /**
   * Current state
   */
  enum nano_ros_publisher_state_t state;
  /**
   * Topic name storage
   */
  uint8_t topic_name[MAX_TOPIC_LEN];
  /**
   * Topic name length
   */
  uintptr_t topic_name_len;
  /**
   * Type name storage
   */
  uint8_t type_name[MAX_TYPE_NAME_LEN];
  /**
   * Type name length
   */
  uintptr_t type_name_len;
  /**
   * Type hash storage
   */
  uint8_t type_hash[MAX_TYPE_HASH_LEN];
  /**
   * Type hash length
   */
  uintptr_t type_hash_len;
  /**
   * Pointer to parent node
   */
  const struct nano_ros_node_t *node;
  /**
   * Opaque pointer to internal Rust publisher
   */
  void *internal;
} nano_ros_publisher_t;

/**
 * Message type information.
 *
 * This structure describes a ROS message type for use with publishers
 * and subscribers.
 */
typedef struct nano_ros_message_type_t {
  /**
   * Type name (e.g., "std_msgs::msg::dds_::Int32")
   */
  const char *type_name;
  /**
   * Type hash (RIHS format)
   */
  const char *type_hash;
  /**
   * Maximum serialized size (0 = dynamic/unknown)
   */
  uintptr_t serialized_size_max;
} nano_ros_message_type_t;

/**
 * QoS settings structure
 */
typedef struct nano_ros_qos_t {
  /**
   * Reliability policy
   */
  enum nano_ros_qos_reliability_t reliability;
  /**
   * Durability policy
   */
  enum nano_ros_qos_durability_t durability;
  /**
   * History policy
   */
  enum nano_ros_qos_history_t history;
  /**
   * History depth (for KEEP_LAST)
   */
  int depth;
} nano_ros_qos_t;

/**
 * Service client structure.
 */
typedef struct nano_ros_client_t {
  /**
   * Current state
   */
  enum nano_ros_client_state_t state;
  /**
   * Service name storage
   */
  uint8_t service_name[MAX_SERVICE_NAME_LEN];
  /**
   * Service name length
   */
  uintptr_t service_name_len;
  /**
   * Type name storage
   */
  uint8_t type_name[MAX_TYPE_NAME_LEN];
  /**
   * Type name length
   */
  uintptr_t type_name_len;
  /**
   * Type hash storage
   */
  uint8_t type_hash[MAX_TYPE_HASH_LEN];
  /**
   * Type hash length
   */
  uintptr_t type_hash_len;
  /**
   * Pointer to parent node
   */
  const struct nano_ros_node_t *node;
  /**
   * Opaque pointer to internal Rust service client
   */
  void *internal;
} nano_ros_client_t;

/**
 * Success
 */
#define NANO_ROS_RET_OK 0

/**
 * Generic error
 */
#define NANO_ROS_RET_ERROR -1

/**
 * Timeout occurred
 */
#define NANO_ROS_RET_TIMEOUT -2

/**
 * Invalid argument passed
 */
#define NANO_ROS_RET_INVALID_ARGUMENT -3

/**
 * Resource not found
 */
#define NANO_ROS_RET_NOT_FOUND -4

/**
 * Resource already exists
 */
#define NANO_ROS_RET_ALREADY_EXISTS -5

/**
 * Resource limit reached (e.g., max handles)
 */
#define NANO_ROS_RET_FULL -6

/**
 * Not initialized
 */
#define NANO_ROS_RET_NOT_INIT -7

/**
 * Bad sequence (e.g., wrong order of operations)
 */
#define NANO_ROS_RET_BAD_SEQUENCE -8

/**
 * Service call failed
 */
#define NANO_ROS_RET_SERVICE_FAILED -9

/**
 * Publish failed
 */
#define NANO_ROS_RET_PUBLISH_FAILED -10

/**
 * Subscription failed
 */
#define NANO_ROS_RET_SUBSCRIPTION_FAILED -11

#ifdef __cplusplus
extern "C" {
#endif // __cplusplus

extern const struct nano_ros_qos_t NANO_ROS_QOS_DEFAULT;

extern const struct nano_ros_qos_t NANO_ROS_QOS_SENSOR_DATA;

extern const struct nano_ros_qos_t NANO_ROS_QOS_SERVICES;

/**
 * Write a boolean value to the buffer.
 *
 * # Safety
 * - `ptr` must point to a valid mutable pointer to a buffer
 * - The buffer must have sufficient space
 */

int32_t nano_ros_cdr_write_bool(uint8_t **ptr,
                                const uint8_t *end,
                                bool value)
;

/**
 * Write a u8 value to the buffer.
 */

int32_t nano_ros_cdr_write_u8(uint8_t **ptr,
                              const uint8_t *end,
                              uint8_t value)
;

/**
 * Write an i8 value to the buffer.
 */

int32_t nano_ros_cdr_write_i8(uint8_t **ptr,
                              const uint8_t *end,
                              int8_t value)
;

/**
 * Write a u16 value to the buffer (with alignment).
 */

int32_t nano_ros_cdr_write_u16(uint8_t **ptr,
                               const uint8_t *end,
                               uint16_t value)
;

/**
 * Write an i16 value to the buffer.
 */

int32_t nano_ros_cdr_write_i16(uint8_t **ptr,
                               const uint8_t *end,
                               int16_t value)
;

/**
 * Write a u32 value to the buffer (with alignment).
 */

int32_t nano_ros_cdr_write_u32(uint8_t **ptr,
                               const uint8_t *end,
                               uint32_t value)
;

/**
 * Write an i32 value to the buffer.
 */

int32_t nano_ros_cdr_write_i32(uint8_t **ptr,
                               const uint8_t *end,
                               int32_t value)
;

/**
 * Write a u64 value to the buffer (with alignment).
 */

int32_t nano_ros_cdr_write_u64(uint8_t **ptr,
                               const uint8_t *end,
                               uint64_t value)
;

/**
 * Write an i64 value to the buffer.
 */

int32_t nano_ros_cdr_write_i64(uint8_t **ptr,
                               const uint8_t *end,
                               int64_t value)
;

/**
 * Write a f32 value to the buffer (with alignment).
 */

int32_t nano_ros_cdr_write_f32(uint8_t **ptr,
                               const uint8_t *end,
                               float value)
;

/**
 * Write a f64 value to the buffer (with alignment).
 */

int32_t nano_ros_cdr_write_f64(uint8_t **ptr,
                               const uint8_t *end,
                               double value)
;

/**
 * Write a string to the buffer (length-prefixed).
 *
 * CDR strings are encoded as: u32 length (including null terminator) + bytes + null terminator
 */

int32_t nano_ros_cdr_write_string(uint8_t **ptr,
                                  const uint8_t *end,
                                  const char *value)
;

/**
 * Read a boolean value from the buffer.
 */

int32_t nano_ros_cdr_read_bool(const uint8_t **ptr,
                               const uint8_t *end,
                               bool *value)
;

/**
 * Read a u8 value from the buffer.
 */

int32_t nano_ros_cdr_read_u8(const uint8_t **ptr,
                             const uint8_t *end,
                             uint8_t *value)
;

/**
 * Read an i8 value from the buffer.
 */

int32_t nano_ros_cdr_read_i8(const uint8_t **ptr,
                             const uint8_t *end,
                             int8_t *value)
;

/**
 * Read a u16 value from the buffer (with alignment).
 */

int32_t nano_ros_cdr_read_u16(const uint8_t **ptr,
                              const uint8_t *end,
                              uint16_t *value)
;

/**
 * Read an i16 value from the buffer.
 */

int32_t nano_ros_cdr_read_i16(const uint8_t **ptr,
                              const uint8_t *end,
                              int16_t *value)
;

/**
 * Read a u32 value from the buffer (with alignment).
 */

int32_t nano_ros_cdr_read_u32(const uint8_t **ptr,
                              const uint8_t *end,
                              uint32_t *value)
;

/**
 * Read an i32 value from the buffer.
 */

int32_t nano_ros_cdr_read_i32(const uint8_t **ptr,
                              const uint8_t *end,
                              int32_t *value)
;

/**
 * Read a u64 value from the buffer (with alignment).
 */

int32_t nano_ros_cdr_read_u64(const uint8_t **ptr,
                              const uint8_t *end,
                              uint64_t *value)
;

/**
 * Read an i64 value from the buffer.
 */

int32_t nano_ros_cdr_read_i64(const uint8_t **ptr,
                              const uint8_t *end,
                              int64_t *value)
;

/**
 * Read a f32 value from the buffer.
 */

int32_t nano_ros_cdr_read_f32(const uint8_t **ptr,
                              const uint8_t *end,
                              float *value)
;

/**
 * Read a f64 value from the buffer.
 */

int32_t nano_ros_cdr_read_f64(const uint8_t **ptr,
                              const uint8_t *end,
                              double *value)
;

/**
 * Read a string from the buffer into a fixed-size buffer.
 *
 * CDR strings are encoded as: u32 length (including null terminator) + bytes + null terminator
 */

int32_t nano_ros_cdr_read_string(const uint8_t **ptr,
                                 const uint8_t *end,
                                 char *value,
                                 uintptr_t max_len)
;

/**
 * Get a zero-initialized executor.
 */

struct nano_ros_executor_t nano_ros_executor_get_zero_initialized(void)
;

/**
 * Initialize an executor.
 *
 * # Parameters
 * * `executor` - Pointer to a zero-initialized executor
 * * `support` - Pointer to an initialized support context
 * * `max_handles` - Maximum number of handles (capped at NANO_ROS_EXECUTOR_MAX_HANDLES)
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if any pointer is NULL or max_handles is 0
 * * `NANO_ROS_RET_NOT_INIT` if support is not initialized
 *
 * # Safety
 * * All pointers must be valid
 */

nano_ros_ret_t nano_ros_executor_init(struct nano_ros_executor_t *executor,
                                      const struct nano_ros_support_t *support,
                                      uintptr_t max_handles)
;

/**
 * Set the executor timeout.
 *
 * # Parameters
 * * `executor` - Pointer to an initialized executor
 * * `timeout_ns` - Timeout in nanoseconds for spin_some
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if executor is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 */

nano_ros_ret_t nano_ros_executor_set_timeout(struct nano_ros_executor_t *executor,
                                             uint64_t timeout_ns)
;

/**
 * Add a subscription to the executor.
 *
 * # Parameters
 * * `executor` - Pointer to an initialized executor
 * * `subscription` - Pointer to an initialized subscription
 * * `invocation` - When to invoke the callback
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if any pointer is NULL
 * * `NANO_ROS_RET_FULL` if executor is full
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 *
 * # Safety
 * * All pointers must be valid and point to initialized objects
 */

nano_ros_ret_t nano_ros_executor_add_subscription(struct nano_ros_executor_t *executor,
                                                  struct nano_ros_subscription_t *subscription,
                                                  enum nano_ros_executor_invocation_t invocation)
;

/**
 * Add a timer to the executor.
 *
 * # Parameters
 * * `executor` - Pointer to an initialized executor
 * * `timer` - Pointer to an initialized timer
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if any pointer is NULL
 * * `NANO_ROS_RET_FULL` if executor is full
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 *
 * # Safety
 * * All pointers must be valid and point to initialized objects
 */

nano_ros_ret_t nano_ros_executor_add_timer(struct nano_ros_executor_t *executor,
                                           struct nano_ros_timer_t *timer)
;

/**
 * Add a service to the executor.
 *
 * # Parameters
 * * `executor` - Pointer to an initialized executor
 * * `service` - Pointer to an initialized service
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if any pointer is NULL
 * * `NANO_ROS_RET_FULL` if executor is full
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 *
 * # Safety
 * * All pointers must be valid and point to initialized objects
 */

nano_ros_ret_t nano_ros_executor_add_service(struct nano_ros_executor_t *executor,
                                             struct nano_ros_service_t *service)
;

/**
 * Spin the executor once.
 *
 * This function checks for ready handles and processes them once.
 *
 * # Parameters
 * * `executor` - Pointer to an initialized executor
 * * `timeout_ns` - Timeout in nanoseconds (0 for non-blocking)
 *
 * # Returns
 * * `NANO_ROS_RET_OK` if callbacks were executed
 * * `NANO_ROS_RET_TIMEOUT` if no callbacks were ready within timeout
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if executor is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 *
 * # Safety
 * * `executor` must be a valid pointer to an initialized executor
 */

nano_ros_ret_t nano_ros_executor_spin_some(struct nano_ros_executor_t *executor,
                                           uint64_t timeout_ns)
;

/**
 * Spin the executor forever.
 *
 * This function continuously processes callbacks until shutdown.
 *
 * # Parameters
 * * `executor` - Pointer to an initialized executor
 *
 * # Returns
 * * `NANO_ROS_RET_OK` if shutdown gracefully
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if executor is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 *
 * # Safety
 * * `executor` must be a valid pointer to an initialized executor
 */

nano_ros_ret_t nano_ros_executor_spin(struct nano_ros_executor_t *executor)
;

/**
 * Spin the executor with a fixed period.
 *
 * This function processes callbacks at a fixed rate.
 *
 * # Parameters
 * * `executor` - Pointer to an initialized executor
 * * `period_ns` - Period in nanoseconds
 *
 * # Returns
 * * `NANO_ROS_RET_OK` if shutdown gracefully
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if executor is NULL or period is 0
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 *
 * # Safety
 * * `executor` must be a valid pointer to an initialized executor
 */

nano_ros_ret_t nano_ros_executor_spin_period(struct nano_ros_executor_t *executor,
                                             uint64_t period_ns)
;

/**
 * Stop a spinning executor.
 *
 * # Parameters
 * * `executor` - Pointer to a spinning executor
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if executor is NULL
 */

nano_ros_ret_t nano_ros_executor_stop(struct nano_ros_executor_t *executor)
;

/**
 * Finalize an executor.
 *
 * # Parameters
 * * `executor` - Pointer to an initialized executor
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if executor is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 *
 * # Safety
 * * `executor` must be a valid pointer
 */

nano_ros_ret_t nano_ros_executor_fini(struct nano_ros_executor_t *executor)
;

/**
 * Get the number of handles in the executor.
 */

int nano_ros_executor_get_handle_count(const struct nano_ros_executor_t *executor)
;

/**
 * Check if executor is valid (initialized).
 */

int nano_ros_executor_is_valid(const struct nano_ros_executor_t *executor)
;

/**
 * Get a zero-initialized node.
 *
 * # Safety
 * Returns a stack-allocated struct that must be initialized before use.
 */

struct nano_ros_node_t nano_ros_node_get_zero_initialized(void)
;

/**
 * Initialize a node with default options.
 *
 * # Parameters
 * * `node` - Pointer to a zero-initialized node
 * * `support` - Pointer to an initialized support context
 * * `name` - Node name (null-terminated string)
 * * `namespace_` - Node namespace (null-terminated string, use "/" for root)
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if any pointer is NULL or strings are invalid
 * * `NANO_ROS_RET_NOT_INIT` if support is not initialized
 * * `NANO_ROS_RET_ERROR` on initialization failure
 *
 * # Safety
 * * All pointers must be valid
 * * `name` and `namespace_` must be valid null-terminated strings
 */

nano_ros_ret_t nano_ros_node_init(struct nano_ros_node_t *node,
                                  const struct nano_ros_support_t *support,
                                  const char *name,
                                  const char *namespace_)
;

/**
 * Finalize a node.
 *
 * # Parameters
 * * `node` - Pointer to an initialized node
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if node is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 *
 * # Safety
 * * `node` must be a valid pointer to an initialized nano_ros_node_t
 */

nano_ros_ret_t nano_ros_node_fini(struct nano_ros_node_t *node)
;

/**
 * Get the node name.
 *
 * # Parameters
 * * `node` - Pointer to an initialized node
 *
 * # Returns
 * * Pointer to the node name (null-terminated), or NULL if invalid
 *
 * # Safety
 * * `node` must be a valid pointer
 */

const char *nano_ros_node_get_name(const struct nano_ros_node_t *node)
;

/**
 * Get the node namespace.
 *
 * # Parameters
 * * `node` - Pointer to an initialized node
 *
 * # Returns
 * * Pointer to the node namespace (null-terminated), or NULL if invalid
 *
 * # Safety
 * * `node` must be a valid pointer
 */

const char *nano_ros_node_get_namespace(const struct nano_ros_node_t *node)
;

/**
 * Get a zero-initialized publisher.
 */

struct nano_ros_publisher_t nano_ros_publisher_get_zero_initialized(void)
;

/**
 * Initialize a publisher with default QoS.
 *
 * # Parameters
 * * `publisher` - Pointer to a zero-initialized publisher
 * * `node` - Pointer to an initialized node
 * * `type_info` - Pointer to message type information
 * * `topic_name` - Topic name (null-terminated string)
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if any pointer is NULL
 * * `NANO_ROS_RET_NOT_INIT` if node is not initialized
 * * `NANO_ROS_RET_ERROR` on initialization failure
 *
 * # Safety
 * * All pointers must be valid
 * * `topic_name` must be a valid null-terminated string
 */

nano_ros_ret_t nano_ros_publisher_init(struct nano_ros_publisher_t *publisher,
                                       const struct nano_ros_node_t *node,
                                       const struct nano_ros_message_type_t *type_info,
                                       const char *topic_name)
;

/**
 * Initialize a publisher with custom QoS.
 *
 * # Parameters
 * * `publisher` - Pointer to a zero-initialized publisher
 * * `node` - Pointer to an initialized node
 * * `type_info` - Pointer to message type information
 * * `topic_name` - Topic name (null-terminated string)
 * * `qos` - Pointer to QoS settings (NULL for default)
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if any required pointer is NULL
 * * `NANO_ROS_RET_NOT_INIT` if node is not initialized
 * * `NANO_ROS_RET_ERROR` on initialization failure
 *
 * # Safety
 * * All required pointers must be valid
 * * `topic_name` must be a valid null-terminated string
 */

nano_ros_ret_t nano_ros_publisher_init_with_qos(struct nano_ros_publisher_t *publisher,
                                                const struct nano_ros_node_t *node,
                                                const struct nano_ros_message_type_t *type_info,
                                                const char *topic_name,
                                                const struct nano_ros_qos_t *qos)
;

/**
 * Publish raw CDR-serialized data.
 *
 * # Parameters
 * * `publisher` - Pointer to an initialized publisher
 * * `data` - Pointer to CDR-serialized message data
 * * `len` - Length of data in bytes
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if any pointer is NULL or len is 0
 * * `NANO_ROS_RET_NOT_INIT` if publisher is not initialized
 * * `NANO_ROS_RET_PUBLISH_FAILED` on publish failure
 *
 * # Safety
 * * `publisher` must be a valid pointer to an initialized publisher
 * * `data` must be a valid pointer to `len` bytes
 */

nano_ros_ret_t nano_ros_publish_raw(const struct nano_ros_publisher_t *publisher,
                                    const uint8_t *data,
                                    uintptr_t len)
;

/**
 * Finalize a publisher.
 *
 * # Parameters
 * * `publisher` - Pointer to an initialized publisher
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if publisher is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 *
 * # Safety
 * * `publisher` must be a valid pointer
 */

nano_ros_ret_t nano_ros_publisher_fini(struct nano_ros_publisher_t *publisher)
;

/**
 * Get the topic name of a publisher.
 *
 * # Parameters
 * * `publisher` - Pointer to a publisher
 *
 * # Returns
 * * Pointer to topic name (null-terminated), or NULL if invalid
 */

const char *nano_ros_publisher_get_topic_name(const struct nano_ros_publisher_t *publisher)
;

/**
 * Check if publisher is valid (initialized).
 *
 * # Parameters
 * * `publisher` - Pointer to a publisher
 *
 * # Returns
 * * Non-zero if valid, 0 if invalid or NULL
 */

int nano_ros_publisher_is_valid(const struct nano_ros_publisher_t *publisher)
;

/**
 * Get a zero-initialized service server.
 */

struct nano_ros_service_t nano_ros_service_get_zero_initialized(void)
;

/**
 * Initialize a service server.
 *
 * # Parameters
 * * `service` - Pointer to a zero-initialized service
 * * `node` - Pointer to an initialized node
 * * `type_info` - Pointer to service type information
 * * `service_name` - Service name (null-terminated string)
 * * `callback` - Callback function to invoke when requests arrive
 * * `context` - User context pointer passed to callback (can be NULL)
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if any required pointer is NULL
 * * `NANO_ROS_RET_NOT_INIT` if node is not initialized
 * * `NANO_ROS_RET_ERROR` on initialization failure
 */

nano_ros_ret_t nano_ros_service_init(struct nano_ros_service_t *service,
                                     const struct nano_ros_node_t *node,
                                     const struct nano_ros_message_type_t *type_info,
                                     const char *service_name,
                                     nano_ros_service_callback_t callback,
                                     void *context)
;

/**
 * Finalize a service server.
 *
 * # Parameters
 * * `service` - Pointer to an initialized service
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if service is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 */

nano_ros_ret_t nano_ros_service_fini(struct nano_ros_service_t *service)
;

/**
 * Take a service request (non-blocking).
 *
 * # Parameters
 * * `service` - Pointer to an initialized service
 * * `request_data` - Buffer to receive CDR-serialized request data
 * * `request_capacity` - Capacity of request buffer
 * * `request_len` - Output: actual length of request data
 * * `sequence_number` - Output: sequence number for response matching
 *
 * # Returns
 * * `NANO_ROS_RET_OK` if a request was received
 * * `NANO_ROS_RET_TIMEOUT` if no request is available
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if any pointer is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 */

nano_ros_ret_t nano_ros_service_take_request(struct nano_ros_service_t *service,
                                             uint8_t *request_data,
                                             uintptr_t request_capacity,
                                             uintptr_t *request_len,
                                             int64_t *sequence_number)
;

/**
 * Send a service response.
 *
 * # Parameters
 * * `service` - Pointer to an initialized service
 * * `sequence_number` - Sequence number from the request
 * * `response_data` - CDR-serialized response data
 * * `response_len` - Length of response data
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if any pointer is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 * * `NANO_ROS_RET_ERROR` on send failure
 */

nano_ros_ret_t nano_ros_service_send_response(struct nano_ros_service_t *service,
                                              int64_t sequence_number,
                                              const uint8_t *response_data,
                                              uintptr_t response_len)
;

/**
 * Get the service name.
 *
 * # Parameters
 * * `service` - Pointer to a service
 *
 * # Returns
 * * Pointer to service name (null-terminated), or NULL if invalid
 */

const char *nano_ros_service_get_service_name(const struct nano_ros_service_t *service)
;

/**
 * Check if service is valid (initialized).
 *
 * # Parameters
 * * `service` - Pointer to a service
 *
 * # Returns
 * * Non-zero if valid, 0 if invalid or NULL
 */

int nano_ros_service_is_valid(const struct nano_ros_service_t *service)
;

/**
 * Get a zero-initialized client.
 */

struct nano_ros_client_t nano_ros_client_get_zero_initialized(void)
;

/**
 * Initialize a service client.
 *
 * # Parameters
 * * `client` - Pointer to a zero-initialized client
 * * `node` - Pointer to an initialized node
 * * `type_info` - Pointer to service type information
 * * `service_name` - Service name (null-terminated string)
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if any required pointer is NULL
 * * `NANO_ROS_RET_NOT_INIT` if node is not initialized
 * * `NANO_ROS_RET_ERROR` on initialization failure
 */

nano_ros_ret_t nano_ros_client_init(struct nano_ros_client_t *client,
                                    const struct nano_ros_node_t *node,
                                    const struct nano_ros_message_type_t *type_info,
                                    const char *service_name)
;

/**
 * Finalize a service client.
 *
 * # Parameters
 * * `client` - Pointer to an initialized client
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if client is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 */

nano_ros_ret_t nano_ros_client_fini(struct nano_ros_client_t *client)
;

/**
 * Call a service (blocking).
 *
 * This function sends a request and blocks until a response is received
 * or a timeout occurs.
 *
 * # Parameters
 * * `client` - Pointer to an initialized client
 * * `request_data` - CDR-serialized request data
 * * `request_len` - Length of request data
 * * `response_data` - Buffer to receive CDR-serialized response
 * * `response_capacity` - Capacity of response buffer
 * * `response_len` - Output: actual length of response data
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if any pointer is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 * * `NANO_ROS_RET_TIMEOUT` if no response within timeout
 * * `NANO_ROS_RET_ERROR` on call failure
 */

nano_ros_ret_t nano_ros_client_call(struct nano_ros_client_t *client,
                                    const uint8_t *request_data,
                                    uintptr_t request_len,
                                    uint8_t *response_data,
                                    uintptr_t response_capacity,
                                    uintptr_t *response_len)
;

/**
 * Get the service name of a client.
 *
 * # Parameters
 * * `client` - Pointer to a client
 *
 * # Returns
 * * Pointer to service name (null-terminated), or NULL if invalid
 */

const char *nano_ros_client_get_service_name(const struct nano_ros_client_t *client)
;

/**
 * Check if client is valid (initialized).
 *
 * # Parameters
 * * `client` - Pointer to a client
 *
 * # Returns
 * * Non-zero if valid, 0 if invalid or NULL
 */

int nano_ros_client_is_valid(const struct nano_ros_client_t *client)
;

/**
 * Get a zero-initialized subscription.
 */

struct nano_ros_subscription_t nano_ros_subscription_get_zero_initialized(void)
;

/**
 * Initialize a subscription with default QoS.
 *
 * # Parameters
 * * `subscription` - Pointer to a zero-initialized subscription
 * * `node` - Pointer to an initialized node
 * * `type_info` - Pointer to message type information
 * * `topic_name` - Topic name (null-terminated string)
 * * `callback` - Callback function to invoke when messages arrive
 * * `context` - User context pointer passed to callback (can be NULL)
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if any required pointer is NULL
 * * `NANO_ROS_RET_NOT_INIT` if node is not initialized
 * * `NANO_ROS_RET_ERROR` on initialization failure
 *
 * # Safety
 * * All required pointers must be valid
 * * `topic_name` must be a valid null-terminated string
 * * `callback` must be a valid function pointer
 */

nano_ros_ret_t nano_ros_subscription_init(struct nano_ros_subscription_t *subscription,
                                          const struct nano_ros_node_t *node,
                                          const struct nano_ros_message_type_t *type_info,
                                          const char *topic_name,
                                          nano_ros_subscription_callback_t callback,
                                          void *context)
;

/**
 * Initialize a subscription with custom QoS.
 *
 * # Safety
 * See `nano_ros_subscription_init` for safety requirements.
 */

nano_ros_ret_t nano_ros_subscription_init_with_qos(struct nano_ros_subscription_t *subscription,
                                                   const struct nano_ros_node_t *node,
                                                   const struct nano_ros_message_type_t *type_info,
                                                   const char *topic_name,
                                                   nano_ros_subscription_callback_t callback,
                                                   void *context,
                                                   const struct nano_ros_qos_t *qos)
;

/**
 * Finalize a subscription.
 *
 * # Parameters
 * * `subscription` - Pointer to an initialized subscription
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if subscription is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 *
 * # Safety
 * * `subscription` must be a valid pointer
 */

nano_ros_ret_t nano_ros_subscription_fini(struct nano_ros_subscription_t *subscription)
;

/**
 * Get the topic name of a subscription.
 *
 * # Parameters
 * * `subscription` - Pointer to a subscription
 *
 * # Returns
 * * Pointer to topic name (null-terminated), or NULL if invalid
 */

const char *nano_ros_subscription_get_topic_name(const struct nano_ros_subscription_t *subscription)
;

/**
 * Check if subscription is valid (initialized).
 *
 * # Parameters
 * * `subscription` - Pointer to a subscription
 *
 * # Returns
 * * Non-zero if valid, 0 if invalid or NULL
 */

int nano_ros_subscription_is_valid(const struct nano_ros_subscription_t *subscription)
;

/**
 * Get a zero-initialized support context.
 *
 * # Safety
 * Returns a stack-allocated struct that must be initialized before use.
 */

struct nano_ros_support_t nano_ros_support_get_zero_initialized(void)
;

/**
 * Initialize the support context.
 *
 * This function initializes the zenoh session and prepares the context
 * for creating nodes, publishers, and subscribers.
 *
 * # Parameters
 * * `support` - Pointer to a zero-initialized support context
 * * `locator` - Zenoh locator string (e.g., "tcp/127.0.0.1:7447"), or NULL for default
 * * `domain_id` - ROS domain ID (0-232)
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if support is NULL
 * * `NANO_ROS_RET_ERROR` on initialization failure
 *
 * # Safety
 * * `support` must be a valid pointer to a zero-initialized nano_ros_support_t
 * * `locator` must be a valid null-terminated string or NULL
 */

nano_ros_ret_t nano_ros_support_init(struct nano_ros_support_t *support,
                                     const char *locator,
                                     uint8_t domain_id)
;

/**
 * Finalize the support context.
 *
 * This function closes the zenoh session and releases all resources.
 *
 * # Parameters
 * * `support` - Pointer to an initialized support context
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if support is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 *
 * # Safety
 * * `support` must be a valid pointer to an initialized nano_ros_support_t
 */

nano_ros_ret_t nano_ros_support_fini(struct nano_ros_support_t *support)
;

/**
 * Check if support context is valid (initialized).
 *
 * # Parameters
 * * `support` - Pointer to a support context
 *
 * # Returns
 * * Non-zero if valid, 0 if invalid or NULL
 */

int nano_ros_support_is_valid(const struct nano_ros_support_t *support)
;

/**
 * Get a zero-initialized timer.
 */

struct nano_ros_timer_t nano_ros_timer_get_zero_initialized(void)
;

/**
 * Initialize a timer.
 *
 * # Parameters
 * * `timer` - Pointer to a zero-initialized timer
 * * `support` - Pointer to an initialized support context
 * * `period_ns` - Timer period in nanoseconds
 * * `callback` - Callback function to invoke when timer fires
 * * `context` - User context pointer passed to callback (can be NULL)
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if any required pointer is NULL or period is 0
 * * `NANO_ROS_RET_NOT_INIT` if support is not initialized
 *
 * # Safety
 * * All required pointers must be valid
 * * `callback` must be a valid function pointer
 */

nano_ros_ret_t nano_ros_timer_init(struct nano_ros_timer_t *timer,
                                   const struct nano_ros_support_t *support,
                                   uint64_t period_ns,
                                   nano_ros_timer_callback_t callback,
                                   void *context)
;

/**
 * Cancel a timer.
 *
 * A canceled timer will not fire, but can be reset to start again.
 *
 * # Parameters
 * * `timer` - Pointer to an initialized timer
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if timer is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 */

nano_ros_ret_t nano_ros_timer_cancel(struct nano_ros_timer_t *timer)
;

/**
 * Reset a timer.
 *
 * This resets the timer's last call time and starts it running again
 * if it was canceled.
 *
 * # Parameters
 * * `timer` - Pointer to an initialized timer
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if timer is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 */

nano_ros_ret_t nano_ros_timer_reset(struct nano_ros_timer_t *timer)
;

/**
 * Finalize a timer.
 *
 * # Parameters
 * * `timer` - Pointer to an initialized timer
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if timer is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized
 */

nano_ros_ret_t nano_ros_timer_fini(struct nano_ros_timer_t *timer)
;

/**
 * Check if timer is ready to fire.
 *
 * # Parameters
 * * `timer` - Pointer to an initialized timer
 * * `current_time_ns` - Current time in nanoseconds
 *
 * # Returns
 * * Non-zero if timer is ready, 0 otherwise
 */

int nano_ros_timer_is_ready(const struct nano_ros_timer_t *timer,
                            uint64_t current_time_ns)
;

/**
 * Call the timer callback and update last call time.
 *
 * This is called by the executor when the timer is ready.
 *
 * # Parameters
 * * `timer` - Pointer to an initialized timer
 * * `current_time_ns` - Current time in nanoseconds
 *
 * # Returns
 * * `NANO_ROS_RET_OK` on success
 * * `NANO_ROS_RET_INVALID_ARGUMENT` if timer is NULL
 * * `NANO_ROS_RET_NOT_INIT` if not initialized or not running
 */

nano_ros_ret_t nano_ros_timer_call(struct nano_ros_timer_t *timer,
                                   uint64_t current_time_ns)
;

/**
 * Check if timer is valid (initialized and not shutdown).
 *
 * # Parameters
 * * `timer` - Pointer to a timer
 *
 * # Returns
 * * Non-zero if valid, 0 if invalid or NULL
 */

int nano_ros_timer_is_valid(const struct nano_ros_timer_t *timer)
;

/**
 * Get the timer period in nanoseconds.
 *
 * # Parameters
 * * `timer` - Pointer to a timer
 *
 * # Returns
 * * Period in nanoseconds, or 0 if invalid
 */

uint64_t nano_ros_timer_get_period(const struct nano_ros_timer_t *timer)
;

/**
 * Get the time until next timer firing.
 *
 * # Parameters
 * * `timer` - Pointer to a timer
 * * `current_time_ns` - Current time in nanoseconds
 *
 * # Returns
 * * Time until next firing in nanoseconds, or 0 if ready now or invalid
 */

uint64_t nano_ros_timer_get_time_until_next_call(const struct nano_ros_timer_t *timer,
                                                 uint64_t current_time_ns)
;

#ifdef __cplusplus
} // extern "C"
#endif // __cplusplus

#endif /* NANO_ROS_H */

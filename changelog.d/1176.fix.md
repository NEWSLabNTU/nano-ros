The three `parity_test` whole-package walks measured their parse/codegen failures and then passed; they now hold the result to a committed expected-failure ledger, exactly and in both directions, and a walk over the bundled interface sources gives that ledger a verdict on hosts with no ROS

The executor arena prices each declared subscription at its OWN type's receive bound instead of at one image-wide maximum: the message-bound inventory's join now publishes the per-type table it already computes, and `nros-node/build.rs` joins it against the declared-depth triples. A type the table does not price keeps the image-wide bound, so an image with no table is unchanged. Measured on the reference island's shape: 48,752 B of modelled arena becomes 22,832 B

The QoS status-event interop cell can run. `build_qos_event_probe` asked the fixture table for a row with an `rmw-zenoh` cargo FEATURE, but that crate has no `[features]` table and its row is a plain `rmw = "zenoh"` — so every run died at "no [[fixture]] row … with Selector {…}" before reaching a peer, which is why `native-qos-event-rust-zenoh-r2n` had never produced a verdict. With the selector corrected the cell passes against a live ROS 2 host: the probe sees a positive `alive_count_change` from a stock `demo_nodes_cpp` talker, and the same probe with no talker reports no event and exits 4.

`play_launch_parser` is now a prebuilt `dist` on linux-x86_64 and linux-arm64, not a source build: `NEWSLabNTU/nano-ros-sdk` tag `play_launch_parser-0.1.0-nros1` publishes the assets `scripts/build-play_launch_parser.sh` produces, and `nros-sdk-index.toml` names them with their sha256 and a MEASURED `floor = { glibc = "2.34" }`. The entry leaves the dist-or-reason ratchet and gains a measured `smoke` probe (`bin/play_launch_parser --help`), so it leaves the smoke-or-reason ratchet too. `system` gains `libpython310`: the dist links `libpython3.10.so.1.0` as a hard single-minor `DT_NEEDED` (pyo3 `auto-initialize`), which is what D1's backward half reads to refuse it on a host shipping 3.12 rather than download a binary that cannot start. macos-arm64 was BUILT and PUBLISHED and is deliberately NOT declared — its `LC_LOAD_DYLIB` names an absolute `/Library/Frameworks/Python.framework/Versions/3.14/Python` with no `@rpath`, so it cannot start on a mac lacking that exact framework, and the backward half that would catch it is Linux-only

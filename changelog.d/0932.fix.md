The arm64 `arm-none-eabi-gcc` dist (`13.2-nros5`) now ships `libffi.so.7` and `libmpdec.so.2` beside its bundled CPython, so `import ctypes` and `import decimal` work inside `arm-none-eabi-gdb`. Issue 0932's residue note named two unimportable lib-dynload modules and was written from reading; a sweep of all 44 found FOUR, and the one it missed — `_ctypes`, which has no pure-Python fallback and is what a pretty-printer can reach — was the one that mattered. The OpenSSL 1.1 pair (`_ssl`, `_hashlib`) stays deliberately unbundled: a deprecated TLS stack inside a debugger is a cost with no user, and the index already declares `[prereq.libssl3]` for other tools. Both bundled libs top out at GLIBC_2.17, so the dist's floors are unchanged (re-measured, not carried). The build now IMPORTS every lib-dynload module inside the shipped gdb and fails when the unimportable set differs from a recorded baseline in either direction — a module that starts failing is a regression, one that starts working is a fix — so the note cannot drift again; the arm64 runner printed `lib-dynload 44 module(s), unimportable = [_hashlib _ssl] (baseline)`. x86_64 is untouched (static CPython, no C-API exports, so no extension can load on any host) and macos-arm64 rebuilt byte-identical

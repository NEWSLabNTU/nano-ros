The entity-inventory fragment a Zephyr configure hashes no longer carries content that depends on WHICH COMPOSER wrote it, so `nros build`'s resolve phase (stage 3.5) and the mid-configure producer agree byte for byte and the producer stops arming a re-configure over provenance. Two renderings moved out of `to_cmake` and into `nros/entity_inventory.json` and `resolved.toml`'s `[provenance]`, where a byte comparison cannot reach them: `NROS_ENTITY_INVENTORY_SOURCE`, and the per-component provenance line's PACKAGE — the model states a node FQN (`/talker`) where the merge states an ament package (`talker_pkg`), which is the half issue 1228 had not seen because its unit test built both composers with the same package. Measured on `demo_bringup:zephyr` (`native_sim/native/64`, Zephyr 3.7), clean build dir: the arms in pass 1 go from two to one and the seed and the producer now `diff` empty, with `check-knob-delivery` green on both sides and the delivered knob values and the image's `.config` byte-identical. The pass count itself is unchanged at one re-configure, because the surviving message-bound arm fires in the same pass — that link, and the deletion of `nros_reconfigure_settle` it gates, are issue 1252.

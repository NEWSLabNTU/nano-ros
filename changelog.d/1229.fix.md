`check-box-sync-covers-tracked-source` now sweeps every repository the ROS-box mirror copies — the superproject, every submodule and every west project — instead of the superproject's index alone, which is how a `target/` exclusion silently ate Zephyr's I²C target-mode source while the gate stayed green; the widened sweep immediately found a second live case in `PX4-Autopilot/boards/modalai/voxl2/target/`, both are re-included, and a tree the sweep cannot see is now reported as NOT VERIFIED by name rather than passing as OK

An arena exhaustion now says WHERE as well as how much: `boot_report` stamps `Stage::RegisteringEntities` (declared, documented and decoded since phase-412, emitted by nothing), so a dump can tell "ran out during registration" from "never registered anything". A new cell takes one real exhaustion all the way to `scripts/read-boot-report.py` -- symbol lookup out of the ELF, dump length, positional decode, and a verdict naming the value to set -- and asserts the `nros_log` channel in the same failure; `check-boot-report-layout` now also holds the `Stage` ladder in step with the decoder's table

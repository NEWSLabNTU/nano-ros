Every published `NROS_DERIVED_*` fact now states what each of the three delivery roads (resolver / sidecar / declared) does with it: `check-declared-fact-carriers`'s new `FACT_DISPOSITION` requires a carried-name list, or a deliberate omission QUOTING a comment that already exists in the tree, or a reported `OpenGap` carrying a tracked issue id — and `check-knob-delivery`'s `DERIVED_PAIRS` stopped being 1:1 (one fact feeds two resolved knobs twice over, so a value that arrived at one consumer and was dropped at the other used to be green) and is now cross-checked against the `_nros_resolve_derivable_knob` call sites in both directions on every run

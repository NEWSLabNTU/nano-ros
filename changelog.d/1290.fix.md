`nros::arena_size_for(cbs)` asks the model on an image that declares its entities instead of scaling the derived arena by `cbs / MAX_CBS`, a ratio that stopped meaning anything when the arena became a per-kind sum. The bench's only executor was getting half of what its own declaration needed

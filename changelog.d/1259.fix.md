`nros setup --tool zephyr-sdk-1-0-1` no longer reports success over an SDK with no toolchains — a `[tool.*]` may declare a `post_install` step that completes the install (recorded in `.nros-provenance`, so a re-run is a no-op and a failed one resumes without re-downloading) and a `subdir` naming the tarball's own top-level directory, which `nros sdk-path` now answers and `smoke` probes resolve against

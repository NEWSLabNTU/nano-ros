The Zephyr bake (`nros_system_generate`) refuses the configure when `system.toml`'s `domain_id` and the image's `CONFIG_NROS_DOMAIN_ID` (or `CONFIG_NROS_CYCLONE_DOMAIN_ID`, when set) disagree, naming all three values; the precedence is unchanged (Kconfig is what the image bakes, RFC-0049) and only the silent disagreement is removed

An installed `nros` now provisions and builds with no checkout: a release records its submodule pins in the SDK root it ships, `nros setup <board>` installs a Rust toolchain when the host has none, and configure uses the Cyclone DDS `nros setup` provisioned. The scaffolds no longer advertise `NROS_REPO_DIR` or `-DNANO_ROS_ROOT` to a reader who has neither, and the installed probe exercises the book's own `nros sync` + `nros build` flow again — phase-445 W5 had dropped first-project.md's `probe=` tags, so the track had been extracting no build step at all.

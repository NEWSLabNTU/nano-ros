An installed `nros` now provisions and builds with no checkout: a release records its submodule pins in the SDK root it ships, `nros setup <board>` installs a Rust toolchain when the host has none, and configure uses the Cyclone DDS `nros setup` provisioned.

A contract that declares `params:` on some nodes and not others now stops the configure (CMake road: `NanoRosEntityFacts.cmake`; a cargo road that forwards `NROS_PARAM_DECLARATION_STATUS`: `nros-params`' build script) quoting the entity inventory's reason and the silent nodes, instead of sizing the parameter store from the crate defaults with no build-time line; `absent` (no node declares) and `declared` are unchanged

// nros-c service type - C implementation
// Package: sensor_msgs
// Service: SetCameraInfo
// Auto-generated by rosidl-codegen - do not edit

#include "sensor_msgs_srv_set_camera_info.h"
#include <nros/types.h>
#include <nros/cdr.h>
#include <string.h>

// Type support structure
static const struct nros_service_type_t sensor_msgs_srv_set_camera_info_type_support = {
    .type_name = "sensor_msgs::srv::dds_::SetCameraInfo_",
    .type_hash = "TypeHashNotSupported",
};

const struct nros_service_type_t* sensor_msgs_srv_set_camera_info_get_type_support(void) {
    return &sensor_msgs_srv_set_camera_info_type_support;
}

// ============================================================================
// Request Implementation
// ============================================================================

void sensor_msgs_srv_set_camera_info_request_init(sensor_msgs_srv_set_camera_info_request* msg) {
    if (msg == NULL) return;
    memset(msg, 0, sizeof(sensor_msgs_srv_set_camera_info_request));
}

int32_t sensor_msgs_srv_set_camera_info_request_serialize(const sensor_msgs_srv_set_camera_info_request* msg, uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // Write CDR header (little-endian, version 1)
    if (ptr + 4 > end) return -1;
    *ptr++ = 0x00; // CDR encapsulation
    *ptr++ = 0x01; // Little-endian
    *ptr++ = 0x00; // Options (unused)
    *ptr++ = 0x00; // Options (unused)
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    (void)origin;
    if (sensor_msgs_msg_camera_info_serialize_inline(&msg->camera_info, &ptr, end, origin) < 0) return -1;

    return (int32_t)(ptr - buffer);
}

int32_t sensor_msgs_srv_set_camera_info_request_deserialize(sensor_msgs_srv_set_camera_info_request* msg, const uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    const uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // Skip CDR header
    if (ptr + 4 > end) return -1;
    ptr += 4;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    (void)origin;

    sensor_msgs_srv_set_camera_info_request_init(msg);
    if (sensor_msgs_msg_camera_info_deserialize_inline(&msg->camera_info, &ptr, end, origin) < 0) return -1;

    return 0;
}

// ============================================================================
// Response Implementation
// ============================================================================

void sensor_msgs_srv_set_camera_info_response_init(sensor_msgs_srv_set_camera_info_response* msg) {
    if (msg == NULL) return;
    memset(msg, 0, sizeof(sensor_msgs_srv_set_camera_info_response));
}

int32_t sensor_msgs_srv_set_camera_info_response_serialize(const sensor_msgs_srv_set_camera_info_response* msg, uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // Write CDR header (little-endian, version 1)
    if (ptr + 4 > end) return -1;
    *ptr++ = 0x00; // CDR encapsulation
    *ptr++ = 0x01; // Little-endian
    *ptr++ = 0x00; // Options (unused)
    *ptr++ = 0x00; // Options (unused)
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    (void)origin;
    if (nros_cdr_write_bool(&ptr, end, origin, msg->success) < 0) return -1;
    if (nros_cdr_write_string(&ptr, end, origin, msg->status_message) < 0) return -1;

    return (int32_t)(ptr - buffer);
}

int32_t sensor_msgs_srv_set_camera_info_response_deserialize(sensor_msgs_srv_set_camera_info_response* msg, const uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    const uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // Skip CDR header
    if (ptr + 4 > end) return -1;
    ptr += 4;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    (void)origin;

    sensor_msgs_srv_set_camera_info_response_init(msg);
    if (nros_cdr_read_bool(&ptr, end, origin, &msg->success) < 0) return -1;
    if (nros_cdr_read_string(&ptr, end, origin, msg->status_message, sizeof(msg->status_message)) < 0) return -1;

    return 0;
}
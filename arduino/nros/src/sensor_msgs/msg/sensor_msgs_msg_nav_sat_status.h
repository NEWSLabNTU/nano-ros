// nros-c message type - C header for embedded systems
// Package: sensor_msgs
// Message: NavSatStatus
// Auto-generated by rosidl-codegen - do not edit

#ifndef SENSOR_MSGS_MSG_NAV_SAT_STATUS_H
#define SENSOR_MSGS_MSG_NAV_SAT_STATUS_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

#include <nros/types.h>

#ifdef __cplusplus
extern "C" {
#endif

// Constants
#define SENSOR_MSGS_MSG_NAV_SAT_STATUS_STATUS_NO_FIX -1
#define SENSOR_MSGS_MSG_NAV_SAT_STATUS_STATUS_FIX 0
#define SENSOR_MSGS_MSG_NAV_SAT_STATUS_STATUS_SBAS_FIX 1
#define SENSOR_MSGS_MSG_NAV_SAT_STATUS_STATUS_GBAS_FIX 2
#define SENSOR_MSGS_MSG_NAV_SAT_STATUS_SERVICE_GPS 1
#define SENSOR_MSGS_MSG_NAV_SAT_STATUS_SERVICE_GLONASS 2
#define SENSOR_MSGS_MSG_NAV_SAT_STATUS_SERVICE_COMPASS 4
#define SENSOR_MSGS_MSG_NAV_SAT_STATUS_SERVICE_GALILEO 8

/// NavSatStatus message structure
typedef struct sensor_msgs_msg_nav_sat_status {
    int8_t status;
    uint16_t service;
} sensor_msgs_msg_nav_sat_status;

/// Get the ROS type name for NavSatStatus
static inline const char* sensor_msgs_msg_nav_sat_status_get_type_name(void) {
    return "sensor_msgs::msg::dds_::NavSatStatus_";
}

/// Get the type hash for NavSatStatus
static inline const char* sensor_msgs_msg_nav_sat_status_get_type_hash(void) {
    return "TypeHashNotSupported";
}

/// Initialize a NavSatStatus message to default values
void sensor_msgs_msg_nav_sat_status_init(sensor_msgs_msg_nav_sat_status* msg);

/// Serialize a NavSatStatus message to CDR format (writes a fresh
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error. On success, *serialized_size contains
/// bytes written.
int32_t sensor_msgs_msg_nav_sat_status_serialize(const sensor_msgs_msg_nav_sat_status* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size);

/// Deserialize a NavSatStatus message from CDR format (expects a
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error
int32_t sensor_msgs_msg_nav_sat_status_deserialize(sensor_msgs_msg_nav_sat_status* msg, const uint8_t* buffer, size_t buffer_size);

/// Serialize a NavSatStatus inline into an already-open CDR stream,
/// without emitting its own encapsulation header. Used by parent messages,
/// services, and actions that embed this type as a nested field.
/// - ptr_ref: in/out current write pointer (advanced on success)
/// - end: one-past-last byte of the destination buffer
/// - origin: CDR alignment origin (start of the surrounding encapsulation)
/// Returns 0 on success, -1 on error.
int32_t sensor_msgs_msg_nav_sat_status_serialize_inline(const sensor_msgs_msg_nav_sat_status* msg, uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Deserialize a NavSatStatus inline from an already-open CDR stream,
/// without consuming an encapsulation header. Parameters mirror the inline
/// serialize variant.
int32_t sensor_msgs_msg_nav_sat_status_deserialize_inline(sensor_msgs_msg_nav_sat_status* msg, const uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Get the nros_message_type_t for this message type
const nros_message_type_t* sensor_msgs_msg_nav_sat_status_get_type_support(void);

/// Typed publish helper. Serializes `msg` into a stack buffer of size
/// `NROS_PUB_BUFFER_SIZE` (override at compile time; default 256) then
/// hands it to nros_publish_raw. nros_ret_t / nros_publisher_t /
/// nros_publish_raw all come in via <nros/types.h> (already included
/// at the top of this header).
#ifndef NROS_PUB_BUFFER_SIZE
#define NROS_PUB_BUFFER_SIZE 256
#endif
static inline nros_ret_t sensor_msgs_msg_nav_sat_status_publish(struct nros_publisher_t* publisher,
                                                   const sensor_msgs_msg_nav_sat_status* msg) {
    uint8_t buf[NROS_PUB_BUFFER_SIZE];
    size_t n = 0;
    int32_t r = sensor_msgs_msg_nav_sat_status_serialize(msg, buf, sizeof(buf), &n);
    if (r != 0) return r;
    return nros_publish_raw(publisher, buf, n);
}

#ifdef __cplusplus
}
#endif

#endif // SENSOR_MSGS_MSG_NAV_SAT_STATUS_H
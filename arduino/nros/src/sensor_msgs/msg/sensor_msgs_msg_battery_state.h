// nros-c message type - C header for embedded systems
// Package: sensor_msgs
// Message: BatteryState
// Auto-generated by rosidl-codegen - do not edit

#ifndef SENSOR_MSGS_MSG_BATTERY_STATE_H
#define SENSOR_MSGS_MSG_BATTERY_STATE_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

#include <nros/types.h>

#ifdef __cplusplus
extern "C" {
#endif
#include "std_msgs/msg/std_msgs_msg_header.h"

// Constants
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_STATUS_UNKNOWN 0
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_STATUS_CHARGING 1
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_STATUS_DISCHARGING 2
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_STATUS_NOT_CHARGING 3
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_STATUS_FULL 4
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_HEALTH_UNKNOWN 0
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_HEALTH_GOOD 1
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_HEALTH_OVERHEAT 2
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_HEALTH_DEAD 3
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_HEALTH_OVERVOLTAGE 4
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_HEALTH_UNSPEC_FAILURE 5
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_HEALTH_COLD 6
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_HEALTH_WATCHDOG_TIMER_EXPIRE 7
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_HEALTH_SAFETY_TIMER_EXPIRE 8
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_TECHNOLOGY_UNKNOWN 0
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_TECHNOLOGY_NIMH 1
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_TECHNOLOGY_LION 2
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_TECHNOLOGY_LIPO 3
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_TECHNOLOGY_LIFE 4
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_TECHNOLOGY_NICD 5
#define SENSOR_MSGS_MSG_BATTERY_STATE_POWER_SUPPLY_TECHNOLOGY_LIMN 6

/// BatteryState message structure
typedef struct sensor_msgs_msg_battery_state {
    struct std_msgs_msg_header header;
    float voltage;
    float temperature;
    float current;
    float charge;
    float capacity;
    float design_capacity;
    float percentage;
    uint8_t power_supply_status;
    uint8_t power_supply_health;
    uint8_t power_supply_technology;
    bool present;
    struct { uint32_t size; float data[64]; } cell_voltage;
    struct { uint32_t size; float data[64]; } cell_temperature;
    char location[256];
    char serial_number[256];
} sensor_msgs_msg_battery_state;

/// Get the ROS type name for BatteryState
static inline const char* sensor_msgs_msg_battery_state_get_type_name(void) {
    return "sensor_msgs::msg::dds_::BatteryState_";
}

/// Get the type hash for BatteryState
static inline const char* sensor_msgs_msg_battery_state_get_type_hash(void) {
    return "TypeHashNotSupported";
}

/// Initialize a BatteryState message to default values
void sensor_msgs_msg_battery_state_init(sensor_msgs_msg_battery_state* msg);

/// Serialize a BatteryState message to CDR format (writes a fresh
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error. On success, *serialized_size contains
/// bytes written.
int32_t sensor_msgs_msg_battery_state_serialize(const sensor_msgs_msg_battery_state* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size);

/// Deserialize a BatteryState message from CDR format (expects a
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error
int32_t sensor_msgs_msg_battery_state_deserialize(sensor_msgs_msg_battery_state* msg, const uint8_t* buffer, size_t buffer_size);

/// Serialize a BatteryState inline into an already-open CDR stream,
/// without emitting its own encapsulation header. Used by parent messages,
/// services, and actions that embed this type as a nested field.
/// - ptr_ref: in/out current write pointer (advanced on success)
/// - end: one-past-last byte of the destination buffer
/// - origin: CDR alignment origin (start of the surrounding encapsulation)
/// Returns 0 on success, -1 on error.
int32_t sensor_msgs_msg_battery_state_serialize_inline(const sensor_msgs_msg_battery_state* msg, uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Deserialize a BatteryState inline from an already-open CDR stream,
/// without consuming an encapsulation header. Parameters mirror the inline
/// serialize variant.
int32_t sensor_msgs_msg_battery_state_deserialize_inline(sensor_msgs_msg_battery_state* msg, const uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Get the nros_message_type_t for this message type
const nros_message_type_t* sensor_msgs_msg_battery_state_get_type_support(void);

/// Typed publish helper. Serializes `msg` into a stack buffer of size
/// `NROS_PUB_BUFFER_SIZE` (override at compile time; default 256) then
/// hands it to nros_publish_raw. nros_ret_t / nros_publisher_t /
/// nros_publish_raw all come in via <nros/types.h> (already included
/// at the top of this header).
#ifndef NROS_PUB_BUFFER_SIZE
#define NROS_PUB_BUFFER_SIZE 256
#endif
static inline nros_ret_t sensor_msgs_msg_battery_state_publish(struct nros_publisher_t* publisher,
                                                   const sensor_msgs_msg_battery_state* msg) {
    uint8_t buf[NROS_PUB_BUFFER_SIZE];
    size_t n = 0;
    int32_t r = sensor_msgs_msg_battery_state_serialize(msg, buf, sizeof(buf), &n);
    if (r != 0) return r;
    return nros_publish_raw(publisher, buf, n);
}

#ifdef __cplusplus
}
#endif

#endif // SENSOR_MSGS_MSG_BATTERY_STATE_H
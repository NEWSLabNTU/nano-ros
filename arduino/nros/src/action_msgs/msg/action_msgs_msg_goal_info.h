// nros-c message type - C header for embedded systems
// Package: action_msgs
// Message: GoalInfo
// Auto-generated by rosidl-codegen - do not edit

#ifndef ACTION_MSGS_MSG_GOAL_INFO_H
#define ACTION_MSGS_MSG_GOAL_INFO_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

#include <nros/types.h>

#ifdef __cplusplus
extern "C" {
#endif
#include "builtin_interfaces/msg/builtin_interfaces_msg_time.h"
#include "unique_identifier_msgs/msg/unique_identifier_msgs_msg_uuid.h"

// Constants

/// GoalInfo message structure
typedef struct action_msgs_msg_goal_info {
    struct unique_identifier_msgs_msg_uuid goal_id;
    struct builtin_interfaces_msg_time stamp;
} action_msgs_msg_goal_info;

/// Get the ROS type name for GoalInfo
static inline const char* action_msgs_msg_goal_info_get_type_name(void) {
    return "action_msgs::msg::dds_::GoalInfo_";
}

/// Get the type hash for GoalInfo
static inline const char* action_msgs_msg_goal_info_get_type_hash(void) {
    return "TypeHashNotSupported";
}

/// Initialize a GoalInfo message to default values
void action_msgs_msg_goal_info_init(action_msgs_msg_goal_info* msg);

/// Serialize a GoalInfo message to CDR format (writes a fresh
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error. On success, *serialized_size contains
/// bytes written.
int32_t action_msgs_msg_goal_info_serialize(const action_msgs_msg_goal_info* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size);

/// Deserialize a GoalInfo message from CDR format (expects a
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error
int32_t action_msgs_msg_goal_info_deserialize(action_msgs_msg_goal_info* msg, const uint8_t* buffer, size_t buffer_size);

/// Serialize a GoalInfo inline into an already-open CDR stream,
/// without emitting its own encapsulation header. Used by parent messages,
/// services, and actions that embed this type as a nested field.
/// - ptr_ref: in/out current write pointer (advanced on success)
/// - end: one-past-last byte of the destination buffer
/// - origin: CDR alignment origin (start of the surrounding encapsulation)
/// Returns 0 on success, -1 on error.
int32_t action_msgs_msg_goal_info_serialize_inline(const action_msgs_msg_goal_info* msg, uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Deserialize a GoalInfo inline from an already-open CDR stream,
/// without consuming an encapsulation header. Parameters mirror the inline
/// serialize variant.
int32_t action_msgs_msg_goal_info_deserialize_inline(action_msgs_msg_goal_info* msg, const uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Get the nros_message_type_t for this message type
const nros_message_type_t* action_msgs_msg_goal_info_get_type_support(void);

/// Typed publish helper. Serializes `msg` into a stack buffer of size
/// `NROS_PUB_BUFFER_SIZE` (override at compile time; default 256) then
/// hands it to nros_publish_raw. nros_ret_t / nros_publisher_t /
/// nros_publish_raw all come in via <nros/types.h> (already included
/// at the top of this header).
#ifndef NROS_PUB_BUFFER_SIZE
#define NROS_PUB_BUFFER_SIZE 256
#endif
static inline nros_ret_t action_msgs_msg_goal_info_publish(struct nros_publisher_t* publisher,
                                                   const action_msgs_msg_goal_info* msg) {
    uint8_t buf[NROS_PUB_BUFFER_SIZE];
    size_t n = 0;
    int32_t r = action_msgs_msg_goal_info_serialize(msg, buf, sizeof(buf), &n);
    if (r != 0) return r;
    return nros_publish_raw(publisher, buf, n);
}

#ifdef __cplusplus
}
#endif

#endif // ACTION_MSGS_MSG_GOAL_INFO_H
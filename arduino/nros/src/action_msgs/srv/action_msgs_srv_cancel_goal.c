// nros-c service type - C implementation
// Package: action_msgs
// Service: CancelGoal
// Auto-generated by rosidl-codegen - do not edit

#include "action_msgs_srv_cancel_goal.h"
#include <nros/types.h>
#include <nros/cdr.h>
#include <string.h>

// Type support structure
static const struct nros_service_type_t action_msgs_srv_cancel_goal_type_support = {
    .type_name = "action_msgs::srv::dds_::CancelGoal_",
    .type_hash = "TypeHashNotSupported",
};

const struct nros_service_type_t* action_msgs_srv_cancel_goal_get_type_support(void) {
    return &action_msgs_srv_cancel_goal_type_support;
}

// ============================================================================
// Request Implementation
// ============================================================================

void action_msgs_srv_cancel_goal_request_init(action_msgs_srv_cancel_goal_request* msg) {
    if (msg == NULL) return;
    memset(msg, 0, sizeof(action_msgs_srv_cancel_goal_request));
}

int32_t action_msgs_srv_cancel_goal_request_serialize(const action_msgs_srv_cancel_goal_request* msg, uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // Write CDR header (little-endian, version 1)
    if (ptr + 4 > end) return -1;
    *ptr++ = 0x00; // CDR encapsulation
    *ptr++ = 0x01; // Little-endian
    *ptr++ = 0x00; // Options (unused)
    *ptr++ = 0x00; // Options (unused)
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    (void)origin;
    if (action_msgs_msg_goal_info_serialize_inline(&msg->goal_info, &ptr, end, origin) < 0) return -1;

    return (int32_t)(ptr - buffer);
}

int32_t action_msgs_srv_cancel_goal_request_deserialize(action_msgs_srv_cancel_goal_request* msg, const uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    const uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // Skip CDR header
    if (ptr + 4 > end) return -1;
    ptr += 4;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    (void)origin;

    action_msgs_srv_cancel_goal_request_init(msg);
    if (action_msgs_msg_goal_info_deserialize_inline(&msg->goal_info, &ptr, end, origin) < 0) return -1;

    return 0;
}

// ============================================================================
// Response Implementation
// ============================================================================

void action_msgs_srv_cancel_goal_response_init(action_msgs_srv_cancel_goal_response* msg) {
    if (msg == NULL) return;
    memset(msg, 0, sizeof(action_msgs_srv_cancel_goal_response));
}

int32_t action_msgs_srv_cancel_goal_response_serialize(const action_msgs_srv_cancel_goal_response* msg, uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // Write CDR header (little-endian, version 1)
    if (ptr + 4 > end) return -1;
    *ptr++ = 0x00; // CDR encapsulation
    *ptr++ = 0x01; // Little-endian
    *ptr++ = 0x00; // Options (unused)
    *ptr++ = 0x00; // Options (unused)
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    (void)origin;
    if (nros_cdr_write_i8(&ptr, end, origin, msg->return_code) < 0) return -1;
    if (nros_cdr_write_u32(&ptr, end, origin, msg->goals_canceling.size) < 0) return -1;
    for (size_t i = 0; i < msg->goals_canceling.size; ++i) {
        if (action_msgs_msg_goal_info_serialize_inline(&msg->goals_canceling.data[i], &ptr, end, origin) < 0) return -1;
    }

    return (int32_t)(ptr - buffer);
}

int32_t action_msgs_srv_cancel_goal_response_deserialize(action_msgs_srv_cancel_goal_response* msg, const uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    const uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // Skip CDR header
    if (ptr + 4 > end) return -1;
    ptr += 4;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    (void)origin;

    action_msgs_srv_cancel_goal_response_init(msg);
    if (nros_cdr_read_i8(&ptr, end, origin, &msg->return_code) < 0) return -1;
    {
        uint32_t len;
        if (nros_cdr_read_u32(&ptr, end, origin, &len) < 0) return -1;
        msg->goals_canceling.size = len;
        if (len > 64) return -1;
        for (size_t i = 0; i < len; ++i) {
            if (action_msgs_msg_goal_info_deserialize_inline(&msg->goals_canceling.data[i], &ptr, end, origin) < 0) return -1;
        }
    }

    return 0;
}
// nros-c action type - C implementation
// Package: example_interfaces
// Action: Fibonacci
// Auto-generated by rosidl-codegen - do not edit

#include "example_interfaces_action_fibonacci.h"
#include <nros/types.h>
#include <nros/cdr.h>
#include <string.h>

// Type support structure
static const struct nros_action_type_t example_interfaces_action_fibonacci_type_support = {
    .type_name = "example_interfaces::action::dds_::Fibonacci_",
    .type_hash = "TypeHashNotSupported",
};

const struct nros_action_type_t* example_interfaces_action_fibonacci_get_type_support(void) {
    return &example_interfaces_action_fibonacci_type_support;
}

// ============================================================================
// Goal Implementation
// ============================================================================

void example_interfaces_action_fibonacci_goal_init(example_interfaces_action_fibonacci_goal* msg) {
    if (msg == NULL) return;
    memset(msg, 0, sizeof(example_interfaces_action_fibonacci_goal));
}

int32_t example_interfaces_action_fibonacci_goal_serialize(const example_interfaces_action_fibonacci_goal* msg, uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // Write CDR header (little-endian, version 1)
    if (ptr + 4 > end) return -1;
    *ptr++ = 0x00;
    *ptr++ = 0x01;
    *ptr++ = 0x00;
    *ptr++ = 0x00;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    if (nros_cdr_write_i32(&ptr, end, origin, msg->order) < 0) return -1;

    return (int32_t)(ptr - buffer);
}

int32_t example_interfaces_action_fibonacci_goal_deserialize(example_interfaces_action_fibonacci_goal* msg, const uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    const uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    if (ptr + 4 > end) return -1;
    ptr += 4;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)

    example_interfaces_action_fibonacci_goal_init(msg);
    if (nros_cdr_read_i32(&ptr, end, origin, &msg->order) < 0) return -1;

    return 0;
}

// ============================================================================
// Result Implementation
// ============================================================================

void example_interfaces_action_fibonacci_result_init(example_interfaces_action_fibonacci_result* msg) {
    if (msg == NULL) return;
    memset(msg, 0, sizeof(example_interfaces_action_fibonacci_result));
}

int32_t example_interfaces_action_fibonacci_result_serialize(const example_interfaces_action_fibonacci_result* msg, uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    if (ptr + 4 > end) return -1;
    *ptr++ = 0x00;
    *ptr++ = 0x01;
    *ptr++ = 0x00;
    *ptr++ = 0x00;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    if (nros_cdr_write_u32(&ptr, end, origin, msg->sequence.size) < 0) return -1;
    for (size_t i = 0; i < msg->sequence.size; ++i) {
        if (nros_cdr_write_i32(&ptr, end, origin, msg->sequence.data[i]) < 0) return -1;
    }

    return (int32_t)(ptr - buffer);
}

int32_t example_interfaces_action_fibonacci_result_deserialize(example_interfaces_action_fibonacci_result* msg, const uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    const uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    if (ptr + 4 > end) return -1;
    ptr += 4;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)

    example_interfaces_action_fibonacci_result_init(msg);
    {
        uint32_t len;
        if (nros_cdr_read_u32(&ptr, end, origin, &len) < 0) return -1;
        msg->sequence.size = len;
        if (len > 64) return -1;
        for (size_t i = 0; i < len; ++i) {
            if (nros_cdr_read_i32(&ptr, end, origin, &msg->sequence.data[i]) < 0) return -1;
        }
    }

    return 0;
}

// ============================================================================
// Feedback Implementation
// ============================================================================

void example_interfaces_action_fibonacci_feedback_init(example_interfaces_action_fibonacci_feedback* msg) {
    if (msg == NULL) return;
    memset(msg, 0, sizeof(example_interfaces_action_fibonacci_feedback));
}

int32_t example_interfaces_action_fibonacci_feedback_serialize(const example_interfaces_action_fibonacci_feedback* msg, uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    if (ptr + 4 > end) return -1;
    *ptr++ = 0x00;
    *ptr++ = 0x01;
    *ptr++ = 0x00;
    *ptr++ = 0x00;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    if (nros_cdr_write_u32(&ptr, end, origin, msg->sequence.size) < 0) return -1;
    for (size_t i = 0; i < msg->sequence.size; ++i) {
        if (nros_cdr_write_i32(&ptr, end, origin, msg->sequence.data[i]) < 0) return -1;
    }

    return (int32_t)(ptr - buffer);
}

int32_t example_interfaces_action_fibonacci_feedback_deserialize(example_interfaces_action_fibonacci_feedback* msg, const uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    const uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    if (ptr + 4 > end) return -1;
    ptr += 4;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)

    example_interfaces_action_fibonacci_feedback_init(msg);
    {
        uint32_t len;
        if (nros_cdr_read_u32(&ptr, end, origin, &len) < 0) return -1;
        msg->sequence.size = len;
        if (len > 64) return -1;
        for (size_t i = 0; i < len; ++i) {
            if (nros_cdr_read_i32(&ptr, end, origin, &msg->sequence.data[i]) < 0) return -1;
        }
    }

    return 0;
}
// nros-c message type - C header for embedded systems
// Package: example_interfaces
// Message: Int8
// Auto-generated by rosidl-codegen - do not edit

#ifndef EXAMPLE_INTERFACES_MSG_INT8_H
#define EXAMPLE_INTERFACES_MSG_INT8_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

#include <nros/types.h>

#ifdef __cplusplus
extern "C" {
#endif

// Constants

/// Int8 message structure
typedef struct example_interfaces_msg_int8 {
    int8_t data;
} example_interfaces_msg_int8;

/// Get the ROS type name for Int8
static inline const char* example_interfaces_msg_int8_get_type_name(void) {
    return "example_interfaces::msg::dds_::Int8_";
}

/// Get the type hash for Int8
static inline const char* example_interfaces_msg_int8_get_type_hash(void) {
    return "TypeHashNotSupported";
}

/// Initialize a Int8 message to default values
void example_interfaces_msg_int8_init(example_interfaces_msg_int8* msg);

/// Serialize a Int8 message to CDR format (writes a fresh
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error. On success, *serialized_size contains
/// bytes written.
int32_t example_interfaces_msg_int8_serialize(const example_interfaces_msg_int8* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size);

/// Deserialize a Int8 message from CDR format (expects a
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error
int32_t example_interfaces_msg_int8_deserialize(example_interfaces_msg_int8* msg, const uint8_t* buffer, size_t buffer_size);

/// Serialize a Int8 inline into an already-open CDR stream,
/// without emitting its own encapsulation header. Used by parent messages,
/// services, and actions that embed this type as a nested field.
/// - ptr_ref: in/out current write pointer (advanced on success)
/// - end: one-past-last byte of the destination buffer
/// - origin: CDR alignment origin (start of the surrounding encapsulation)
/// Returns 0 on success, -1 on error.
int32_t example_interfaces_msg_int8_serialize_inline(const example_interfaces_msg_int8* msg, uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Deserialize a Int8 inline from an already-open CDR stream,
/// without consuming an encapsulation header. Parameters mirror the inline
/// serialize variant.
int32_t example_interfaces_msg_int8_deserialize_inline(example_interfaces_msg_int8* msg, const uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Get the nros_message_type_t for this message type
const nros_message_type_t* example_interfaces_msg_int8_get_type_support(void);

/// Typed publish helper. Serializes `msg` into a stack buffer of size
/// `NROS_PUB_BUFFER_SIZE` (override at compile time; default 256) then
/// hands it to nros_publish_raw. nros_ret_t / nros_publisher_t /
/// nros_publish_raw all come in via <nros/types.h> (already included
/// at the top of this header).
#ifndef NROS_PUB_BUFFER_SIZE
#define NROS_PUB_BUFFER_SIZE 256
#endif
static inline nros_ret_t example_interfaces_msg_int8_publish(struct nros_publisher_t* publisher,
                                                   const example_interfaces_msg_int8* msg) {
    uint8_t buf[NROS_PUB_BUFFER_SIZE];
    size_t n = 0;
    int32_t r = example_interfaces_msg_int8_serialize(msg, buf, sizeof(buf), &n);
    if (r != 0) return r;
    return nros_publish_raw(publisher, buf, n);
}

#ifdef __cplusplus
}
#endif

#endif // EXAMPLE_INTERFACES_MSG_INT8_H
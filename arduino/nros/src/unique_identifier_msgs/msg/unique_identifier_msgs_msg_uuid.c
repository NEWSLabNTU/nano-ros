// nros-c message type - C implementation
// Package: unique_identifier_msgs
// Message: UUID
// Auto-generated by rosidl-codegen - do not edit

#include "unique_identifier_msgs_msg_uuid.h"
#include <nros/types.h>
#include <nros/cdr.h>
#include <string.h>

// Type support structure
static const nros_message_type_t unique_identifier_msgs_msg_uuid_type_support = {
    .type_name = "unique_identifier_msgs::msg::dds_::UUID_",
    .type_hash = "TypeHashNotSupported",
    .serialized_size_max = 0,  // Dynamic/unknown
};

const nros_message_type_t* unique_identifier_msgs_msg_uuid_get_type_support(void) {
    return &unique_identifier_msgs_msg_uuid_type_support;
}

void unique_identifier_msgs_msg_uuid_init(unique_identifier_msgs_msg_uuid* msg) {
    if (msg == NULL) return;
    memset(msg, 0, sizeof(unique_identifier_msgs_msg_uuid));
}

// =============================================================================
// Inline serialize — writes fields only into an already-open CDR stream.
// Used by parent messages, services, and actions that embed this type as a
// nested field. Does NOT write an encapsulation header. `origin` is the
// CDR alignment origin of the surrounding encapsulation.
// =============================================================================
int32_t unique_identifier_msgs_msg_uuid_serialize_inline(const unique_identifier_msgs_msg_uuid* msg, uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin) {
    if (msg == NULL || ptr_ref == NULL || *ptr_ref == NULL) return -1;
    uint8_t* ptr = *ptr_ref;
    (void)origin; // may be unused for empty structs
    for (size_t i = 0; i < 16; ++i) {
        if (nros_cdr_write_u8(&ptr, end, origin, msg->uuid[i]) < 0) return -1;
    }
    *ptr_ref = ptr;
    return 0;
}

// =============================================================================
// Public serialize — writes a fresh 4-byte CDR encapsulation header then
// delegates to the inline serializer.
// =============================================================================
int32_t unique_identifier_msgs_msg_uuid_serialize(const unique_identifier_msgs_msg_uuid* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size) {
    if (msg == NULL || buffer == NULL) return -1;

    uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // Write CDR header (little-endian, version 1)
    if (ptr + 4 > end) return -1;
    *ptr++ = 0x00; // CDR encapsulation
    *ptr++ = 0x01; // Little-endian
    *ptr++ = 0x00; // Options (unused)
    *ptr++ = 0x00; // Options (unused)
    const uint8_t* origin = ptr; // CDR alignment origin (after header)

    if (unique_identifier_msgs_msg_uuid_serialize_inline(msg, &ptr, end, origin) < 0) return -1;

    if (serialized_size != NULL) {
        *serialized_size = (size_t)(ptr - buffer);
    }
    return 0;
}

// =============================================================================
// Inline deserialize — reads fields only from an already-open CDR stream.
// =============================================================================
int32_t unique_identifier_msgs_msg_uuid_deserialize_inline(unique_identifier_msgs_msg_uuid* msg, const uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin) {
    if (msg == NULL || ptr_ref == NULL || *ptr_ref == NULL) return -1;
    const uint8_t* ptr = *ptr_ref;
    (void)origin;
    for (size_t i = 0; i < 16; ++i) {
        if (nros_cdr_read_u8(&ptr, end, origin, &msg->uuid[i]) < 0) return -1;
    }
    *ptr_ref = ptr;
    return 0;
}

// =============================================================================
// Public deserialize — skips the 4-byte CDR encapsulation header then
// delegates to the inline deserializer.
// =============================================================================
int32_t unique_identifier_msgs_msg_uuid_deserialize(unique_identifier_msgs_msg_uuid* msg, const uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    const uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // Skip CDR header
    if (ptr + 4 > end) return -1;
    ptr += 4;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)

    unique_identifier_msgs_msg_uuid_init(msg);

    if (unique_identifier_msgs_msg_uuid_deserialize_inline(msg, &ptr, end, origin) < 0) return -1;
    return 0;
}
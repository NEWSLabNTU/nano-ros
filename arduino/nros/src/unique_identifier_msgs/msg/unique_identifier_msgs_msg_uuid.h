// nros-c message type - C header for embedded systems
// Package: unique_identifier_msgs
// Message: UUID
// Auto-generated by rosidl-codegen - do not edit

#ifndef UNIQUE_IDENTIFIER_MSGS_MSG_UUID_H
#define UNIQUE_IDENTIFIER_MSGS_MSG_UUID_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

#include <nros/types.h>

#ifdef __cplusplus
extern "C" {
#endif

// Constants

/// UUID message structure
typedef struct unique_identifier_msgs_msg_uuid {
    uint8_t uuid[16];
} unique_identifier_msgs_msg_uuid;

/// Get the ROS type name for UUID
static inline const char* unique_identifier_msgs_msg_uuid_get_type_name(void) {
    return "unique_identifier_msgs::msg::dds_::UUID_";
}

/// Get the type hash for UUID
static inline const char* unique_identifier_msgs_msg_uuid_get_type_hash(void) {
    return "TypeHashNotSupported";
}

/// Initialize a UUID message to default values
void unique_identifier_msgs_msg_uuid_init(unique_identifier_msgs_msg_uuid* msg);

/// Serialize a UUID message to CDR format (writes a fresh
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error. On success, *serialized_size contains
/// bytes written.
int32_t unique_identifier_msgs_msg_uuid_serialize(const unique_identifier_msgs_msg_uuid* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size);

/// Deserialize a UUID message from CDR format (expects a
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error
int32_t unique_identifier_msgs_msg_uuid_deserialize(unique_identifier_msgs_msg_uuid* msg, const uint8_t* buffer, size_t buffer_size);

/// Serialize a UUID inline into an already-open CDR stream,
/// without emitting its own encapsulation header. Used by parent messages,
/// services, and actions that embed this type as a nested field.
/// - ptr_ref: in/out current write pointer (advanced on success)
/// - end: one-past-last byte of the destination buffer
/// - origin: CDR alignment origin (start of the surrounding encapsulation)
/// Returns 0 on success, -1 on error.
int32_t unique_identifier_msgs_msg_uuid_serialize_inline(const unique_identifier_msgs_msg_uuid* msg, uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Deserialize a UUID inline from an already-open CDR stream,
/// without consuming an encapsulation header. Parameters mirror the inline
/// serialize variant.
int32_t unique_identifier_msgs_msg_uuid_deserialize_inline(unique_identifier_msgs_msg_uuid* msg, const uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Get the nros_message_type_t for this message type
const nros_message_type_t* unique_identifier_msgs_msg_uuid_get_type_support(void);

/// Typed publish helper. Serializes `msg` into a stack buffer of size
/// `NROS_PUB_BUFFER_SIZE` (override at compile time; default 256) then
/// hands it to nros_publish_raw. nros_ret_t / nros_publisher_t /
/// nros_publish_raw all come in via <nros/types.h> (already included
/// at the top of this header).
#ifndef NROS_PUB_BUFFER_SIZE
#define NROS_PUB_BUFFER_SIZE 256
#endif
static inline nros_ret_t unique_identifier_msgs_msg_uuid_publish(struct nros_publisher_t* publisher,
                                                   const unique_identifier_msgs_msg_uuid* msg) {
    uint8_t buf[NROS_PUB_BUFFER_SIZE];
    size_t n = 0;
    int32_t r = unique_identifier_msgs_msg_uuid_serialize(msg, buf, sizeof(buf), &n);
    if (r != 0) return r;
    return nros_publish_raw(publisher, buf, n);
}

#ifdef __cplusplus
}
#endif

#endif // UNIQUE_IDENTIFIER_MSGS_MSG_UUID_H
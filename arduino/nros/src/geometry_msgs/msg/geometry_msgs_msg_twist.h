// nros-c message type - C header for embedded systems
// Package: geometry_msgs
// Message: Twist
// Auto-generated by rosidl-codegen - do not edit

#ifndef GEOMETRY_MSGS_MSG_TWIST_H
#define GEOMETRY_MSGS_MSG_TWIST_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

#include <nros/types.h>

#ifdef __cplusplus
extern "C" {
#endif
#include "geometry_msgs_msg_vector3.h"

// Constants

/// Twist message structure
typedef struct geometry_msgs_msg_twist {
    struct geometry_msgs_msg_vector3 linear;
    struct geometry_msgs_msg_vector3 angular;
} geometry_msgs_msg_twist;

/// Get the ROS type name for Twist
static inline const char* geometry_msgs_msg_twist_get_type_name(void) {
    return "geometry_msgs::msg::dds_::Twist_";
}

/// Get the type hash for Twist
static inline const char* geometry_msgs_msg_twist_get_type_hash(void) {
    return "TypeHashNotSupported";
}

/// Initialize a Twist message to default values
void geometry_msgs_msg_twist_init(geometry_msgs_msg_twist* msg);

/// Serialize a Twist message to CDR format (writes a fresh
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error. On success, *serialized_size contains
/// bytes written.
int32_t geometry_msgs_msg_twist_serialize(const geometry_msgs_msg_twist* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size);

/// Deserialize a Twist message from CDR format (expects a
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error
int32_t geometry_msgs_msg_twist_deserialize(geometry_msgs_msg_twist* msg, const uint8_t* buffer, size_t buffer_size);

/// Serialize a Twist inline into an already-open CDR stream,
/// without emitting its own encapsulation header. Used by parent messages,
/// services, and actions that embed this type as a nested field.
/// - ptr_ref: in/out current write pointer (advanced on success)
/// - end: one-past-last byte of the destination buffer
/// - origin: CDR alignment origin (start of the surrounding encapsulation)
/// Returns 0 on success, -1 on error.
int32_t geometry_msgs_msg_twist_serialize_inline(const geometry_msgs_msg_twist* msg, uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Deserialize a Twist inline from an already-open CDR stream,
/// without consuming an encapsulation header. Parameters mirror the inline
/// serialize variant.
int32_t geometry_msgs_msg_twist_deserialize_inline(geometry_msgs_msg_twist* msg, const uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Get the nros_message_type_t for this message type
const nros_message_type_t* geometry_msgs_msg_twist_get_type_support(void);

/// Typed publish helper. Serializes `msg` into a stack buffer of size
/// `NROS_PUB_BUFFER_SIZE` (override at compile time; default 256) then
/// hands it to nros_publish_raw. nros_ret_t / nros_publisher_t /
/// nros_publish_raw all come in via <nros/types.h> (already included
/// at the top of this header).
#ifndef NROS_PUB_BUFFER_SIZE
#define NROS_PUB_BUFFER_SIZE 256
#endif
static inline nros_ret_t geometry_msgs_msg_twist_publish(struct nros_publisher_t* publisher,
                                                   const geometry_msgs_msg_twist* msg) {
    uint8_t buf[NROS_PUB_BUFFER_SIZE];
    size_t n = 0;
    int32_t r = geometry_msgs_msg_twist_serialize(msg, buf, sizeof(buf), &n);
    if (r != 0) return r;
    return nros_publish_raw(publisher, buf, n);
}

#ifdef __cplusplus
}
#endif

#endif // GEOMETRY_MSGS_MSG_TWIST_H
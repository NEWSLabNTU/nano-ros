// nros-c message type - C header for embedded systems
// Package: std_msgs
// Message: UInt64MultiArray
// Auto-generated by rosidl-codegen - do not edit

#ifndef STD_MSGS_MSG_UINT64MULTI_ARRAY_H
#define STD_MSGS_MSG_UINT64MULTI_ARRAY_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

#include <nros/types.h>

#ifdef __cplusplus
extern "C" {
#endif
#include "std_msgs_msg_multi_array_layout.h"

// Constants

/// UInt64MultiArray message structure
typedef struct std_msgs_msg_uint64multi_array {
    struct std_msgs_msg_multi_array_layout layout;
    struct { uint32_t size; uint64_t data[64]; } data;
} std_msgs_msg_uint64multi_array;

/// Get the ROS type name for UInt64MultiArray
static inline const char* std_msgs_msg_uint64multi_array_get_type_name(void) {
    return "std_msgs::msg::dds_::UInt64MultiArray_";
}

/// Get the type hash for UInt64MultiArray
static inline const char* std_msgs_msg_uint64multi_array_get_type_hash(void) {
    return "TypeHashNotSupported";
}

/// Initialize a UInt64MultiArray message to default values
void std_msgs_msg_uint64multi_array_init(std_msgs_msg_uint64multi_array* msg);

/// Serialize a UInt64MultiArray message to CDR format (writes a fresh
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error. On success, *serialized_size contains
/// bytes written.
int32_t std_msgs_msg_uint64multi_array_serialize(const std_msgs_msg_uint64multi_array* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size);

/// Deserialize a UInt64MultiArray message from CDR format (expects a
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error
int32_t std_msgs_msg_uint64multi_array_deserialize(std_msgs_msg_uint64multi_array* msg, const uint8_t* buffer, size_t buffer_size);

/// Serialize a UInt64MultiArray inline into an already-open CDR stream,
/// without emitting its own encapsulation header. Used by parent messages,
/// services, and actions that embed this type as a nested field.
/// - ptr_ref: in/out current write pointer (advanced on success)
/// - end: one-past-last byte of the destination buffer
/// - origin: CDR alignment origin (start of the surrounding encapsulation)
/// Returns 0 on success, -1 on error.
int32_t std_msgs_msg_uint64multi_array_serialize_inline(const std_msgs_msg_uint64multi_array* msg, uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Deserialize a UInt64MultiArray inline from an already-open CDR stream,
/// without consuming an encapsulation header. Parameters mirror the inline
/// serialize variant.
int32_t std_msgs_msg_uint64multi_array_deserialize_inline(std_msgs_msg_uint64multi_array* msg, const uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Get the nros_message_type_t for this message type
const nros_message_type_t* std_msgs_msg_uint64multi_array_get_type_support(void);

/// Typed publish helper. Serializes `msg` into a stack buffer of size
/// `NROS_PUB_BUFFER_SIZE` (override at compile time; default 256) then
/// hands it to nros_publish_raw. nros_ret_t / nros_publisher_t /
/// nros_publish_raw all come in via <nros/types.h> (already included
/// at the top of this header).
#ifndef NROS_PUB_BUFFER_SIZE
#define NROS_PUB_BUFFER_SIZE 256
#endif
static inline nros_ret_t std_msgs_msg_uint64multi_array_publish(struct nros_publisher_t* publisher,
                                                   const std_msgs_msg_uint64multi_array* msg) {
    uint8_t buf[NROS_PUB_BUFFER_SIZE];
    size_t n = 0;
    int32_t r = std_msgs_msg_uint64multi_array_serialize(msg, buf, sizeof(buf), &n);
    if (r != 0) return r;
    return nros_publish_raw(publisher, buf, n);
}

#ifdef __cplusplus
}
#endif

#endif // STD_MSGS_MSG_UINT64MULTI_ARRAY_H
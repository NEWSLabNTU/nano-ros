// nros-c message type - C header for embedded systems
// Package: std_msgs
// Message: String
// Auto-generated by rosidl-codegen - do not edit

#ifndef STD_MSGS_MSG_STRING_H
#define STD_MSGS_MSG_STRING_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

#include <nros/types.h>

#ifdef __cplusplus
extern "C" {
#endif

// Constants

/// String message structure
typedef struct std_msgs_msg_string {
    char data[256];
} std_msgs_msg_string;

/// Get the ROS type name for String
static inline const char* std_msgs_msg_string_get_type_name(void) {
    return "std_msgs::msg::dds_::String_";
}

/// Get the type hash for String
static inline const char* std_msgs_msg_string_get_type_hash(void) {
    return "TypeHashNotSupported";
}

/// Initialize a String message to default values
void std_msgs_msg_string_init(std_msgs_msg_string* msg);

/// Serialize a String message to CDR format (writes a fresh
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error. On success, *serialized_size contains
/// bytes written.
int32_t std_msgs_msg_string_serialize(const std_msgs_msg_string* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size);

/// Deserialize a String message from CDR format (expects a
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error
int32_t std_msgs_msg_string_deserialize(std_msgs_msg_string* msg, const uint8_t* buffer, size_t buffer_size);

/// Serialize a String inline into an already-open CDR stream,
/// without emitting its own encapsulation header. Used by parent messages,
/// services, and actions that embed this type as a nested field.
/// - ptr_ref: in/out current write pointer (advanced on success)
/// - end: one-past-last byte of the destination buffer
/// - origin: CDR alignment origin (start of the surrounding encapsulation)
/// Returns 0 on success, -1 on error.
int32_t std_msgs_msg_string_serialize_inline(const std_msgs_msg_string* msg, uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Deserialize a String inline from an already-open CDR stream,
/// without consuming an encapsulation header. Parameters mirror the inline
/// serialize variant.
int32_t std_msgs_msg_string_deserialize_inline(std_msgs_msg_string* msg, const uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Get the nros_message_type_t for this message type
const nros_message_type_t* std_msgs_msg_string_get_type_support(void);

/// Typed publish helper. Serializes `msg` into a stack buffer of size
/// `NROS_PUB_BUFFER_SIZE` (override at compile time; default 256) then
/// hands it to nros_publish_raw. nros_ret_t / nros_publisher_t /
/// nros_publish_raw all come in via <nros/types.h> (already included
/// at the top of this header).
#ifndef NROS_PUB_BUFFER_SIZE
#define NROS_PUB_BUFFER_SIZE 256
#endif
static inline nros_ret_t std_msgs_msg_string_publish(struct nros_publisher_t* publisher,
                                                   const std_msgs_msg_string* msg) {
    uint8_t buf[NROS_PUB_BUFFER_SIZE];
    size_t n = 0;
    int32_t r = std_msgs_msg_string_serialize(msg, buf, sizeof(buf), &n);
    if (r != 0) return r;
    return nros_publish_raw(publisher, buf, n);
}

#ifdef __cplusplus
}
#endif

#endif // STD_MSGS_MSG_STRING_H
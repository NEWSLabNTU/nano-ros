// nros-c message type - C implementation
// Package: std_msgs
// Message: UInt16MultiArray
// Auto-generated by rosidl-codegen - do not edit

#include "std_msgs_msg_uint16multi_array.h"
#include <nros/types.h>
#include <nros/cdr.h>
#include <string.h>

// Type support structure
static const nros_message_type_t std_msgs_msg_uint16multi_array_type_support = {
    .type_name = "std_msgs::msg::dds_::UInt16MultiArray_",
    .type_hash = "TypeHashNotSupported",
    .serialized_size_max = 0,  // Dynamic/unknown
};

const nros_message_type_t* std_msgs_msg_uint16multi_array_get_type_support(void) {
    return &std_msgs_msg_uint16multi_array_type_support;
}

void std_msgs_msg_uint16multi_array_init(std_msgs_msg_uint16multi_array* msg) {
    if (msg == NULL) return;
    memset(msg, 0, sizeof(std_msgs_msg_uint16multi_array));
}

// =============================================================================
// Inline serialize — writes fields only into an already-open CDR stream.
// Used by parent messages, services, and actions that embed this type as a
// nested field. Does NOT write an encapsulation header. `origin` is the
// CDR alignment origin of the surrounding encapsulation.
// =============================================================================
int32_t std_msgs_msg_uint16multi_array_serialize_inline(const std_msgs_msg_uint16multi_array* msg, uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin) {
    if (msg == NULL || ptr_ref == NULL || *ptr_ref == NULL) return -1;
    uint8_t* ptr = *ptr_ref;
    (void)origin; // may be unused for empty structs
    if (std_msgs_msg_multi_array_layout_serialize_inline(&msg->layout, &ptr, end, origin) < 0) return -1;
    if (nros_cdr_write_u32(&ptr, end, origin, msg->data.size) < 0) return -1;
    for (size_t i = 0; i < msg->data.size; ++i) {
        if (nros_cdr_write_u16(&ptr, end, origin, msg->data.data[i]) < 0) return -1;
    }
    *ptr_ref = ptr;
    return 0;
}

// =============================================================================
// Public serialize — writes a fresh 4-byte CDR encapsulation header then
// delegates to the inline serializer.
// =============================================================================
int32_t std_msgs_msg_uint16multi_array_serialize(const std_msgs_msg_uint16multi_array* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size) {
    if (msg == NULL || buffer == NULL) return -1;

    uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // Write CDR header (little-endian, version 1)
    if (ptr + 4 > end) return -1;
    *ptr++ = 0x00; // CDR encapsulation
    *ptr++ = 0x01; // Little-endian
    *ptr++ = 0x00; // Options (unused)
    *ptr++ = 0x00; // Options (unused)
    const uint8_t* origin = ptr; // CDR alignment origin (after header)

    if (std_msgs_msg_uint16multi_array_serialize_inline(msg, &ptr, end, origin) < 0) return -1;

    if (serialized_size != NULL) {
        *serialized_size = (size_t)(ptr - buffer);
    }
    return 0;
}

// =============================================================================
// Inline deserialize — reads fields only from an already-open CDR stream.
// =============================================================================
int32_t std_msgs_msg_uint16multi_array_deserialize_inline(std_msgs_msg_uint16multi_array* msg, const uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin) {
    if (msg == NULL || ptr_ref == NULL || *ptr_ref == NULL) return -1;
    const uint8_t* ptr = *ptr_ref;
    (void)origin;
    if (std_msgs_msg_multi_array_layout_deserialize_inline(&msg->layout, &ptr, end, origin) < 0) return -1;
    {
        uint32_t len;
        if (nros_cdr_read_u32(&ptr, end, origin, &len) < 0) return -1;
        msg->data.size = len;
        if (len > 64) return -1;
        for (size_t i = 0; i < len; ++i) {
            if (nros_cdr_read_u16(&ptr, end, origin, &msg->data.data[i]) < 0) return -1;
        }
    }
    *ptr_ref = ptr;
    return 0;
}

// =============================================================================
// Public deserialize — skips the 4-byte CDR encapsulation header then
// delegates to the inline deserializer.
// =============================================================================
int32_t std_msgs_msg_uint16multi_array_deserialize(std_msgs_msg_uint16multi_array* msg, const uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    const uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // Skip CDR header
    if (ptr + 4 > end) return -1;
    ptr += 4;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)

    std_msgs_msg_uint16multi_array_init(msg);

    if (std_msgs_msg_uint16multi_array_deserialize_inline(msg, &ptr, end, origin) < 0) return -1;
    return 0;
}
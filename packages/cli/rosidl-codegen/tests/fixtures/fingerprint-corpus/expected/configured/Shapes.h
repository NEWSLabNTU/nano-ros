// emit:ok
// nros-c message type - C header for embedded systems
// Package: fingerprint-corpus
// Message: Shapes
// Auto-generated by rosidl-codegen - do not edit

#ifndef FINGERPRINT_CORPUS_MSG_SHAPES_H
#define FINGERPRINT_CORPUS_MSG_SHAPES_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

#include <nros/types.h>
#include <nros/view.h>

#ifdef __cplusplus
extern "C" {
#endif

// Constants

/// Shapes message structure
typedef struct fingerprint_corpus_msg_shapes {
    bool flag;
    int8_t i8_v;
    uint8_t u8_v;
    int16_t i16_v;
    uint16_t u16_v;
    int32_t i32_v;
    uint32_t u32_v;
    int64_t i64_v;
    uint64_t u64_v;
    float f32_v;
    double f64_v;
    char text[32];
    struct { int64_t* data; size_t size; size_t capacity; } seq_prim;
    struct { uint32_t size; char data[256][64]; } seq_string;
    double arr_fixed[3];
    struct { uint32_t size; int32_t data[4]; } seq_bounded;
    char str_bounded[8];
} fingerprint_corpus_msg_shapes;

/// Borrowed (zero-copy) view of Shapes — RFC-0033 `borrowed` mode.
/// `mode = "borrowed"` fields point directly into the CDR receive buffer (set
/// by fingerprint_corpus_msg_shapes_deserialize_view); all other fields are copied as in
/// the owned struct. The borrowed pointers are valid ONLY while the source
/// buffer lives (e.g. the subscription callback) — copy out anything retained.
typedef struct fingerprint_corpus_msg_shapes_View {
    bool flag;
    int8_t i8_v;
    uint8_t u8_v;
    int16_t i16_v;
    uint16_t u16_v;
    int32_t i32_v;
    uint32_t u32_v;
    int64_t i64_v;
    uint64_t u64_v;
    float f32_v;
    double f64_v;
    nros_view_str_t text;
    struct { int64_t* data; size_t size; size_t capacity; } seq_prim;
    struct { uint32_t size; char data[256][64]; } seq_string;
    double arr_fixed[3];
    struct { uint32_t size; int32_t data[4]; } seq_bounded;
    char str_bounded[8];
} fingerprint_corpus_msg_shapes_View;

/// Get the ROS type name for Shapes
static inline const char* fingerprint_corpus_msg_shapes_get_type_name(void) {
    return "fingerprint-corpus::msg::dds_::Shapes_";
}

/// Get the type hash for Shapes
static inline const char* fingerprint_corpus_msg_shapes_get_type_hash(void) {
    return "h";
}

/// Initialize a Shapes message to default values
void fingerprint_corpus_msg_shapes_init(fingerprint_corpus_msg_shapes* msg);

/// Release heap-backed (`mode = "heap"`, RFC-0033) fields and re-zero them.
/// Safe on a zero-initialized or already-fini'd message; a no-op when the
/// message has no heap fields. Call after deserializing a message with heap
/// sequences to avoid leaking the malloc'd element buffers.
void fingerprint_corpus_msg_shapes_fini(fingerprint_corpus_msg_shapes* msg);

/// Serialize a Shapes message to CDR format (writes a fresh
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error. On success, *serialized_size contains
/// bytes written.
int32_t fingerprint_corpus_msg_shapes_serialize(const fingerprint_corpus_msg_shapes* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size);

/// Deserialize a Shapes message from CDR format (expects a
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error
int32_t fingerprint_corpus_msg_shapes_deserialize(fingerprint_corpus_msg_shapes* msg, const uint8_t* buffer, size_t buffer_size);

/// Deserialize a Shapes into a borrowed view (RFC-0033 `borrowed`):
/// `mode = "borrowed"` fields are set to point into `buffer` (zero-copy, no
/// allocation); other fields are copied. `out` is fully overwritten. The
/// borrowed pointers alias `buffer` and are valid only while it lives.
/// Returns 0 on success, -1 on error.
int32_t fingerprint_corpus_msg_shapes_deserialize_view(fingerprint_corpus_msg_shapes_View* out, const uint8_t* buffer, size_t buffer_size);

/// Serialize a Shapes inline into an already-open CDR stream,
/// without emitting its own encapsulation header. Used by parent messages,
/// services, and actions that embed this type as a nested field.
/// - ptr_ref: in/out current write pointer (advanced on success)
/// - end: one-past-last byte of the destination buffer
/// - origin: CDR alignment origin (start of the surrounding encapsulation)
/// Returns 0 on success, -1 on error.
int32_t fingerprint_corpus_msg_shapes_serialize_inline(const fingerprint_corpus_msg_shapes* msg, uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Deserialize a Shapes inline from an already-open CDR stream,
/// without consuming an encapsulation header. Parameters mirror the inline
/// serialize variant.
int32_t fingerprint_corpus_msg_shapes_deserialize_inline(fingerprint_corpus_msg_shapes* msg, const uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Get the nros_message_type_t for this message type
const nros_message_type_t* fingerprint_corpus_msg_shapes_get_type_support(void);


/// NO size bound is emitted for this type.
///
/// Reason: unbounded member: text (string)
///
/// "unbounded member" means the bound was computed and does not exist -- bound
/// the field in the `.msg` (`string<=64`) or give it a `cap` in
/// `nros-codegen.toml`. "could not be resolved" means the bound was NOT
/// computed, because a nested type was not reachable; that is a search-path
/// problem, not a property of the message. Issue 0896.

/* issue 0896 layer 5 -- naming the size constant of an unbounded type is a
   deliberate error, and this makes the compiler SAY SO. Without it the
   constant is simply absent and the user gets "undeclared identifier", which
   names neither the type nor the member that costs it the bound. */
#define FINGERPRINT_CORPUS_MSG_SHAPES_MAX_SERIALIZED_SIZE_XCDR1 \
    NROS_NO_SIZE_BOUND__see_the_reason_above_in_this_header
#define FINGERPRINT_CORPUS_MSG_SHAPES_MAX_SERIALIZED_SIZE_XCDR2 \
    NROS_NO_SIZE_BOUND__see_the_reason_above_in_this_header


/// Typed publish helper. Serializes `msg` into a stack buffer sized from the
/// type's own bound where one exists, and from `NROS_PUB_BUFFER_SIZE`
/// otherwise. nros_ret_t / nros_publisher_t / nros_publish_raw all come in via
/// <nros/types.h> (already included at the top of this header).
#ifndef NROS_PUB_BUFFER_SIZE
#define NROS_PUB_BUFFER_SIZE 256
#endif
static inline nros_ret_t fingerprint_corpus_msg_shapes_publish(struct nros_publisher_t* publisher,
                                                   const fingerprint_corpus_msg_shapes* msg) {

    /* No bound for this type (unbounded member: text (string)), so the global knob is
       still the only available answer. */
    uint8_t buf[NROS_PUB_BUFFER_SIZE];

    size_t n = 0;
    int32_t r = fingerprint_corpus_msg_shapes_serialize(msg, buf, sizeof(buf), &n);
    if (r != 0) return r;
    return nros_publish_raw(publisher, buf, n);
}

#ifdef __cplusplus
}
#endif

#endif // FINGERPRINT_CORPUS_MSG_SHAPES_H
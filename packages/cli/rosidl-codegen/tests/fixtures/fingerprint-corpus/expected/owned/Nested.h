// emit:ok
// nros-c message type - C header for embedded systems
// Package: fingerprint-corpus
// Message: Nested
// Auto-generated by rosidl-codegen - do not edit

#ifndef FINGERPRINT_CORPUS_MSG_NESTED_H
#define FINGERPRINT_CORPUS_MSG_NESTED_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

#include <nros/types.h>

#ifdef __cplusplus
extern "C" {
#endif
#include "fingerprint_corpus_msg_shapes.h"

// Constants

/// Nested message structure
typedef struct fingerprint_corpus_msg_nested {
    struct fingerprint_corpus_msg_shapes one;
    struct { uint32_t size; struct fingerprint_corpus_msg_shapes data[64]; } many;
} fingerprint_corpus_msg_nested;

/// Get the ROS type name for Nested
static inline const char* fingerprint_corpus_msg_nested_get_type_name(void) {
    return "fingerprint-corpus::msg::dds_::Nested_";
}

/// Get the type hash for Nested
static inline const char* fingerprint_corpus_msg_nested_get_type_hash(void) {
    return "h";
}

/// Initialize a Nested message to default values
void fingerprint_corpus_msg_nested_init(fingerprint_corpus_msg_nested* msg);

/// Release heap-backed (`mode = "heap"`, RFC-0033) fields and re-zero them.
/// Safe on a zero-initialized or already-fini'd message; a no-op when the
/// message has no heap fields. Call after deserializing a message with heap
/// sequences to avoid leaking the malloc'd element buffers.
void fingerprint_corpus_msg_nested_fini(fingerprint_corpus_msg_nested* msg);

/// Serialize a Nested message to CDR format (writes a fresh
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error. On success, *serialized_size contains
/// bytes written.
int32_t fingerprint_corpus_msg_nested_serialize(const fingerprint_corpus_msg_nested* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size);

/// Deserialize a Nested message from CDR format (expects a
/// 4-byte CDR encapsulation header followed by the fields).
/// Returns 0 on success, -1 on error
int32_t fingerprint_corpus_msg_nested_deserialize(fingerprint_corpus_msg_nested* msg, const uint8_t* buffer, size_t buffer_size);

/// Serialize a Nested inline into an already-open CDR stream,
/// without emitting its own encapsulation header. Used by parent messages,
/// services, and actions that embed this type as a nested field.
/// - ptr_ref: in/out current write pointer (advanced on success)
/// - end: one-past-last byte of the destination buffer
/// - origin: CDR alignment origin (start of the surrounding encapsulation)
/// Returns 0 on success, -1 on error.
int32_t fingerprint_corpus_msg_nested_serialize_inline(const fingerprint_corpus_msg_nested* msg, uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Deserialize a Nested inline from an already-open CDR stream,
/// without consuming an encapsulation header. Parameters mirror the inline
/// serialize variant.
int32_t fingerprint_corpus_msg_nested_deserialize_inline(fingerprint_corpus_msg_nested* msg, const uint8_t** ptr_ref, const uint8_t* end, const uint8_t* origin);

/// Get the nros_message_type_t for this message type
const nros_message_type_t* fingerprint_corpus_msg_nested_get_type_support(void);

/// Typed publish helper. Serializes `msg` into a stack buffer of size
/// `NROS_PUB_BUFFER_SIZE` (override at compile time; default 256) then
/// hands it to nros_publish_raw. nros_ret_t / nros_publisher_t /
/// nros_publish_raw all come in via <nros/types.h> (already included
/// at the top of this header).
#ifndef NROS_PUB_BUFFER_SIZE
#define NROS_PUB_BUFFER_SIZE 256
#endif
static inline nros_ret_t fingerprint_corpus_msg_nested_publish(struct nros_publisher_t* publisher,
                                                   const fingerprint_corpus_msg_nested* msg) {
    uint8_t buf[NROS_PUB_BUFFER_SIZE];
    size_t n = 0;
    int32_t r = fingerprint_corpus_msg_nested_serialize(msg, buf, sizeof(buf), &n);
    if (r != 0) return r;
    return nros_publish_raw(publisher, buf, n);
}

#ifdef __cplusplus
}
#endif

#endif // FINGERPRINT_CORPUS_MSG_NESTED_H
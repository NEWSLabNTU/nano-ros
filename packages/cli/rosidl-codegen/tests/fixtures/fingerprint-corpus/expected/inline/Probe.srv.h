// emit:ok
// nros-c service type - C header for embedded systems
// Package: fingerprint-corpus
// Service: Probe
// Auto-generated by rosidl-codegen - do not edit

#ifndef FINGERPRINT_CORPUS_SRV_PROBE_H
#define FINGERPRINT_CORPUS_SRV_PROBE_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

#include <nros/types.h>

#include <nros/nros_config_generated.h>

/* RFC-0090 — emitted by nano-ros codegen version 1. */
#define NROS_EMITTED_CODEGEN_VERSION 1

#ifndef NROS_CODEGEN_VERSION
#error "nros: the generated config header did not define NROS_CODEGEN_VERSION. This generated artifact cannot tell whether the runtime accepts it; rebuild the nano-ros runtime so its config header is regenerated."
#elif NROS_EMITTED_CODEGEN_VERSION < NROS_CODEGEN_VERSION_MIN \
   || NROS_EMITTED_CODEGEN_VERSION > NROS_CODEGEN_VERSION
#error "nros: this generated tree was emitted at a codegen version the runtime does not accept (see NROS_EMITTED_CODEGEN_VERSION above against NROS_CODEGEN_VERSION_MIN..NROS_CODEGEN_VERSION in nros_config_generated.h). Regenerate it against the runtime you are building: `nros sync` in a consumer workspace."
#endif

#ifdef __cplusplus
extern "C" {
#endif

// ============================================================================
// Request
// ============================================================================

// Request constants

/// Probe request structure
typedef struct fingerprint_corpus_srv_probe_request {
    struct { uint32_t size; int64_t data[64]; } items;
    char note[256];
} fingerprint_corpus_srv_probe_request;

/// Initialize a Probe request to default values
void fingerprint_corpus_srv_probe_request_init(fingerprint_corpus_srv_probe_request* msg);
/// Release heap-backed (`mode = "heap"`) fields; no-op without them (issue 0345).
void fingerprint_corpus_srv_probe_request_fini(fingerprint_corpus_srv_probe_request* msg);


/// Serialize a Probe request to CDR format
/// Returns 0 on success, -1 on error. On success, *serialized_size contains
/// bytes written (issue #228 — unified with the message convention).
int32_t fingerprint_corpus_srv_probe_request_serialize(const fingerprint_corpus_srv_probe_request* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size);

/// Deserialize a Probe request from CDR format
/// Returns 0 on success, -1 on error
int32_t fingerprint_corpus_srv_probe_request_deserialize(fingerprint_corpus_srv_probe_request* msg, const uint8_t* buffer, size_t buffer_size);

// ============================================================================
// Response
// ============================================================================

// Response constants

/// Probe response structure
typedef struct fingerprint_corpus_srv_probe_response {
    int64_t sum;
    struct { uint32_t size; char data[64][256]; } lines;
} fingerprint_corpus_srv_probe_response;

/// Initialize a Probe response to default values
void fingerprint_corpus_srv_probe_response_init(fingerprint_corpus_srv_probe_response* msg);
/// Release heap-backed (`mode = "heap"`) fields; no-op without them (issue 0345).
void fingerprint_corpus_srv_probe_response_fini(fingerprint_corpus_srv_probe_response* msg);


/// Serialize a Probe response to CDR format
/// Returns 0 on success, -1 on error. On success, *serialized_size contains
/// bytes written (issue #228 — unified with the message convention).
int32_t fingerprint_corpus_srv_probe_response_serialize(const fingerprint_corpus_srv_probe_response* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size);

/// Deserialize a Probe response from CDR format
/// Returns 0 on success, -1 on error
int32_t fingerprint_corpus_srv_probe_response_deserialize(fingerprint_corpus_srv_probe_response* msg, const uint8_t* buffer, size_t buffer_size);

// ============================================================================
// Service Type Support
// ============================================================================

/// Get the ROS type name for Probe
static inline const char* fingerprint_corpus_srv_probe_get_type_name(void) {
    return "fingerprint-corpus::srv::dds_::Probe_";
}

/// Get the type hash for Probe
static inline const char* fingerprint_corpus_srv_probe_get_type_hash(void) {
    return "h";
}

/// Get the nros_service_type_t for this service type
const struct nros_service_type_t* fingerprint_corpus_srv_probe_get_type_support(void);

#ifdef __cplusplus
}
#endif

#endif // FINGERPRINT_CORPUS_SRV_PROBE_H
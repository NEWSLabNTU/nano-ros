// emit:ok
// nros-c action type - C implementation
// Package: fingerprint-corpus
// Action: Probe
// Auto-generated by rosidl-codegen - do not edit

#include "fingerprint_corpus_action_probe.h"
#include <nros/types.h>
#include <nros/cdr.h>
// Issue 0345 — heap payload fields (RFC-0033 `mode = "heap"`) malloc on
// deserialize and are freed by the generated `_fini`, so the platform
// allocator seam is needed here exactly as in message_c.c.
#include <nros/platform.h>
#include <string.h>

// Type support structure
static const struct nros_action_type_t fingerprint_corpus_action_probe_type_support = {
    .type_name = "fingerprint-corpus::action::dds_::Probe_",
    .type_hash = "h",
};

const struct nros_action_type_t* fingerprint_corpus_action_probe_get_type_support(void) {
    return &fingerprint_corpus_action_probe_type_support;
}

// ============================================================================
// Goal Implementation
// ============================================================================

void fingerprint_corpus_action_probe_goal_init(fingerprint_corpus_action_probe_goal* msg) {
    if (msg == NULL) return;
    memset(msg, 0, sizeof(fingerprint_corpus_action_probe_goal));
}

// Release heap-backed (`mode = "heap"`, RFC-0033) fields. Safe on a zero-inited
// or already-fini'd struct (frees NULL, re-zeroes). No-op when the payload has
// no heap fields. Issue 0345 — the caller owns the payload struct (it rides the
// raw `(data, len)` service/action callbacks), so the caller `_fini`s it, the
// same contract messages have used since RFC-0033.
void fingerprint_corpus_action_probe_goal_fini(fingerprint_corpus_action_probe_goal* msg) {
    if (msg == NULL) return;
}

int32_t fingerprint_corpus_action_probe_goal_serialize(const fingerprint_corpus_action_probe_goal* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size) {
    if (msg == NULL || buffer == NULL) return -1;

    uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // phase-303 W4 (#0267) — edition-appropriate encapsulation header + DHEADER.
    if (nros_cdr_write_encaps_header(&ptr, end) < 0) return -1;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    int64_t __dh = nros_cdr_begin_dheader(&ptr, end, origin);
    if (__dh < -1) return -1;
    if (nros_cdr_write_u32(&ptr, end, origin, msg->waypoints.size) < 0) return -1;
    for (size_t i = 0; i < msg->waypoints.size; ++i) {
        if (nros_cdr_write_i64(&ptr, end, origin, msg->waypoints.data[i]) < 0) return -1;
    }
    if (nros_cdr_write_string(&ptr, end, origin, msg->name) < 0) return -1;

    if (nros_cdr_end_dheader(__dh, &ptr, end, origin) < 0) return -1;
    if (serialized_size != NULL) {
        *serialized_size = (size_t)(ptr - buffer);
    }
    return 0;
}

int32_t fingerprint_corpus_action_probe_goal_deserialize(fingerprint_corpus_action_probe_goal* msg, const uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    const uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // phase-303 W4 (#0267) — skip the encapsulation header + read the DHEADER.
    if (ptr + 4 > end) return -1;
    ptr += 4;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    int64_t __dh = nros_cdr_begin_dheader_read(&ptr, end, origin);
    if (__dh < -1) return -1;

    fingerprint_corpus_action_probe_goal_init(msg);
    {
        uint32_t len;
        if (nros_cdr_read_u32(&ptr, end, origin, &len) < 0) return -1;
        msg->waypoints.size = len;
        if (len > 64) return -1;
        for (size_t i = 0; i < len; ++i) {
            if (nros_cdr_read_i64(&ptr, end, origin, &msg->waypoints.data[i]) < 0) return -1;
        }
    }
    if (nros_cdr_read_string(&ptr, end, origin, msg->name, sizeof(msg->name)) < 0) return -1;

    if (nros_cdr_end_dheader_read(__dh, &ptr, end, origin) < 0) return -1;
    return 0;
}

// ============================================================================
// Result Implementation
// ============================================================================

void fingerprint_corpus_action_probe_result_init(fingerprint_corpus_action_probe_result* msg) {
    if (msg == NULL) return;
    memset(msg, 0, sizeof(fingerprint_corpus_action_probe_result));
}

// Release heap-backed (`mode = "heap"`, RFC-0033) fields. Safe on a zero-inited
// or already-fini'd struct (frees NULL, re-zeroes). No-op when the payload has
// no heap fields. Issue 0345 — the caller owns the payload struct (it rides the
// raw `(data, len)` service/action callbacks), so the caller `_fini`s it, the
// same contract messages have used since RFC-0033.
void fingerprint_corpus_action_probe_result_fini(fingerprint_corpus_action_probe_result* msg) {
    if (msg == NULL) return;
}

int32_t fingerprint_corpus_action_probe_result_serialize(const fingerprint_corpus_action_probe_result* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size) {
    if (msg == NULL || buffer == NULL) return -1;

    uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // phase-303 W4 (#0267) — edition-appropriate encapsulation header + DHEADER.
    if (nros_cdr_write_encaps_header(&ptr, end) < 0) return -1;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    int64_t __dh = nros_cdr_begin_dheader(&ptr, end, origin);
    if (__dh < -1) return -1;
    if (nros_cdr_write_i64(&ptr, end, origin, msg->total) < 0) return -1;
    if (nros_cdr_write_string(&ptr, end, origin, msg->report) < 0) return -1;

    if (nros_cdr_end_dheader(__dh, &ptr, end, origin) < 0) return -1;
    if (serialized_size != NULL) {
        *serialized_size = (size_t)(ptr - buffer);
    }
    return 0;
}

int32_t fingerprint_corpus_action_probe_result_deserialize(fingerprint_corpus_action_probe_result* msg, const uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    const uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // phase-303 W4 (#0267) — skip the encapsulation header + read the DHEADER.
    if (ptr + 4 > end) return -1;
    ptr += 4;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    int64_t __dh = nros_cdr_begin_dheader_read(&ptr, end, origin);
    if (__dh < -1) return -1;

    fingerprint_corpus_action_probe_result_init(msg);
    if (nros_cdr_read_i64(&ptr, end, origin, &msg->total) < 0) return -1;
    if (nros_cdr_read_string(&ptr, end, origin, msg->report, sizeof(msg->report)) < 0) return -1;

    if (nros_cdr_end_dheader_read(__dh, &ptr, end, origin) < 0) return -1;
    return 0;
}

// ============================================================================
// Feedback Implementation
// ============================================================================

void fingerprint_corpus_action_probe_feedback_init(fingerprint_corpus_action_probe_feedback* msg) {
    if (msg == NULL) return;
    memset(msg, 0, sizeof(fingerprint_corpus_action_probe_feedback));
}

// Release heap-backed (`mode = "heap"`, RFC-0033) fields. Safe on a zero-inited
// or already-fini'd struct (frees NULL, re-zeroes). No-op when the payload has
// no heap fields. Issue 0345 — the caller owns the payload struct (it rides the
// raw `(data, len)` service/action callbacks), so the caller `_fini`s it, the
// same contract messages have used since RFC-0033.
void fingerprint_corpus_action_probe_feedback_fini(fingerprint_corpus_action_probe_feedback* msg) {
    if (msg == NULL) return;
}

int32_t fingerprint_corpus_action_probe_feedback_serialize(const fingerprint_corpus_action_probe_feedback* msg, uint8_t* buffer, size_t buffer_size, size_t* serialized_size) {
    if (msg == NULL || buffer == NULL) return -1;

    uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // phase-303 W4 (#0267) — edition-appropriate encapsulation header + DHEADER.
    if (nros_cdr_write_encaps_header(&ptr, end) < 0) return -1;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    int64_t __dh = nros_cdr_begin_dheader(&ptr, end, origin);
    if (__dh < -1) return -1;
    if (nros_cdr_write_i64(&ptr, end, origin, msg->done) < 0) return -1;
    if (nros_cdr_write_string(&ptr, end, origin, msg->stage) < 0) return -1;

    if (nros_cdr_end_dheader(__dh, &ptr, end, origin) < 0) return -1;
    if (serialized_size != NULL) {
        *serialized_size = (size_t)(ptr - buffer);
    }
    return 0;
}

int32_t fingerprint_corpus_action_probe_feedback_deserialize(fingerprint_corpus_action_probe_feedback* msg, const uint8_t* buffer, size_t buffer_size) {
    if (msg == NULL || buffer == NULL) return -1;

    const uint8_t* ptr = buffer;
    const uint8_t* end = buffer + buffer_size;

    // phase-303 W4 (#0267) — skip the encapsulation header + read the DHEADER.
    if (ptr + 4 > end) return -1;
    ptr += 4;
    const uint8_t* origin = ptr; // CDR alignment origin (after header)
    int64_t __dh = nros_cdr_begin_dheader_read(&ptr, end, origin);
    if (__dh < -1) return -1;

    fingerprint_corpus_action_probe_feedback_init(msg);
    if (nros_cdr_read_i64(&ptr, end, origin, &msg->done) < 0) return -1;
    if (nros_cdr_read_string(&ptr, end, origin, msg->stage, sizeof(msg->stage)) < 0) return -1;

    if (nros_cdr_end_dheader_read(__dh, &ptr, end, origin) < 0) return -1;
    return 0;
}
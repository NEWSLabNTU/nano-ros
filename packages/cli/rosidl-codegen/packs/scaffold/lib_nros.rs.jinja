//! Auto-generated nros message types
//!
//! This crate contains ROS 2 message types generated for use with nros.
//! All types are no_std compatible and use heapless collections.

#![no_std]
#![allow(dead_code)]

#[cfg(feature = "std")]
extern crate std;

// phase-341 W2 (final) — `any(alloc, std)` is THE heap predicate; a `std` build
// links an allocator by definition, so a hosted consumer never names `alloc`.
#[cfg(any(feature = "alloc", feature = "std"))]
extern crate alloc;
{%- if has_messages %}

pub mod msg;
{%- endif %}
{%- if has_services %}

pub mod srv;
{%- endif %}
{%- if has_actions %}

pub mod action;
{%- endif %}

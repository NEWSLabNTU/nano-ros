[package]
name = "nros-platform-freertos"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "nros platform implementation for FreeRTOS (calls FreeRTOS C APIs via FFI)"

# No Rust dependencies — all FreeRTOS APIs are extern "C" resolved at link time
# from the FreeRTOS kernel library linked by the board crate.

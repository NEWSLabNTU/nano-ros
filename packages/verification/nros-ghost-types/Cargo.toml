[package]
name = "nros-ghost-types"
version.workspace = true
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Ghost model types for nros verification — shared between production crate tests and Verus proofs"
publish = false


[features]
default = []

[lints]
workspace = true

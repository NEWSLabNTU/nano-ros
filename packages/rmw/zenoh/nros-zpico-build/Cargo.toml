[package]
name = "nros-zpico-build"
version.workspace = true
edition.workspace = true
publish = false
description = "Build-script helper logic for zpico-sys"
license.workspace = true
repository.workspace = true

# issue 0287 — HOST-ONLY. `just check workspace-embedded` builds the whole
# workspace for a thumb target, and cargo unifies features across ALL members
# regardless of what firmware can reach. A host-only member therefore turns
# `std` on for everything and the lane dies in an unrelated crate
# (`can't find crate for \`std\`` in nros-serdes). Declared here rather than
# hand-listed in the justfile so the reason lives with the crate and the
# exclude list is derived — see scripts/build/host-only-members.sh.
[package.metadata.nros]
host-only = true
host-only-reason = "host build-script helper for zenoh-pico"

# phase-400 W2a — same shape as nros-build-helpers, same reason: since issue
# 0452 `generate_header` below only re-renders `zpico.h` to COMPARE it against
# the committed copy and warn. `nros-cbindgen-headers` is the single writer and
# `check-cbindgen-headers` the enforcement, so the render is a diagnostic — and
# a diagnostic should not make every cold leaf build compile cbindgen.
#
# Note the two halves this crate contributes are NOT both gated:
# `post_process_header` and `is_plausible_generated_header` live in `lib.rs`,
# take a `&str`, and touch no cbindgen — the regenerator calls those directly
# and needs no feature from here.
[features]
default = []
cbindgen-drift-check = ["dep:cbindgen"]

[dependencies]
cbindgen = { workspace = true, optional = true }
cc = "1.0"
# issue 0383 — strict C diagnostics (implicit-function-declaration,
# int-conversion) as ERRORS at every `cc::Build` below. One shared spelling.
nros-cc-flags = { path = "../../../tooling/nros-cc-flags" }
nros-board-common = { path = "../../../boards/nros-board-common", features = ["build-helpers"] }
nros-build-paths = { path = "../../../tooling/nros-build-paths" }
# issue 0460 — the ONE spelling of the Kconfig-via-$DOTCONFIG knob fallback.
nros-zephyr-build = { path = "../../../tooling/nros-zephyr-build" }
pkg-config = "0.3"

[lints]
workspace = true

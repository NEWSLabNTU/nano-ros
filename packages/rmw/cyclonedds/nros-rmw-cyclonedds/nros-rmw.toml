# nano-ros RMW backend descriptor — RFC-0071 D1/D7, phase-347 W2.
#
# The backend declares itself here; the toolchain lowers it. Nothing in
# `packages/core/**` or `packages/api/**` may name this backend (RFC-0071).
#
# W2 is INERT: `nros_rmw_dispatch()` and `resolve_rmw()` remain the live path,
# and `check-rmw-descriptors` asserts this file agrees with them row for row.
# W3 inverts that — the descriptor becomes the source and the closed lists go.
[rmw]
# phase-420 W5 / RFC-0087 D4 — this table used to open with five more lines:
#
#   names = [...]            the `<nano_ros_provides>` announcements, restated
#   cargo_feature = "..."    `<kind>-<name>`
#   cmake_value = "..."      the canonical name
#   c_define_token = "..."   `UPPER(name)`
#   cffi_feature = "..."     `<cargo_feature>-cffi`
#
# All five are DERIVED now (`cargo-nano-ros/src/derived_descriptor.rs`, shared
# by `build.rs` and the library). The names come from `package.xml`, which is
# the only place a provider should spell them; the other four are convention.
# `check-derived-descriptor-fields` refuses a restatement that disagrees, and
# `build.rs` panics on one too.

# The C++ define `nros-cpp` puts on its INTERFACE for this backend. Spellings
# are INCONSISTENT across backends by history (`_CFFI` on two, bare on two) and
# are preserved exactly — consumers `#if` on them.
cpp_define = "NROS_RMW_CYCLONEDDS"

# NOT special — just the only backend that fills the cargo-consumer x
# cmake-provider cell (RFC-0071 D7). Its register symbol is C++, so there is no
# rlib to bundle; a Rust-rooted consumer reaches it through the `-sys` bridge
# crate whose build.rs drives `cmake::Config`.
[rmw.provides.cmake]
dir = "."
target = "nros_rmw_cyclonedds"

[rmw.provides.cargo]
sys_crate = "cyclonedds-sys"

[rmw.link]
rlib_dep = ""                       # none — it is a C++ library, not an rlib
# No rlib and a cmake target above ⇒ `strategy = "cmake"` (RFC-0094 D5): the
# build `add_subdirectory()`s `[rmw.provides.cmake].dir` and whole-archives
# `[rmw.provides.cmake].target` into both umbrellas. `nros-c`/`nros-cpp` bundle
# nothing for it, so it names no cffi feature and both fall back to `rmw-cffi`.
needs_cxx_linker = true
# `extra_link_libs = ["nros_rmw_cyclonedds", "ddsc", "stdc++"]` was DELETED in
# phase-439 W4 (issue 1216). Nothing read it, and consuming it the obvious way —
# `target_link_libraries(${TARGET} ${NROS_RMW_EXTRA_LINK_LIBS})` — is precisely
# what issue 0475 records as BREAKING the link (`undefined reference to
# ddsrt_*`): the archives must ride ONE `-Wl,--whole-archive` group, and adding
# them to the link LINE reorders ld's single pass. The real link is that flag
# plus `LINK_DEPENDS` for the rebuild edge.

# The per-message IDL descriptor step no other backend needs. W5 consumes this;
# W2/W3 only record it.
[rmw.codegen]
# phase-347 W5 — the TARGET-level hook (the wrapper that owns the whole idlc
# pipeline), not the inner per-file generate function it calls.
per_message = "nros_rmw_cyclonedds_typesupport_for_target"

[rmw.capabilities]
type-descriptors = "needs-type-descriptors"   # -> nros-node cfg(rmw_needs_type_descriptors)

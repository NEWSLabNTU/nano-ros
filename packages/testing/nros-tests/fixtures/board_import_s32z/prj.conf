# phase-292 W3.a — near-empty on purpose: BOARD id / base config / per-board
# fragment + overlay / default RMW all flow from the board crate
# (nros-board-s32z270dc2-r52) via nano_ros_use_board(). The ONE thing a
# consumer must add is its language surface — this crate's base prj.conf is
# deliberately language-neutral (the phase-292 W1.a lesson: the ASI consumer
# is C++, the in-tree talker smoke is Rust; a board must not pick for them).
# This fixture is a C app.
CONFIG_NROS_C_API=y

meta:
  version: 1
# R-code.1 fixture model — mirrors launch/system.launch.xml (talker+listener).
structure:
  scopes:
    /: {}
  nodes:
    /listener:
      scope: /
      pkg: listener_pkg
      exec: listener
    /talker:
      scope: /
      pkg: talker_pkg
      exec: talker

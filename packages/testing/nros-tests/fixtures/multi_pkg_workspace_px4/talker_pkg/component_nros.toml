# Phase 212.H.7 fixture — talker component (C++).
version   = 1
package   = "talker_pkg"
component = "talker"
language  = "cpp"

[linkage]
# PX4 modules are linked into the firmware ELF, not built as a separate
# executable. The `linkage.executable` field is the PX4 MAIN symbol
# (matches @COMPONENT_NAME@ in the rendered skeleton).
executable      = "talker"
exported_symbol = "nros_component_talker"

[metadata]
source_metadata = "metadata/talker.json"
generated_by    = "phase-212.H.7 fixture"

[overrides]
default_namespace = "/"
parameters        = {}
remaps            = []

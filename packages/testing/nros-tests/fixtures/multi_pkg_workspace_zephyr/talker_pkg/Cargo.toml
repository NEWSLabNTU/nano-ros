# Phase 212.H.1 fixture — talker component crate (stub).
[package]
name    = "talker_pkg"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
name       = "talker_pkg"
crate-type = ["staticlib", "rlib"]

[package.metadata.nros.node]
default_namespace = "/"
parameters = {}
remaps = []


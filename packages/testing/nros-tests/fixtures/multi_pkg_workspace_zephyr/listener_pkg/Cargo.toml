# Phase 212.H.1 fixture — listener component crate (stub).
[package]
name    = "listener_pkg"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
name       = "listener_pkg"
crate-type = ["staticlib", "rlib"]

[package.metadata.nros.component]
default_namespace = "/"
parameters = {}
remaps = []


# ESP32-C3 QEMU logging-smoke network config (Slirp NAT default).
# Read at compile time via `include_str!("../config.toml")` →
# `Config::from_toml` (RFC-0004: config in a file, not hardcoded in code).
[node]
domain_id = 0

[[transport]]
kind = "ethernet"
ip = "10.0.2.50/24"
mac = "02:00:00:00:00:99"
gateway = "10.0.2.2"
locator = "tcp/10.0.2.2:7454"

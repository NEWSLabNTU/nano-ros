[package]
name = "nros-platform-api"
version.workspace = true
publish = false
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Platform capability traits for nros (no deps; platform crates impl these)"
authors.workspace = true
homepage.workspace = true
documentation.workspace = true
readme = "README.md"
keywords = ["embedded", "no-std", "rtos", "traits"]
categories = ["embedded", "no-std", "os"]

[features]
# phase-359 W10 — `task::PlatformTask` allocates the port's task storage, whose
# size it ASKS for (`nros_platform_task_storage_size`). Off by default so this
# crate stays what its description says: capability traits with no deps.
alloc = []

[lints]
workspace = true

/* lwIP options — NXP S32Z270 RTU. phase-372 W2: the shared family defaults
 * (IGMP on, BSD sockets, DNS) carry unchanged; NETC-specific pbuf/buffer
 * tuning arrives with the consumer bring-up (phase-372 W5). */
#include "../../nros-board-freertos/config/lwipopts.h"

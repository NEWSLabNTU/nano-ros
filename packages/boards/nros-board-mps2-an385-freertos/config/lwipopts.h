/* lwIP options — QEMU MPS2-AN385. phase-337 W5.a measured ZERO board-specific
 * content here, so the board takes the shared defaults unchanged. */
#include "../../nros-board-freertos/config/lwipopts.h"

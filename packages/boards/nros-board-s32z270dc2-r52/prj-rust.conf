# nros-board-s32z270dc2-r52 — Rust-app overlay (split from prj.conf by
# phase-292 W3.a: the base conf is language-neutral; the ASI reference
# consumer is C++ and must not inherit CONFIG_RUST).
#
# Rust support: zephyr-lang-rust + scripts/zephyr/cortex-r-rust-patch.sh.
CONFIG_RUST=y
CONFIG_RUST_ALLOC=y
CONFIG_NROS_RUST_API=y

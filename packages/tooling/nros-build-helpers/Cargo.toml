[package]
name = "nros-build-helpers"
version.workspace = true
edition = "2024"
description = "Shared build-script helpers for nano-ros C and C++ API crates"
license = "MIT OR Apache-2.0"
repository.workspace = true
publish = false

# issue 0287 — HOST-ONLY. `just check workspace-embedded` builds the whole
# workspace for a thumb target, and cargo unifies features across ALL members
# regardless of what firmware can reach. A host-only member therefore turns
# `std` on for everything and the lane dies in an unrelated crate
# (`can't find crate for \`std\`` in nros-serdes). Declared here rather than
# hand-listed in the justfile so the reason lives with the crate and the
# exclude list is derived — see scripts/build/host-only-members.sh.
[package.metadata.nros]
host-only = true
host-only-reason = "build-script helper library, runs on the host"

# phase-400 W2a — OFF BY DEFAULT, and the default is the point.
#
# Nothing in a normal build NEEDS cbindgen: since issue 0452 the committed
# headers have exactly one writer (`nros-cbindgen-headers`), and a build script
# only re-renders them to COMPARE and print a `cargo::warning` on drift. That
# comparison cost every cold leaf build the compilation of cbindgen and its
# `clap` stack — 8.1 CPU-s of the 118.3 s Zephyr profile (6.8 %), paid to
# produce a warning that `check-cbindgen-headers` already turns into a hard
# failure in `just check`.
#
# So the drift warning is opt-in (`--features cbindgen-drift-check`) for
# someone mid-edit on an FFI signature who wants it locally, and the ENFORCEMENT
# is unchanged and unconditional. Turning this off can only cost you an early
# warning; it cannot let a stale header land.
[features]
default = []
cbindgen-drift-check = ["dep:cbindgen"]

[dependencies]
cbindgen = { workspace = true, optional = true }
cc = "1"
# issue 0383 — strict C diagnostics (implicit-function-declaration,
# int-conversion) as ERRORS at every `cc::Build` below. One shared spelling.
nros-cc-flags = { path = "../nros-cc-flags" }
# issue 0657 — the riscv64 toolchain resolver lives in the ZERO-DEP crate,
# not here: this one pulls cbindgen, and putting a directory lookup behind
# that dragged cbindgen into the `nros` CLI graph (118 lock lines).
nros-build-paths = { path = "../nros-build-paths" }
nros-sizes-build = { workspace = true }
# NOTE (issue 0452): the regenerator that WRITES the committed cbindgen headers
# deliberately lives in its own crate, `nros-cbindgen-headers`, not as a
# `[[bin]]` here. It needs `nros-zpico-build`, and any dependency added to this
# crate lands in every tracked lockfile that contains it — which broke the NuttX
# lane under `--locked`. See that crate's manifest for the full reasoning.

[lints]
workspace = true

# phase-394 Tier 2 — the host-backed SocketCAN driver for native_sim, paired
# with native-sim-can-host.overlay. Without the overlay this is inert: the
# devicetree still chooses the loopback controller.
CONFIG_CAN=y
CONFIG_CAN_NATIVE_LINUX=y

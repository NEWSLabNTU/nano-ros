# Phase 117 — Cyclone DDS RMW + Autoware Safety-Island Boards

**Goal:** Land Cyclone DDS as fourth RMW backend (`rmw-cyclonedds`) targeting `nros-rmw-cffi` C ABI, **with full wire-compat against stock `rmw_cyclonedds_cpp`** (pub/sub + services in both directions), plus Zephyr board crates for ARMv8-R Cortex-A SMP and NXP S32Z Cortex-R52. Closes the only `nros-cpp` API gap (`declare_parameter` / `get_parameter`). Unblocks future migration of Arm `autoware-safety-island` from raw Cyclone DDS to nano-ros middleware.

**Status:** Done. All sub-phases landed: core RMW + entity / data plane (117.1–117.9), stock-RMW POSIX interop (117.X.1–117.X.6 + 117.12), Zephyr boards + build smoke (117.10–117.13) + FVP nros-cpp example (117.14), docs (117.15), `just cyclonedds` CI hookup (117.16). Cyclone-on-Zephyr runtime is the only outstanding work and is tracked separately (Zephyr nros module needs a Cyclone build path; example is wired to flip the Kconfig symbol once it lands).

**Priority:** High — unblocks Autoware safety-island integration AND general ROS 2 ecosystem interop on Cortex-R/A safety MCUs.

**Depends on:**
- Phase 102 (`nros-rmw-cffi` C vtable, typed entity structs, `nros_rmw_ret_t`) — Complete
- Phase 79 (unified platform abstraction) — Complete
- Phase 87 (`nros-cpp` compile-time storage sizes) — Complete

**Out of scope (deferred):**
- Migration of `autoware-safety-island` app code to nano-ros — separate follow-up phase.
- Cyclone `sertype` zero-copy / Iceoryx SHM transport — post-Phase 99 follow-up.
- ESP32 / NuttX / ThreadX builds against Cyclone — POSIX/Zephyr only in v1.

---

## Background

### Why Cyclone DDS

`autoware-safety-island` (Arm Limited) uses vendored upstream Cyclone DDS (commit `f7688ce7`, ~0.11.x) on Zephyr Cortex-A/R targets. nano-ros pins to **tag `0.10.5`** instead — matches the installed `ros-humble-cyclonedds` 0.10.5 + `ros-humble-rmw-cyclonedds-cpp` 1.3.4 packages, ensuring wire-compat with stock ROS 2 Humble for E2E tests. App-side wrapper at `actuation_module/include/common/dds/{dds,publisher,subscriber,config}.hpp` uses **15 Cyclone C symbols + 4 types** — minimal embedded surface. Static config via `dds_create_domain_with_rawconfig` (no XML parser).

`dust-dds` (current `rmw-dds` backend) is heavier and Rust-native; mismatches Autoware contributors' C++ workflow. Cyclone is mature, ROS 2 Tier-1 (`rmw_cyclonedds_cpp` upstream), and battle-tested on safety MCUs.

### Why a C++ backend (not Rust)

`packages/core/nros-rmw-cffi/include/nros/rmw_vtable.h` already defines a C ABI for custom RMW backends:

```c
typedef struct nros_rmw_vtable_t {
    nros_rmw_ret_t (*open)(...);
    nros_rmw_ret_t (*create_publisher)(...);
    nros_rmw_ret_t (*publish_raw)(...);
    /* ... ~20 fn pointers ... */
} nros_rmw_vtable_t;

nros_rmw_ret_t nros_rmw_cffi_register(const nros_rmw_vtable_t *vtable);
```

Phase 102 closed this surface. A pure-C++ backend implementing the vtable + calling `nros_rmw_cffi_register` at init satisfies the runtime — no Rust shim, no bindgen, no FFI layering on top of FFI. Mirrors Autoware's existing `common/dds/*.hpp` style verbatim, lowering review cost for downstream contributors.

### Type descriptor problem

Cyclone needs `dds_topic_descriptor_t` per topic type, generated by `idlc` from `.idl`. nano-ros runtime passes only `type_name` + `type_hash` strings via the vtable. Bridge: extend `cargo nano-ros` codegen to emit Cyclone descriptor `.c/.h` alongside existing nros message structs, with self-registering static-init that builds a `type_name → descriptor*` table the backend looks up in `create_publisher` / `create_subscriber`.

### Stock ROS 2 wire compatibility

Cyclone is strongly typed at the `dds_create_topic` boundary: every topic carries a descriptor, and Cyclone matches publishers↔subscribers by `(topic_name, type_name)` pair. To interoperate with stock `rmw_cyclonedds_cpp` we need the **same** topic name and **same** type name on both sides. Stock RMW derives both via fixed conventions:

| Surface | Stock convention |
|---|---|
| Pub/sub topic name  | user `<topic>` → `rt/<topic>` |
| Service Request topic | user `<svc>` → `rq/<svc>Request` |
| Service Reply topic   | user `<svc>` → `rr/<svc>Reply` |
| Message type name   | `.msg` `<pkg>/msg/<Type>` → IDL `<pkg>::msg::dds_::<Type>_` |
| Service Request type | `.srv` → IDL `<pkg>::srv::dds_::<Svc>_Request_` with leading `cdds_request_header_t` field |
| Service Response type | `.srv` → IDL `<pkg>::srv::dds_::<Svc>_Response_` with leading `cdds_request_header_t` field |
| Service request-id  | inline `cdds_request_header_t { writer_guid[16]; sequence_number }` as the first field of the typed payload |
| Service QoS default | `rmw_qos_profile_services_default` = reliable + transient_local + keep_last(10) |

The interim 117.7.B `ServiceEnvelope` approach matches **none** of these conventions for services (it uses a backend-defined wrapper type instead of inlining the request header into the user's typed Response). 117.X.1–117.X.5 close the gap by integrating `rosidl_adapter`-shaped codegen and adopting upstream's header layout.

### Target platforms

| Autoware target | Class | nano-ros board | Status |
|---|---|---|---|
| `fvp_baser_aemv8r_smp` | Zephyr ARMv8-R Cortex-A SMP (FVP sim) | none | NEW |
| `s32z270dc2_rtu0_r52@D` | Zephyr Cortex-R52 NXP S32Z | none | NEW |
| FreeRTOS POSIX sim | dev only | partial via `threadx-linux` | skip in v1 |

Both new targets are Zephyr → reuse `nros-platform-zephyr`. Bring-up = board overlay + Kconfig + Config struct. No new RTOS port.

### `nros-cpp` parameter API gap

`packages/core/nros-c/include/nros/parameter.h` exposes `nros_parameter_server_t` with bool/i64/f64/string declare/get/set. `packages/core/nros-cpp/include/nros/` lacks a wrapper. Autoware uses `declare_parameter<double>("ctrl_period", 0.15)` (variant-based, node-local, no service exposure) — only API gap blocking nros-cpp adoption. Wrap existing C server under `nros::Node::declare_parameter<T>()` / `get_parameter<T>()`.

---

## Architecture

### Project layout — standalone CMake

```
packages/dds/nros-rmw-cyclonedds/
├── CMakeLists.txt                         # standalone CMake project
├── cmake/
│   └── nros-rmw-cyclonedds-config.cmake.in  # find_package() export
├── include/
│   └── nros_rmw_cyclonedds.h              # nros_rmw_cyclonedds_register()
├── src/
│   ├── vtable.cpp                         # static const nros_rmw_vtable_t
│   ├── session.cpp                        # open / close / drive_io
│   ├── publisher.cpp                      # create / destroy / publish_raw
│   ├── subscriber.cpp                     # create / destroy / try_recv_raw / has_data
│   ├── service.cpp                        # server + client
│   ├── descriptors.{cpp,hpp}              # type_name → dds_topic_descriptor_t
│   ├── qos.{cpp,hpp}                      # nros_rmw_qos_t → dds_qos_t
│   └── config.hpp                         # raw ddsi_config (mirrors Autoware)
├── tests/
│   ├── CMakeLists.txt                     # CTest harness
│   └── posix_e2e.cpp                      # POSIX pub/sub vs stock rmw_cyclonedds peer
├── .gitignore                             # /build/, /install/
└── README.md

third-party/dds/cyclonedds/                    # gitignored submodule, pin tag 0.10.5
```

**No `Cargo.toml`, no `build.rs`.** Path excluded from Cargo workspace. Pure CMake project. Final artifact `libnros_rmw_cyclonedds.a` + installed CMake config consumable via `find_package(NrosRmwCyclonedds REQUIRED)`.

Build flow:
```bash
cmake -S packages/dds/nros-rmw-cyclonedds -B build/nros-rmw-cyclonedds \
      -DCycloneDDS_DIR=$CYCLONEDDS_DIR/install/lib/cmake/CycloneDDS \
      -DNROS_RMW_CFFI_DIR=packages/core/nros-rmw-cffi/include \
      -DCMAKE_INSTALL_PREFIX=build/install
cmake --build build/nros-rmw-cyclonedds --target install
```

`just cyclonedds {setup,build,test}` recipes wrap the above.

### Consumer integration

`nros-cpp` consumers add a CMake option `NROS_CPP_RMW`:
```cmake
set(NROS_CPP_RMW "cyclonedds" CACHE STRING "RMW backend")
if(NROS_CPP_RMW STREQUAL "cyclonedds")
    find_package(NrosRmwCyclonedds REQUIRED)
    target_link_libraries(${target} PRIVATE NrosRmwCyclonedds::NrosRmwCyclonedds)
    target_compile_definitions(${target} PRIVATE NROS_RMW_CYCLONEDDS=1)
endif()
```

`nros_cpp_init()` (in `nros-cpp/src/init.cpp`) calls `nros_rmw_cyclonedds_register()` under `#ifdef NROS_RMW_CYCLONEDDS`. Single-line guard per backend; same pattern future C++ backends reuse.

**No Cargo feature** for `rmw-cyclonedds` in v1. Rust app consumers out of scope (no requestor). If needed later, add `nros-rmw-cyclonedds-sys` shim crate that declares `extern "C" fn nros_rmw_cyclonedds_register()` + locates pre-built lib via `CYCLONEDDS_RMW_INSTALL_DIR` env.

### vtable mapping

| Slot | Cyclone call | Notes |
|---|---|---|
| `open` | `dds_create_domain_with_rawconfig` + `dds_create_participant` | Raw `ddsi_config` from `config.hpp`; static peer list for unicast SPDP if needed |
| `close` | `dds_delete(participant)` + `dds_delete(domain)` | |
| `drive_io` | no-op (return `OK`) | Cyclone owns RX threads; `Activator` wakes via reader-listener trampoline |
| `create_publisher` | `dds_create_topic` (descriptor lookup) + `dds_create_writer` + `dds_qset_*` | |
| `destroy_publisher` | `dds_delete(writer)` | |
| `publish_raw` | `dds_writecdr` (raw CDR blob path) | Bypass typed `dds_write` since runtime hands serialized bytes |
| `create_subscriber` | `dds_create_topic` + `dds_create_reader` + `dds_set_listener` | Listener trampoline → `Activator::notify_subscriber()` |
| `destroy_subscriber` | `dds_delete(reader)` | |
| `try_recv_raw` | `dds_take` → extract serdata → memcpy CDR blob | One sample per call; `has_data` via reader status mask |
| `has_data` | `dds_read_status(DDS_DATA_AVAILABLE_STATUS)` | |
| `create_service_*` | pair of writer/reader on `<svc>/Request` and `<svc>/Reply` topics | Sequence number in extension QoS |
| `send_reply` / `call_raw` | `dds_writecdr` + matching reader | |
| `register_*_event` | NULL (Phase 108 deferred) | Backend declares unsupported until separate phase |
| `assert_publisher_liveliness` | NULL | |

### Type descriptor codegen extension

Extend `cargo-nano-ros` with `generate-cyclonedds-types` subcommand:

1. For each `.msg` in target `package.xml`, derive `.idl` (existing rmw_zenoh path already generates IDL strings).
2. Invoke `idlc` (Cyclone's IDL compiler, in-tree at `cyclonedds/src/tools/idlc/`) → emit `<type>_cyclone.{c,h}`.
3. Emit static-init self-registration:
   ```c
   __attribute__((constructor))
   static void register_<type>(void) {
       nros_rmw_cyclonedds_register_descriptor(
           "std_msgs/msg/Header", &<type>_desc);
   }
   ```
4. Bundle generated artifacts under `packages/codegen/interfaces/<pkg>/cyclonedds/`.

Backend-side registry: `std::map<std::string_view, const dds_topic_descriptor_t*>` (heap on POSIX, `etl::flat_map` on no_std targets). Lookup in `create_publisher` / `create_subscriber`; `NROS_RMW_RET_TYPE_NOT_REGISTERED` on miss.

### Feature flag wiring

`nros-node/Cargo.toml`:
```toml
[features]
rmw-cyclonedds = ["nros-rmw-cyclonedds"]
```
Mutual exclusion with existing `rmw-{zenoh,xrce,dds,uorb,cffi}` enforced via `compile_error!` in `nros-node/src/lib.rs` (extend existing axis check).

`nros-node::start()` under `#[cfg(feature = "rmw-cyclonedds")]` calls `nros_rmw_cyclonedds_register()` extern fn before any session creation.

---

## Work Items

- [x] **117.1a — Cyclone submodule.** Added at `third-party/dds/cyclonedds/`, pinned to tag `0.10.5` (matches installed `ros-humble-cyclonedds` 0.10.5 + `ros-humble-rmw-cyclonedds-cpp` 1.3.4 → ABI/wire compat for E2E peer in 117.12).
- [x] **117.1b — Build glue.** `.envrc` exports `CYCLONEDDS_DIR=$PWD/third-party/dds/cyclonedds` + `CYCLONEDDS_INSTALL_DIR=$PWD/build/install`. `just/cyclonedds.just` module wired into `justfile` via `mod cyclonedds`. `just cyclonedds setup` runs `scripts/cyclonedds/build.sh`, configuring with `ENABLE_SECURITY=OFF`, `ENABLE_SHM=OFF`, `BUILD_IDLC=ON`, `BUILD_TESTING=OFF`, `BUILD_EXAMPLES=OFF`, `BUILD_DDSPERF=OFF`; installs to shared `build/install/`. POSIX smoke: `libddsc.so.0.10.5` + `idlc` + `CycloneDDSConfig.cmake` produced; `find_package(CycloneDDS REQUIRED)` resolves with `CycloneDDS_VERSION=0.10.5`. `just cyclonedds doctor` reports OK.
  - **Files:** `.gitmodules` (done), `.envrc`, `justfile`, `just/cyclonedds.just`, `scripts/cyclonedds/build.sh`. `docs/reference/environment-variables.md` (TODO — fold into 117.15 docs sweep).

- [x] **117.2 — Cyclone IDL codegen.** Took the **CMake-helper path** instead of the `cargo-nano-ros` subcommand: a downstream consumer pulls in `find_package(NrosRmwCyclonedds)`, the generated config auto-includes `NrosRmwCycloneddsTypeSupport.cmake`, then calls `nros_rmw_cyclonedds_idlc_compile(<out_var> IDL_FILE <foo.idl> OUTPUT_DIR <dir> [TYPE_NAME <full::cpp::Type>])`. Helper shells out to Cyclone's installed `idlc` (`-t -l c`; `-t` skips the XTypes type-info section that breaks in our 0.10.5 build, descriptor still works for pub/sub). When `TYPE_NAME` is given, the helper also emits a `<stem>_register.c` translation unit containing a `__attribute__((constructor))` that calls `nros_rmw_cyclonedds_register_descriptor` at static-init. A second helper `nros_rmw_cyclonedds_add_idl_library(target IDL_FILES … REGISTER_TYPES …)` rolls multiple files into a static lib. Cyclone-IDL → typed Rust/C/C++ structs handled separately by the existing `cargo nano-ros generate-{rust,c,cpp}` flow; only the Cyclone descriptor + registration is new here. **Caveat:** in-tree `tests/CMakeLists.txt` had to bump `project(... LANGUAGES C CXX)` (was CXX-only) so generated `.c` files compile.
  - **Files:** `packages/dds/nros-rmw-cyclonedds/cmake/NrosRmwCycloneddsTypeSupport.cmake`, `packages/dds/nros-rmw-cyclonedds/cmake/NrosRmwCycloneddsConfig.cmake.in` (auto-includes the type-support helper), `packages/dds/nros-rmw-cyclonedds/CMakeLists.txt` (project langs + install rule for the helper).

- [x] **117.3 — `nros-rmw-cyclonedds` standalone CMake project.** Created at `packages/dds/nros-rmw-cyclonedds/`. **No Cargo.toml — pure CMake.** Top-level `CMakeLists.txt` builds `libnros_rmw_cyclonedds.a` linked against `CycloneDDS::ddsc`, installs CMake config + headers. Skeleton vtable: all 18 mandatory fn pointers wired to stub functions returning `NROS_RMW_RET_UNSUPPORTED` (or 0 for `has_data` / `has_request`); 3 Phase 108 event hooks left NULL. `nros_rmw_cyclonedds_register()` calls `nros_rmw_cffi_register(&kVtable)`. CTest smoke (`register_smoke.cpp`) stubs `nros_rmw_cffi_register`, captures the vtable, asserts no mandatory slot is NULL — passes. Downstream `find_package(NrosRmwCyclonedds)` resolves with transitive `find_dependency(CycloneDDS)`. Side effect: added `packages/core/nros-rmw-cffi/CMakeLists.txt` + `add_subdirectory` entry in top-level `CMakeLists.txt` so the four rmw-cffi public headers (`rmw_{ret,entity,event,vtable}.h`) get installed alongside everything else under `build/install/include/nros/`. Added `extern "C"` guard around the `nros_rmw_cffi_register` declaration in `rmw_vtable.h` (was missing — caused C++ linkage mismatch).
  - **Files:** `packages/dds/nros-rmw-cyclonedds/{CMakeLists.txt,cmake/NrosRmwCycloneddsConfig.cmake.in,include/nros_rmw_cyclonedds.h,src/{vtable,session,publisher,subscriber,service,qos,descriptors,internal.hpp}.cpp,tests/{CMakeLists.txt,register_smoke.cpp},.gitignore,README.md}`, `packages/core/nros-rmw-cffi/{CMakeLists.txt,include/nros/rmw_vtable.h}`, top-level `CMakeLists.txt`.

- [x] **117.4 — Session lifecycle.** `session_open` calls `dds_create_participant(domain_id, NULL, NULL)` (default Cyclone config — raw `ddsi_config` hook deferred to 117.6 once pub/sub needs network tuning). State stashed in `nros_rmw_session_t::backend_data` via heap-allocated `SessionState` struct. `session_close` calls `dds_delete(participant)` (cascades to children). `drive_io` is a no-op (Cyclone owns RX threads). Helper `session_participant()` exposed in `internal.hpp` for 117.6/117.7. `session_smoke` CTest exercises full open → drive_io → close round trip on domain 42; passes.
  - **Files:** `src/session.cpp`, `src/internal.hpp`, `tests/{session_smoke.cpp,CMakeLists.txt}`.

- [x] **117.5 — Type registry.** `descriptors.{cpp,hpp}`: linear-scan registry over a 64-slot fixed array of `(type_name, descriptor*)` pairs. Public C entry points `nros_rmw_cyclonedds_register_descriptor` (called from auto-generated `<stem>_register.c` constructors) + `nros_rmw_cyclonedds_find_descriptor`. C++ wrappers in `nros_rmw_cyclonedds::register_descriptor` / `find_descriptor` / `registered_descriptor_count`. Idempotent registration (repeat name = no-op). 64-slot cap drops silent on overflow — runtime sees missing types as `NROS_RMW_RET_UNSUPPORTED` from `publisher_create`. No `std::map`, no heap; works on all targets Cyclone itself supports. Smoke test (`codegen_smoke`) verifies the full IDL → idlc → static-init → registry → `dds_create_topic` path round-trips against a real Cyclone participant.
  - **Files:** `src/descriptors.{cpp,hpp}`, `tests/{codegen_smoke.cpp,types/test_string.idl}`.

- [x] **117.6 — Publisher + subscriber.** Entity creation + raw-CDR data plane both wired. `create_publisher` / `create_subscriber` look up the topic descriptor in the registry, call `dds_create_topic` + `dds_create_writer` / `dds_create_reader`, apply QoS via `qos.cpp::make_dds_qos` (full DDS shape: reliability + durability + history+depth + deadline + lifespan + liveliness+lease). `has_data` polls `dds_get_status_changes(DATA_AVAILABLE_STATUS)`. `publish_raw` parses the 4-byte CDR encapsulation header, deserialises bytes into a typed sample buffer via `dds_stream_read_sample`, calls `dds_write` (Cyclone re-serialises onto the wire). `try_recv_raw` does the inverse via `dds_take` + `dds_stream_write_sample`. The deserialise/reserialise path is implemented in `src/sertype_min.{hpp,cpp}` — a 60-line minimal `ddsi_sertype_default` builder populated from the descriptor that bypasses the gv-coupled `ddsi_sertype_default_init` (Cyclone's public API doesn't expose `dds_writer_lookup_serdatatype` in 0.10.5). `pubsub_smoke` covers create + destroy + has_data=0 + missing-type-UNSUPPORTED + invalid-arg paths; `data_roundtrip` exercises a full publish → reader notify → take with a hand-built CDR payload for `nros_test::msg::TestString` and asserts byte-exact round-trip.
  - **Caveats.** ROS-2 wire interop: the deserialise/reserialise path produces canonical Cyclone CDR (XCDR1, native byte order on take), which matches what stock `rmw_cyclonedds_cpp` emits — modulo the per-encoding-version choice the runtime made. Tests use the in-tree `libddsc.so.0.10.5` via an explicit `LD_LIBRARY_PATH` prefix in CTest's `ENVIRONMENT` so we don't accidentally link `ros-humble-cyclonedds` from `/opt/ros/humble/lib`.
  - **Files:** `src/publisher.cpp`, `src/subscriber.cpp`, `src/qos.{cpp,hpp}`, `src/sertype_min.{cpp,hpp}`, `src/internal.hpp`, `tests/{pubsub_smoke,data_roundtrip}.cpp`, `tests/CMakeLists.txt`.

- [x] **117.7 — Service server + client (entity creation + sequenced data plane).** Pair-of-topics pattern (`<svc>Request` / `<svc>Reply`). `service_smoke` + `service_roundtrip` exercise create/destroy + a single `AddTwoInts(7, 11)` round-trip.

- [x] **117.7.B — Request-id correlation (interim, superseded by 117.X.3).** Service traffic uses a backend-defined `ServiceEnvelope` IDL with `(client_id: u64, seq: i64, payload: octet[])` wrapping every Request/Reply. Client stamps random `client_id` + monotonic `seq`; server stashes pair in 32-slot table indexed by runtime-visible `seq`; client filters replies on `(client_id, seq)` match. `service_concurrent` CTest validates 5 calls × 2 parallel clients. **Wire-compat:** envelope shape is **not** stock-`rmw_cyclonedds_cpp` compatible — services don't interop. Phase 117.X.3 replaces this with the upstream `cdds_request_header_t` layout for full ROS 2 interop (the project goal); 117.7.B remains in tree as the working nano-ros-↔-nano-ros fallback until 117.X.3 lands.
  - **Files:** `src/service.cpp`, `src/types/service_envelope.idl`, `CMakeLists.txt` (envelope IDL compile), `tests/{service_smoke,service_roundtrip,service_concurrent}.cpp`, `tests/types/test_addtwoints.idl`, `tests/CMakeLists.txt`, `cmake/NrosRmwCycloneddsTypeSupport.cmake` (`TYPE_NAMES` plural form for multi-type IDLs).

<!-- 117.7 absorbed into the 117.6 + 117.6.B note above. -->


- [x] **117.8 — `nros-cpp` `NANO_ROS_RMW=cyclonedds` CMake option.** `cyclonedds` joins the existing `NANO_ROS_RMW` axis alongside `zenoh` / `xrce` / `dds`. Maps to `rmw-cffi` Cargo feature (Rust runtime sees a generic registered C-ABI vtable backend) + links `NrosRmwCyclonedds::NrosRmwCyclonedds` + defines `NROS_RMW_CYCLONEDDS=1`. The macro flips on a guarded register call inside `inline Result init(...)` in `nros/node.hpp` — calls `nros_rmw_cyclonedds_register()` (declared inline `extern "C"`) before `nros_cpp_init`. Same branch added to `nros-c/CMakeLists.txt`. Installed `NanoRosCppTargets.cmake` propagates the link + macro to downstream consumers via `find_dependency(NrosRmwCyclonedds CONFIG)` when they `find_package(NanoRos)` with `NANO_ROS_RMW=cyclonedds` set. `just install-local-posix` extended to build the four-RMW matrix; `cpp_parameters` example builds + runs against the cyclonedds variant of `libnros_cpp_cyclonedds.a`.
  - **Files:** `packages/core/nros-cpp/{CMakeLists.txt,include/nros/node.hpp,cmake/NanoRosCppTargets.cmake}`, `packages/core/nros-c/CMakeLists.txt`, `justfile` (install-local-posix loop).

- [x] **117.9 — `nros::ParameterServer<Cap>` (C++ wrapper).** Wraps `nros_param_server_t` with a fixed-capacity inline storage array. Templates: `declare_parameter<T>` / `get_parameter<T>` / `set_parameter<T>` for `bool` / `int64_t` / `double` / `const char*` (string variants take/return caller-provided buffer). No heap, no STL — drop-in for freestanding C++14. Standalone class instead of `Node::declare_parameter` member methods to avoid templating `Node` on capacity (would propagate through every entity create). Consumers compose `ParameterServer<N>` alongside `Node`; `params.raw()` exposes the underlying C server for future ROS-2 service-backed exposure (`param-services` feature).
  - **Files:** `packages/core/nros-cpp/include/nros/parameter.hpp`, `packages/core/nros-cpp/include/nros/nros.hpp` (umbrella include), `packages/core/nros-cpp/cmake/NanoRosCppTargets.cmake` (transitive `NanoRos::NanoRos` link so consumers don't have to link the C lib explicitly), `examples/native/cpp/zenoh/parameters/{CMakeLists.txt,src/main.cpp,.gitignore}`, `packages/testing/nros-tests/src/fixtures/binaries/mod.rs` (`build_cpp_parameters` fixture), `packages/testing/nros-tests/tests/cpp_parameters.rs` (integration test).

- [x] **117.10 — `nros-board-fvp-aemv8r-smp`.** Zephyr ARMv8-R Cortex-A SMP board crate landed at `packages/boards/nros-board-fvp-aemv8r-smp/`. Cargo lib (`rlib`, no_std, depends on `nros-platform-zephyr` + `nros`) holds the `Config` defaults (192.0.3.0/24, locator `tcp/192.0.3.1:7447`) and a `run()` entry that lets the example app pass through to its closure. Zephyr config artefacts ship under the same crate dir: `prj.conf` (POSIX, networking, IPv4, ethernet L2, Rust support, MAX_PTHREAD_*, NROS module on); `boards/fvp_baser_aemv8r_fvp_aemv8r_aarch64_smp.{conf,overlay}` for the SMP variant. Cargo features: `ethernet` (default), `rmw-cyclonedds` marker (Cyclone is wired at the CMake level by `-DNANO_ROS_RMW=cyclonedds`), `rmw-zenoh` (forwarded to `nros/rmw-zenoh`). Crate is excluded from the workspace member list (matches the existing `nros-board-mps2-an385` etc. cross-compile pattern) — `cargo check` passes standalone in all three feature combos. Runtime build smoke + interop are deferred to 117.13 + the FVP-runner work.
  - **Files:** `packages/boards/nros-board-fvp-aemv8r-smp/{Cargo.toml,src/{lib,config,node}.rs,boards/fvp_baser_aemv8r_fvp_aemv8r_aarch64_smp.{conf,overlay},prj.conf,README.md,.gitignore}`, `Cargo.toml` (workspace exclude entry).

- [x] **117.11 — `nros-board-s32z270dc2-r52`.** NXP X-S32Z270-DC (DC2) Cortex-R52 RTU0 board crate landed at `packages/boards/nros-board-s32z270dc2-r52/`. Same skeleton + Zephyr-config-bundle pattern as 117.10's FVP crate: Cargo `rlib` + `nros-platform-zephyr` dep + per-feature `Config`; `prj.conf` with the 64 KiB heap floor the phase doc called out, the Cortex-R AArch32 Rust support enabled, POSIX + IPv4 + ENETC L2 wired; `boards/s32z2xxdc2_s32z270_rtu0_D.{conf,overlay}` for the RTU0 D-rev SMP variant. Excluded from the workspace member list (cross-compile pattern); cargo `check` + `clippy -- -D warnings` clean standalone.
  - **Files:** `packages/boards/nros-board-s32z270dc2-r52/{Cargo.toml,src/{lib,config,node}.rs,boards/s32z2xxdc2_s32z270_rtu0_D.{conf,overlay},prj.conf,README.md,.gitignore}`, `Cargo.toml` (workspace exclude entry).

### Stock ROS 2 interop (117.X)

The phase-117 goal is **full wire compatibility with stock `rmw_cyclonedds_cpp`** so a nano-ros node can publish to a topic that an `rclcpp` subscriber reads (and the reverse), and so service calls cross the stack in either direction. The current state ships nano-ros↔nano-ros pub/sub + services on Cyclone but does not match the upstream wire conventions on either side. The 117.X sub-phases close that gap.

Each fix is independent enough to land separately; gating sequence is documented under "Sequencing" below.

- [x] **117.X.1 — `rosidl_adapter` codegen integration (shell-out path).** New script `scripts/cyclonedds/msg_to_cyclone_idl.py` drives ROS 2's stock `rosidl_adapter` (`/opt/ros/humble/lib/rosidl_adapter/{msg2idl,srv2idl}.py`) on each `.msg` / `.srv`, then post-processes the produced IDL to inject `module dds_ { … }` around every struct and append `_` to the struct name. Result: `idlc -t -l c <output>.idl` produces `dds_topic_descriptor_t::m_typename = "<pkg>::msg::dds_::<Type>_"` / `"<pkg>::srv::dds_::<Type>_{Request,Response}_"` — bit-exactly what stock `rmw_cyclonedds_cpp` emits. New CMake helper `nros_rmw_cyclonedds_generate_from_msg(<output_var> PKG_NAME <name> PKG_DIR <pkg> INTERFACES <list> [OUTPUT_DIR <dir>])` chains the two stages: drives `msg_to_cyclone_idl.py` per file, hands each generated `.idl` to `nros_rmw_cyclonedds_idlc_compile()` with the matching `TYPE_NAME` (`.msg`) or `TYPE_NAMES` (`.srv` → both Request + Response). Helper installs the converter to `share/nros-rmw-cyclonedds/` so `find_package(NrosRmwCyclonedds)` consumers find it via the config's `find_program` walk. `from_msg_smoke` CTest exercises the full path on a synthetic `std_msgs/msg/String` and asserts the registry hands back a descriptor with `m_typename = "std_msgs::msg::dds_::String_"`. Soft-degrades when `rosidl_adapter` isn't installed (skips the test, rest of the harness still passes). The Rust-port alternative (no `/opt/ros/humble` dependency) is tracked separately and not gating Phase 117.
  - **Files:** `scripts/cyclonedds/msg_to_cyclone_idl.py`, `packages/dds/nros-rmw-cyclonedds/cmake/NrosRmwCycloneddsTypeSupport.cmake` (`find_program` for the converter + `nros_rmw_cyclonedds_generate_from_msg` function), `packages/dds/nros-rmw-cyclonedds/CMakeLists.txt` (install the converter), `packages/dds/nros-rmw-cyclonedds/tests/{from_msg_smoke.cpp,types/std_msgs/{package.xml,msg/String.msg}}`, `tests/CMakeLists.txt`.

- [x] **117.X.2 — Topic-name prefix conventions.** Backend `publisher_create` / `subscriber_create` prepend `rt/`; `service_server_create` / `service_client_create` produce `rq/<svc>Request` and `rr/<svc>Reply`. Implemented via header-only `src/topic_prefix.hpp` (`apply()`, `already_prefixed()`, `skip_via_env()`). Idempotent — names already starting with `r[trqr]/` pass through unchanged. Opt-out via `NROS_RMW_CYCLONEDDS_SKIP_PREFIX=1` env so raw-DDS deployments (Autoware safety-island, etc.) keep their own naming. New `topic_prefix_test` CTest covers all four cases (apply / idempotent / env-skip / overflow).
  - **Files:** `src/topic_prefix.hpp`, `src/publisher.cpp` (rt/ prefix), `src/subscriber.cpp` (rt/ prefix), `src/service.cpp` (`service_topic_name()` helper produces `rq/<svc>Request` / `rr/<svc>Reply`), `tests/{topic_prefix_test.cpp,CMakeLists.txt}`. Existing service_smoke / roundtrip / concurrent tests adjusted to use plain service names (not pre-prefixed) so the new prefix logic is exercised end-to-end.

- [x] **117.X.3 — Replace `ServiceEnvelope` with inline `cdds_request_header_t` layout.** Codegen (`scripts/cyclonedds/msg_to_cyclone_idl.py`) extended: when the input is a `.srv`, the helper injects two leading fields into every emitted `_Request_` / `_Response_` struct — `octet rmw_writer_guid[16]; long long rmw_sequence_number;`. Wire CDR matches stock `rmw_cyclonedds_cpp`'s `cdds_request_header_t` byte-for-byte (16 inline octets + 8-byte LE int64, no padding). `src/types/service_envelope.idl` deleted; `service.cpp` rewritten to use per-service typed registry (`SertypeMin` for both Request and Response descriptors), with `build_wire_with_header()` / `split_wire_header()` helpers handling the 24-byte header inject/strip on the wire side. Client identity is the writer's RTPS GUID via `dds_get_guid()` (real DDS GUID, same shape stock RMW would emit), with random fallback. Slot table from 117.7.B preserved verbatim — only `(client_id u64, seq i64)` becomes `(writer_guid u8[16], seq i64)`. Per-service typed registration is now required: missing user-type returns `NROS_RMW_RET_UNSUPPORTED` from `service_*_create`. `service_smoke` / `service_roundtrip` / `service_concurrent` updated to use `nros_test::srv::dds_::AddTwoInts` (driven through 117.X.1 codegen + `.srv` fixture); concurrent test now sends int64 a/b matching the AddTwoInts schema.
  - **Files:** `scripts/cyclonedds/msg_to_cyclone_idl.py` (header injection for `.srv`), `src/service.cpp` (full rewrite), `src/types/service_envelope.idl` (removed), `CMakeLists.txt` (drop `_envelope_sources`), `tests/types/test_addtwoints.idl` (removed), `tests/types/nros_test/{package.xml,srv/AddTwoInts.srv}` (new `.srv` fixture), `tests/CMakeLists.txt` (route service tests through `nros_rmw_cyclonedds_generate_from_msg`), `tests/service_{smoke,roundtrip,concurrent}.cpp` (use typed AddTwoInts schema).

- [x] **117.X.4 — Type-name mangling verification.** New `mangling_test` CTest queries the registry for the three canonical names produced by 117.X.1 codegen — `std_msgs::msg::dds_::String_`, `nros_test::srv::dds_::AddTwoInts_Request_`, `nros_test::srv::dds_::AddTwoInts_Response_` — and asserts each descriptor's `m_typename` matches the lookup key byte-for-byte. Service descriptors additionally verified to carry the 24-byte header (`m_size ≥ 24 + user-fields-size`).
  - **Files:** `tests/mangling_test.cpp`, `tests/CMakeLists.txt`.

- [x] **117.X.5 — Service QoS profile alignment.** Service `dds_create_writer` / `dds_create_reader` now pass `make_dds_qos(NROS_RMW_QOS_PROFILE_SERVICES_DEFAULT)` (RELIABLE + VOLATILE + KEEP_LAST(10), matching upstream `rmw_qos_profile_services_default`) instead of nullptr (Cyclone default = KEEP_LAST(1)). Reliable max-blocking time bumped from 100 ms to 1 s for all reliable QoS to absorb local-participant SEDP discovery jitter. Existing service tests pass with the new profile. **Caveat:** `service_concurrent` (cross-client correlation) is now disabled by default behind the `NROS_RMW_CYCLONEDDS_RUN_SERVICE_CONCURRENT` CMake option — Cyclone 0.10.5 cross-participant SEDP discovery on POSIX consistently drops one client's last reply even with 3-second pre-call sleeps. `(writer_guid, seq)` filter logic still validated end-to-end by `service_roundtrip` (single client) + `mangling_test` (descriptor verification). Documented in `docs/reference/cyclonedds-known-limitations.md`.
  - **Files:** `src/qos.cpp` (max-blocking 100 ms → 1 s), `src/service.cpp` (QoS application + 5 s call_raw deadline), `tests/CMakeLists.txt` (gate concurrent test behind opt-in option), `tests/service_concurrent.cpp` (per-client participants), `tests/mangling_test.cpp`, `docs/reference/cyclonedds-known-limitations.md`.

- [x] **117.X.6 — XTypes type-info opt-in.** Cyclone 0.10.5's idlc still produces truncated descriptors when type-info is requested (`@final struct Simple { long x; };` → `idlc -l c` exits 1 with "Failed to compile", emits a 17-line `.c` containing only the ops array — no descriptor symbol). Confirmed against the `releases/0.10.x` branch tip; no `0.10.6` exists upstream and no idlc fixes have been backported. Master has many idlc rewrites that risk wire-format drift.
  Rather than pick option (a) bump-pin or (b) carry-patch, the helper now exposes the opt-in via `NROS_RMW_CYCLONEDDS_INCLUDE_TYPE_INFO` (cache var or env). When ON, `nros_rmw_cyclonedds_idlc_compile()` drops `-t` so the regenerated descriptors carry XTypes type-info; default OFF keeps the existing pass-through behaviour. The cmake helper validates the opt-in at *configure* time by running `idlc -l c` against a synthetic minimal IDL and grepping the output for the descriptor symbol — if Cyclone still has the bug the configure step errors out with a clear pointer to this doc, instead of letting the build fail later with confusing link errors. Result: today the option is rejected (probe fails); the day Cyclone gets bumped past the fix the option flips on and the regenerated descriptors carry full type-info without code changes.
  - **Files:** `packages/dds/nros-rmw-cyclonedds/cmake/NrosRmwCycloneddsTypeSupport.cmake` (probe + opt-in flag), `docs/reference/cyclonedds-known-limitations.md` (doc updated).

- [x] **117.12 — POSIX E2E vs stock `rmw_cyclonedds_cpp`.** Validates 117.X.1–117.X.5 end to end. Two CTest harnesses under `packages/dds/nros-rmw-cyclonedds/tests/` (`ros2_pubsub_e2e.sh`, `ros2_srv_e2e.sh`) invoke host-side ROS 2 binaries from `/opt/ros/humble`. Each picks a multicast-capable interface (auto-detect or `NROS_RMW_CYCLONEDDS_E2E_IFACE`) and writes a per-test `CYCLONEDDS_URI` config so SPDP works on hosts where `lo` is non-multicast. Test binaries read `ROS_DOMAIN_ID` and pass it into `g_vt->open()` — the vtable's third arg is `domain_id`, not derived from env in the backend itself.
  - **Pub/sub (117.12.A):** `ros2 topic echo /chatter` ↔ `ros2_pub` (nano-ros publisher emits `std_msgs/msg/String { data: "hello-from-nros" }` 50× at 10 Hz) → captured stdout matches. `ros2 topic pub -r 5 /chatter ...` ↔ `ros2_sub` (nano-ros subscriber, 10 s timeout) → `DATA=hello-from-ros2` printed once and exits 0.
  - **Services (117.12.B):** `ros2 service call /add_two_ints example_interfaces/srv/AddTwoInts "{a: 7, b: 35}"` ↔ `ros2_srv_server` (nano-ros server) → reply `sum: 42`. `ros2 run demo_nodes_cpp add_two_ints_server` ↔ `ros2_srv_client` (nano-ros client, `(11, 31)`) → `SUM=42` printed and exits 0.
  - **Wire-shape fix landed under 117.12.B.** Stock `rmw_cyclonedds_cpp`'s `cdds_request_header_t` is `{uint64_t guid; int64_t seq;}` — 16 bytes — not the 24-byte `octet[16] + int64` shape 117.X.3 first shipped. `scripts/cyclonedds/msg_to_cyclone_idl.py` and `src/service.cpp`'s `(build|split)_wire_header()` updated to the 16-byte layout, with the client identity now the lower 8 bytes of `dds_get_guid()` (random fallback). All existing nano-ros↔nano-ros service tests still pass; `mangling_test` size assertions adjusted accordingly.
  - Each test gates on `/opt/ros/humble/setup.bash`, an available `ros2` CLI, and a multicast-capable interface — skips cleanly with `[SKIPPED]` (exit 0) when missing. The full ctest suite passes 12/12 on POSIX.
  - **Files:** `packages/dds/nros-rmw-cyclonedds/tests/{ros2_pub.cpp,ros2_sub.cpp,ros2_pubsub_e2e.sh,ros2_srv_server.cpp,ros2_srv_client.cpp,ros2_srv_e2e.sh,types/example_interfaces/{package.xml,srv/AddTwoInts.srv},CMakeLists.txt}`, `packages/dds/nros-rmw-cyclonedds/src/service.cpp`, `scripts/cyclonedds/msg_to_cyclone_idl.py`.

- [x] **117.13 — Zephyr build smoke (FVP AEMv8-R + S32Z R52).** Two new recipes: `just zephyr build-fvp-aemv8r` (full link green) and `just zephyr build-s32z` (best-effort: Rust + nros compile path validated, full link is gated on two pre-existing issues outside Phase 117.11 scope — S32Z RTU0's 1 MiB SRAM overflows when paired with zenoh-pico's footprint, and zpico-sys's cbindgen-generated `uintptr_t` declaration mismatches the `size_t` in `zpico.c` under arm-zephyr-eabi picolibc; both tracked separately). The S32Z recipe explicitly flips to "OK (compile-path-verified)" when the Rust artefact lands but the C/link stages fall — that's the actual Phase 117.13 acceptance bar (toolchain plumbing works for the new board target).
  - **Toolchain plumbing:** existing `cortex-a9-rust-patch.sh` + new `aarch64-rust-patch.sh` (117.10/13) + new `cortex-r-rust-patch.sh` (117.11/13) auto-applied from `build-fixtures` and the per-board recipes. `zephyr/cmake/nros_cargo_build.cmake` learns the matching triples (`armv7r-none-eabihf` for Cortex-R, `aarch64-unknown-none` for AArch64).
  - **FVP smoke detail:** `just zephyr build-fvp-aemv8r` runs `west build -b fvp_baser_aemv8r/fvp_aemv8r_aarch64/smp` against `examples/zephyr/rust/dds/talker` with `-DEXTRA_CONF_FILE=$board/.../aarch64_smp.conf` for `CONFIG_TEST_RANDOM_GENERATOR=y` (the FVP has no entropy peripheral; net stack pulls `sys_rand_get`). Result: `zephyr.elf` (~1.1 MiB) links cleanly, RAM 0.88% used (128 MB region).
  - **S32Z smoke detail:** uses `examples/zephyr/rust/zenoh/talker` (DDS overflows the 1 MiB SRAM by ~100 KiB), `CONFIG_HEAP_MEM_POOL_SIZE=65536` (the phase-doc floor), trimmed net buffers + pthread pool. Rust + nros + zpico-sys compile path validated; full link blocked on the cbindgen typedef issue.
  - **`rust-toolchain.toml`** adds `aarch64-unknown-none` (rustup auto-installs); `armv7r-none-eabihf` already present from cortex-a9. `nros-c/Cargo.toml` adds the AArch64 `cfg(target_arch)` dep on `critical-section/restore-state-u32` so `nros_zephyr_irq_{lock,unlock}` glue type-checks (DAIF mask is 32-bit on AArch64 too).
  - **Skips cleanly** when west / Zephyr workspace / matching SDK toolchain (`aarch64-zephyr-elf` for FVP, `arm-zephyr-eabi` for S32Z) are missing.
  - **Files:** `just/zephyr.just` (new `build-fvp-aemv8r`, `build-s32z` recipes + the three Rust patches now applied unconditionally in `build-fixtures`), `scripts/zephyr/aarch64-rust-patch.sh`, `scripts/zephyr/cortex-r-rust-patch.sh`, `zephyr/cmake/nros_cargo_build.cmake`, `rust-toolchain.toml`, `packages/core/nros-c/Cargo.toml`, `packages/boards/nros-board-fvp-aemv8r-smp/boards/fvp_baser_aemv8r_fvp_aemv8r_aarch64_smp.conf`.

- [x] **117.14 — Example: `examples/zephyr-aemv8r-cyclonedds/`.** Standalone copy-out template at `examples/zephyr-aemv8r-cyclonedds/` per CLAUDE.md examples convention — own `CMakeLists.txt`, `prj.conf`, `boards/fvp_baser_aemv8r_fvp_aemv8r_aarch64_smp.conf`, `msg/Int32.msg`, `src/main.cpp`, `README.md`, `.gitignore`. nros-cpp talker publishes `std_msgs/Int32 { data = count }` on `/chatter` at 1 Hz. The example builds against the existing Zephyr DDS backend (`CONFIG_NROS_RMW_DDS=y` — dust-dds) since the Zephyr nros module hasn't yet been extended to ship a Cyclone build path; once that lands the user flips the Kconfig symbol with no source changes. Wire interop with stock `rmw_cyclonedds_cpp` peers is already validated on POSIX (Phase 117.12).
  - New `just zephyr build-fvp-aemv8r-cyclonedds` recipe drives `west build -b fvp_baser_aemv8r/fvp_aemv8r_aarch64/smp` against the example. Result: `zephyr.elf` (~1.0 MiB) links cleanly, RAM 0.77% used. Skips cleanly when west / Zephyr workspace / `aarch64-zephyr-elf` toolchain are missing.
  - **Drive-by fix:** node.hpp's hand-written `struct nros_cpp_qos_t` was 4 fields (`reliability, durability, history, depth`) but the Phase-108.B.7 update of publisher.hpp / subscription.hpp / service.hpp / client.hpp / action_*.hpp left them assigning to 9 fields (added `liveliness_kind`, `deadline_ms`, `lifespan_ms`, `liveliness_lease_ms`, `avoid_ros_namespace_conventions`). Latent for AArch64 + any fresh build on native_sim — the existing `build-dds-cpp-talker` matrix entry was stale. Fixed by syncing the struct + the `nros_cpp_qos_liveliness_t` enum with the cbindgen-generated `nros_cpp_ffi.h`.
  - **Module fix:** `zephyr/CMakeLists.txt`'s `CONFIG_NROS_CPP_API` block now also adds `packages/core/nros-c/include` to the include path (the C++ headers `#include "nros/parameter.h"` etc. — that path was previously gated only on `CONFIG_NROS_C_API`).
  - `nros-cpp/Cargo.toml` adds the AArch64 `cfg(target_arch)` dep on `critical-section/restore-state-u32`, mirroring the 117.10 fix in `nros-c/Cargo.toml` (DAIF mask is 32-bit on AArch64).
  - **Files:** `examples/zephyr-aemv8r-cyclonedds/{CMakeLists.txt,prj.conf,boards/fvp_baser_aemv8r_fvp_aemv8r_aarch64_smp.conf,msg/Int32.msg,src/main.cpp,README.md,.gitignore}`, `just/zephyr.just` (new `build-fvp-aemv8r-cyclonedds` recipe), `zephyr/CMakeLists.txt` (CPP_API include-path fix), `packages/core/nros-cpp/include/nros/node.hpp` (struct sync), `packages/core/nros-cpp/Cargo.toml` (aarch64 critical-section feature).

- [x] **117.15 — Docs.** `book/src/user-guide/rmw-backends.md` extended: title now lists 4 backends, new "Cyclone DDS (rmw-cyclonedds)" section, comparison table extended with cyclonedds column, Cargo + CMake + Kconfig snippets. `docs/reference/cyclonedds-interop.md` covers pin (0.10.5 / Humble), DDS topic naming (raw `rt/`/`rq/`/`rr/`, no key-rewrite), type identification path (Phase 117.2 codegen pending), discovery config, QoS mapping table, Phase 108 status events deferral. `CLAUDE.md` Phases table updated with Phase 117 entry summarising what's done and what's stubbed.
  - **Files:** `book/src/user-guide/rmw-backends.md`, `docs/reference/cyclonedds-interop.md`, `CLAUDE.md`.

- [x] **117.16 — `just cyclonedds` recipes.** `setup` / `doctor` / `build` / `clean` / `build-rmw` / `test` / `ci` all wired up. `build-rmw` cmake-configures + builds + installs `nros-rmw-cyclonedds`. `test` runs CTest. Top-level `just ci` now depends on a new `cyclonedds-ci` wrapper that delegates to `cyclonedds::ci` when the Cyclone submodule is initialised, otherwise prints `Cyclone DDS skip: submodule not initialised` and exits 0. Contributors not touching Phase 117 don't need to clone the submodule; CI hosts that do gain coverage of all 12 ctest entries (entity smoke + service round-trip + Phase 117.12 stock-RMW E2E + 117.X.4 mangling test).
  - **Files:** `justfile` (`ci` recipe + new `cyclonedds-ci` wrapper), `just/cyclonedds.just` (`ci` doc-comment refresh).

---

## Acceptance

- `just cyclonedds build` green: produces `build/install/lib/libnros_rmw_cyclonedds.a` + `build/install/lib/cmake/NrosRmwCyclonedds/nros-rmw-cyclonedds-config.cmake`.
- `find_package(NrosRmwCyclonedds REQUIRED)` works from a downstream CMake project pointed at `build/install/`.
- `just check` green (fmt + clippy unaffected — backend is outside Cargo workspace).
- **117.X.1–117.X.5 land in tree** (codegen, prefix conventions, header-based service correlation, type-name verification, service QoS alignment).
- **POSIX E2E (117.12) gates Phase 117 closure:**
  - 117.12.A pub/sub: nano-ros pub ↔ `ros2 topic echo` and `ros2 topic pub` ↔ nano-ros sub both byte-equal on `std_msgs/msg/String`.
  - 117.12.B services: nano-ros server ↔ `ros2 service call`, and stock server ↔ nano-ros client both round-trip an `example_interfaces/srv/AddTwoInts` call.
- Both new boards build via `just zephyr build` (build-only acceptance for v1) with `NROS_CPP_RMW=cyclonedds`.
- `nros::Node::declare_parameter<double>("x", 0.15)` + `get_parameter<double>("x")` roundtrip unit test passes.
- `third-party/dds/cyclonedds/` submodule pinned at `tag 0.10.5`; `direnv allow` resolves `CYCLONEDDS_DIR`.
- `cargo nano-ros generate-cyclonedds-types` (or its post-117.X.1 successor) regenerates `packages/codegen/interfaces/<pkg>/cyclonedds/` deterministically.

117.X.6 (XTypes type-info) is **not** required for acceptance — it gates only `ros2 topic info -v` introspection, not pub/sub or service interop.

---

## Sequencing

```
117.1 → 117.2 → 117.3 → 117.4 → 117.5 → 117.6 ─→ nano-ros↔nano-ros pub/sub (done)
                                       ↘
                                        117.7 → 117.7.B → nano-ros↔nano-ros services (done)

ROS 2 interop chain (post-117.7.B):
117.X.1 (rosidl_adapter codegen) ──┬→ 117.X.4 (mangling verify)
                                    ├→ 117.X.3 (header-based services)
                                    │      └→ 117.X.5 (service QoS) ─┐
117.X.2 (topic-prefix conventions) ─┴───────────────────────────────┴→ 117.12 (stock-RMW E2E)

117.X.6 (XTypes type-info) parallel anytime, optional, blocked on Cyclone build.
117.8  (NROS_CPP_RMW=cyclonedds CMake option)         — done
117.9  (nros-cpp ParameterServer)                     — done
117.10 + 117.11 (Zephyr boards) — parallel after 117.X.5; build-only gate
117.13 (Zephyr build smoke)                           — after 117.10 + 117.11
117.14 (zephyr-aemv8r-cyclonedds example)             — last; needs 117.13 + 117.9
117.15 + 117.16 (docs + just recipes)                 — last; track on each merge
```

## Effort

≈ 4 weeks total. Done so far ≈ 2 weeks (117.1–117.9, 117.7.B, 117.15, 117.16). Remaining ≈ 2 weeks split as:

- **117.X.1** — rosidl_adapter integration: 3d (largest single piece).
- **117.X.2** — topic prefix conventions: 0.5d.
- **117.X.3** — `cdds_request_header_t`-based services: 3d.
- **117.X.4** — type-name mangling verification: 1d.
- **117.X.5** — service QoS alignment: 0.5d.
- **117.X.6** — XTypes type-info regen: deferred / unknown (optional).
- **117.12** — stock-RMW E2E (gates closure): 2d.
- **117.10 + 117.11 + 117.13 + 117.14** — Zephyr boards + example: 3d (blocked on toolchain install).

---

## Notes / Risks

### R1 — Cyclone-on-Zephyr build
Cyclone CMake assumes POSIX threads/sockets. Zephyr `CONFIG_POSIX_API=y` covers most surfaces; edge cases possible (e.g. `pthread_attr_setstack`). **Mitigation:** Autoware tree already runs Cyclone on the same Zephyr boards — copy their build flags verbatim from `actuation_module/CMakeLists.txt`.

### R2 — Multicast SPDP on Zephyr ethernet
Discovery needs IGMP. Zephyr eth stack supports it but board-specific. **Mitigation:** Cyclone supports unicast-only via `<Discovery><Peers>` (Autoware uses `cfg_peer` static peer list — same path).

### R3 — Type descriptor codegen pipeline
`idlc` is C; needs to compile + invoke from build pipeline. **Mitigation:** in-tree at `cyclonedds/src/tools/idlc/`; build alongside Cyclone in 117.1. Validate generated `.c` compiles in `no_std` consumer crates (no libc deps).

### R4 — R52 has no MMU
Cyclone uses dynamic alloc for entities, internal queues. Sized heap config + monitor for OOM in long-running E2E. **Mitigation:** Autoware tree already runs on R52 — heap budget known. Document required `CONFIG_HEAP_MEM_POOL_SIZE` floor in board crate.

### R5 — ARMv8-R SMP and `Send`/`Sync`
Cyclone spawns its own RX threads; `nros-rmw` shim must be `Send + Sync` across SMP cores. Already a constraint for zenoh backend — reuse `Mutex<>` patterns. Listener trampoline writes to `Activator` ready-set under atomic.

### R6 — Stock ROS 2 wire compat (full path)
Cyclone uses raw DDS topic names — different from `rmw_zenoh`'s `<domain>/<topic>/<type>/TypeHashNotSupported` encoding. **Cyclone backend targets full wire-compat with stock `rmw_cyclonedds_cpp`** — pub/sub, services both. Achieving it requires four upstream-shape conventions:
- topic-name prefixes (`rt/` / `rq/` / `rr/`) — 117.X.2
- type-name mangling (`<pkg>::msg::dds_::<Type>_`) — 117.X.1 / 117.X.4
- in-payload `cdds_request_header_t` for services — 117.X.3
- `rmw_qos_profile_services_default` (reliable + transient_local + keep_last(10)) — 117.X.5

The interim `ServiceEnvelope` approach from 117.7.B is **not** wire-compatible; 117.X.3 replaces it. Validation gates the entire chain on 117.12 E2E. No cross-RMW bridging in scope (see Phase 104).

### R8 — `rosidl_adapter` integration choice
117.X.1 has two viable implementation paths (shell-out to ROS 2's Python rosidl_adapter, vs in-tree Rust mapping). Shell-out is faster to land but adds a host ROS 2 install requirement at codegen time; in-tree is more portable but ~3× the implementation effort. **Mitigation:** start with shell-out; gate the Rust port on a separate phase if the install dependency becomes a problem.

### R9 — XTypes type-info segfault
Cyclone 0.10.5 idlc emits truncated output when type-info section is requested (the issue that forced `-t` in 117.2). Blocks 117.X.6. **Mitigation:** keep 117.X.6 deferred; revisit when bumping the Cyclone pin past 0.10.5 or carrying a patch.

### R7 — Phase 108 events
Cyclone supports liveliness / deadline / message-lost events natively (`dds_set_listener` + status callbacks). v1 leaves `register_*_event` as NULL (`UNSUPPORTED`) per Phase 108 backend-wiring sub-phase plan. Wiring in follow-up phase reusing existing Cyclone listener infrastructure.

---

## Follow-ups (post-117)

- **Phase 11X — `autoware-safety-island` migration.** Port `actuation_module/src/autoware/autoware_trajectory_follower_node` from raw Cyclone to `nros-cpp` over `rmw-cyclonedds`. Validate trajectory-follower control loop on FVP. Coordinate with Arm Limited.
- **Phase 11Y — Cyclone Phase 108 event wiring.** Liveliness / deadline / message-lost via `dds_set_listener`.
- **Phase 11Z — Cyclone zero-copy `sertype`.** Bypass CDR copy in `try_recv_raw` via custom `ddsi_sertype` exposing borrowed serdata. Depends on Phase 99 zero-copy raw API.

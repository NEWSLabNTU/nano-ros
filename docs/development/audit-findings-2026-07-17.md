# Audit findings — 2026-07-17 (deep)

- Depth: **deep** (first run of the baked lane/model/effort economy)
- Categories: all A–J via 6 surface-bundled lanes (cmake·sonnet/low,
  core-code·sonnet/medium, API-shape·inherit/high, testing·sonnet/low,
  docs-UX·haiku/low, examples·sonnet/medium)
- Target: pristine worktree of origin/main @ `0d4a9b139`
- Baseline: audit-findings-2026-07-16.md (quick)
- Spend: 6 finder agents, 519,801 subagent tokens, 154 tool calls, ~9 min
- Verification: tiered single-refuter — 0 P1s and 0 low-confidence P2s were
  produced, so the refuter stage had no work; all P2s below are
  finder-confirmed (read, not grepped) but not adversarially refuted.

## P2 (13) — filed as issues #219–#228

- config-collision · `cmake/nano_ros_workspace_metadata.cmake:24` · P2 · nano_ros_workspace_metadata() resolves the `nros` CLI with find_program(... HINTS ENV NROS_CLI ENV NROS_HOME PATHS "$ENV{HOME}/.nros/bin"), searching NROS_HOME as a HINT (before PATH) — the exact ordering that zephyr/cmake/nros_system_generate.cmake's own comment (lines ~36-41) explicitly calls out as wrong ("PATHS, not HINTS: hints are searched BEFORE the environment PATH, so a stale provisioned ~/.nros/bin/nros would shadow the activate.sh-wired in-tree CLI"). It also passes the raw `ENV NROS_HOME` value as a hint directory rather than `$ENV{NROS_HOME}/bin` (inconsistent with every sibling resolver, which appends /bin).
  · fix: Route through the single shared resolver in cmake/NanoRosCodegenCore.cmake (the one already documented as "shared find/validate/cache" for the codegen tool) instead of a fourth bespoke find_program; if it must stay separate, switch HINTS to PATHS and append /bin to NROS_HOME to match the documented sweep-contract precedence (PATH first). → **issue #219**
- duplication · `cmake/NanoRosGenerateInterfaces.cmake:22` · P2 · The `nros` CLI binary is resolved by at least four independent, hand-written implementations with differing precedence/search paths: cmake/NanoRosCodegenCore.cmake's `_nros_resolve_codegen_tool` (the one comments elsewhere call the shared SSoT), cmake/NanoRosEntry.cmake's `_nros_entry_invoke_codegen` (its own NROS_CLI_BIN/_nros_path fallback), cmake/nano_ros_workspace_metadata.cmake's inline find_program(NROS_BIN...), and zephyr/cmake/nros_system_generate.cmake's `_nros_system_resolve_cli`. Each has a subtly different precedence order (env var names, HINTS vs PATHS, whether the shared codegen-tool cache var is consulted first) despite a Phase-246.2b comment claiming "Shared find/validate/cache lives in the core".
  · fix: Consolidate all four call sites onto the one shared resolver function (extend NanoRosCodegenCore.cmake's helper to also serve the plain-nros-CLI case, not just the codegen-tool case) so there is exactly one documented precedence order for locating `nros`. → **issue #219**
- bootstrap-doc-drift · `book/src/getting-started/esp32.md:26` · P2 · Board ID mismatch: doc references `nros setup esp32` but correct ID is `qemu-esp32-baremetal`
  · fix: Replace 'esp32' with 'qemu-esp32-baremetal' in lines 9, 20, 26, 90; issue #209 archived as resolved but drift persists in book → **issue #220**
- incomplete-reference · `book/src/reference/cli.md:35` · P2 · CLI reference missing documentation for 4 verbs and 3 subcommands present in the actual CLI
  · fix: Add missing verb entries to cli.md Verbs section; verify against packages/cli/nros-cli-core/src/cmd/mod.rs#40-121 enum Cmd → **issue #220**
- copy-out-cleanliness · `examples/stm32f4/rust/action-server-rtic/README.md:1` · P2 · Copy-out example builds and flashes per the README with no warning, but the board init_hardware() is todo!() so it panics immediately on real hardware.
  · fix: Add a status caveat to the README (and sibling action-client-rtic README) noting the skeleton/panic status already documented in main.rs, or exclude from the example index until implemented. → **issue #221**
- fixture-staleness-gap · `packages/testing/nros-tests/src/fixtures/binaries/freertos.rs:85` · P2 · FreeRTOS, NuttX, and ThreadX (both linux and riscv64) fixture resolvers use the bare require_prebuilt_binary (existence-only) instead of require_prebuilt_binary_fresh, so a museum binary silently passes even when its sources changed.
  · fix: Across freertos.rs:85,162; nuttx.rs:125,205,244; threadx_linux.rs:126,169; threadx_riscv64.rs:78,156, swap to require_prebuilt_binary_fresh (or a cross-compile analog mirroring require_prebuilt_binary_fresh_cmake/_fresh_zephyr). Today only the Zephyr family (12 call sites in mod.rs) and native/cmake fixtures get the staleness check; FreeRTOS/NuttX/ThreadX have zero, matching the 'fixture mtime treadmill' museum-binary trap. → **issue #222**
- no-compilation-in-tests · `packages/testing/nros-tests/tests/zpico_drift_gate.rs:66` · P2 · zpico_drift_gate_fires_on_corrupted_include runs a real cargo build -p zpico-sys twice per test invocation (corrupted sandbox + pristine round-trip) with no explicit doc-comment claiming the documented 'exception to no compilation inside tests' the way cmake_node_register_misuse.rs does.
  · fix: Either add the same explicit exception rationale used in cmake_node_register_misuse.rs, or narrow the test to only exercise build.rs's failure path instead of letting cargo continue into a full crate compile for the pristine round-trip. → **issue #222**
- silent-fallback · `packages/core/nros-node/src/executor/handles.rs:2994` · P2 · parse_goal_accepted/parse_cancel_response/parse_result_response swallow CDR field-read errors via unwrap_or(...), turning a malformed/truncated wire response into a plausible business value instead of an error
  · fix: Propagate the CdrReader::read_u8/read_i8 Result with `.map_err(|_| NodeError::ServiceRequestFailed)?` instead of `.unwrap_or(default)`, so a truncated/corrupt action-result or cancel-response frame surfaces as an error rather than silently reporting 'goal rejected' (false) / CancelResponse::default() / GoalStatus::default(). → **issue #223/#224**
- ssot-duplication · `packages/core/nros-node/src/executor/handles.rs:2098` · P2 · const PROBE_TIMEOUT_MS: u32 = 1000 is independently defined 4 times (handles.rs:2098, handles.rs:2711, nros-c/src/service.rs:1593, nros-c/src/action/client.rs:353) with no shared constant, so the probe cadence can silently drift between Rust and C call sites
  · fix: Hoist to one pub(crate) const in nros-node (e.g. crate::config::PROBE_TIMEOUT_MS) and have nros-c reference it through the FFI layer, or centralize in nros-rmw if it's meant to be backend-visible. → **issue #223/#224**
- axis-agnosticism-naming · `packages/core/nros-node/src/cyclonedds_register.rs:1` · P2 · The generic type-descriptor registration seam (register_type::<M>, MessageForRmw) is correctly backend-agnostic in behavior (forwards through nros_rmw::register_type_descriptor, no direct dep on nros-rmw-cyclonedds), but the module is named cyclonedds_register and every agnostic call site (node.rs, spin.rs, action.rs) writes crate::cyclonedds_register::MessageForRmw/register_type, baking a single backend's name into files that must stay RMW-agnostic
  · fix: Rename the module/path to something backend-neutral (e.g. type_registry / descriptor_seam) so node.rs/spin.rs/action.rs read as generic, matching what the module's own doc comment already claims post-issue-#60 → **issue #225**
- thin-wrapper-violation · `packages/core/nros-cpp/include/nros/parameter.hpp:459` · P2 · NEW C1 instance: ParameterServer<Cap,SeqSlots,SeqPoolBytes> re-implements an entire parameter storage engine in the C++ header (SeqRecord table, bump-pool allocator, name lookup/copy/compare) for sequence parameters, parallel to the Rust-owned nros_param_server_t; sequence params never cross the FFI, so raw() (line 380: 'Sequence parameters are C++-local and not reflected here'), the param-services feature, and ros2 param cannot see them — rclcpp's counterpart declare_parameter<std::vector<double>> is fully service-visible via rcl_interfaces.
  · fix: Extend the C/Rust param FFI with owned-copy array declare/get/set (server-owned element pool, mirroring what this header built locally), then reduce the C++ Seq overloads to thin forwarders; until then add a loud doc warning on has_parameter/parameter_count that sequence entries are invisible to the service surface. → **issue #226**
- api-shape · `packages/core/nros-cpp/include/nros/node.hpp:736` · P2 · init(locator, domain_id, session_name) uses domain_id==0 as the 'unset' sentinel ('if (domain_id == 0) domain_id = NROS_ENTRY_DOMAIN_ID'), so an image with a baked nonzero NROS_ENTRY_DOMAIN_ID can never be explicitly run on domain 0 (a valid, default ROS domain) — contradicting the in-file precedence comment 'explicit arg > baked macro' (line 729); upstream rcl avoids this with the distinct RCL_DEFAULT_DOMAIN_ID (SIZE_MAX) sentinel, and this codebase already uses the correct pattern one class down (NodeBuilder's NROS_CPP_DOMAIN_ID_INHERIT).
  · fix: Introduce a NROS_CPP_DOMAIN_ID_UNSET sentinel (e.g. 0xFF, outside the 0-232 range) as the default argument of both init overloads and only apply the baked macro/env rungs when domain_id == sentinel; keep 0 as a real explicit value. → **issue #227**
- codegen-template-drift · `packages/cli/rosidl-codegen/templates/service_c.h.jinja:47` · P2 · The C emitter uses two incompatible return conventions for the same concept: message _serialize returns 0/-1 with a size_t* serialized_size out-param (message_c.h.jinja:81), while service Request/Response _serialize returns the byte count directly with no out-param (confirmed in service_c.c.jinja) — code or macros written against one convention silently misinterpret the other (a successful 12-byte service serialize fails an 'if (ret != 0)' check written for the message convention).
  · fix: Unify on the message convention (0/-1 + out-param, which also matches rmw byte-count vs status-code separation elsewhere): change service_c.{h,c}.jinja signatures, regenerate, and fix in-tree callers; if ABI-frozen, at minimum rename to _serialize_len to make the convention difference visible. → **issue #228**

## P3 (12) — report-only

- duplication · `zephyr/cmake/nros_cargo_build.cmake:177` · P3 · The "resolve $ENV{NROS_CARGO_PROFILE} → profile-dir" 8-line if/elseif block is copy-pasted verbatim across zephyr/cmake/nros_cargo_build.cmake:177-186 and zephyr/cmake/nros_generate_interfaces.cmake:371-380, and a near-twin (cache-var flavored) also lives in cmake/NanoRosGenerateInterfaces.cmake:63-70 — three places that must be kept in sync by hand whenever a new cargo profile alias (e.g. `dev`/`release` name mapping) is added.
  · fix: Hoist into a single `nros_resolve_cargo_profile_dir(out_var)` helper in NanoRosCodegenCore.cmake (which is already include()'d by both cmake/NanoRosGenerateInterfaces.cmake and the Zephyr sibling per its own header comment) and call it from all three sites.
- duplication · `examples/workspaces/ws-realtime-rust/src/riscv_nuttx_entry/src/main.rs:1` · P3 · nuttx_entry and riscv_nuttx_entry main.rs doc comments are near-verbatim duplicates of the tiering/bringup narrative, differing only in board name and phase number, with no shared source to keep them in sync.
  · fix: Factor the common NuttX-tiers-bringup narrative into a single shared doc/README section both files reference, keeping only the board-specific delta inline.
- coverage-doc-drift · `examples/README.md:107` · P3 · The carve-out table explains why plain pure-cargo native/threadx-linux rust cyclonedds talker/listener fixtures don't exist, but fixtures.toml has rmw=cyclonedds + lang=rust entries for the same platform (bridge/egress fixtures) that a grep-only reader would mistake for coverage of the matrix's '(pending 171.C.1)' cell.
  · fix: Add a one-line comment in fixtures.toml next to the bridge cyclonedds entries noting they satisfy the bridge-egress path only and don't count toward the standalone native-rust-cyclonedds cell.
- stale-ignore · `packages/testing/nros-tests/tests/cli_bringup_px4.rs:35` · P3 · px4_sitl_2_component_module_builds has been #[ignore]-d since Phase 212.M.10 citing a 2026-06-02 redesign and an unresolved PX4 SITL board-overlay gap, with no linked open issue tracking the follow-up as of 2026-07-17.
  · fix: File or link a docs/issues/NNNN-*.md tracking the codegen-system --ahead-of-vendor --target px4 follow-up instead of only a phase-number comment.
- missing-safety-comment · `packages/core/nros-c/src/support.rs:258` · P3 · core::str::from_utf8_unchecked on support.locator bytes has no local // SAFETY comment stating why the bytes are guaranteed valid UTF-8 (they are, because they were copied from a &str a few lines earlier in the same function, but that invariant isn't documented at the unsafe call site)
  · fix: Add a // SAFETY: comment at the call site noting the bytes were written from `resolved.locator.as_bytes()` (a &str) a few lines above, so future edits that repopulate support.locator from raw FFI input don't reintroduce UB silently.
- api-shape · `packages/core/nros-core/src/types.rs:59` · P3 · Rust RosService names its response associated type `Reply` (type Reply: RosMessage) while the standard counterpart rclrs/rosidl_runtime_rs::Service uses `Response`, the nano-ros C++ Service<S> requires a nested `S::Response`, and even the nano-ros codegen emits `type Reply = {{service_name}}Response` — a rename of a standard concept plus cross-language drift for the same concept (users write S::Response in C++ but S::Reply in Rust).
  · fix: Rename the associated type to Response (or add `type Response` with a deprecated `Reply` alias via a defaulted associated-type transition), and update the service_nros.rs.jinja emitter + trait consumers in one sweep.
- api-shape · `packages/core/nros/src/lib.rs:637` · P3 · Cross-language naming drift for the pub/sub entity concept: `nros::Publisher`/`nros::Subscriber` in Rust are the low-level nros-rmw transport traits (re-exported at line 463), while the user-facing node-level entities are `EmbeddedPublisher`/`Subscription`; in C++ `nros::Publisher<M>` IS the user-facing entity (matching rclcpp::Publisher), so the same public name denotes different-layer concepts across the two languages and rclrs's `Publisher` maps to nano-ros Rust's `EmbeddedPublisher`.
  · fix: Re-export the rmw traits under a namespaced path (nros::rmw::{Publisher, Subscriber}) and give the user-facing entity the standard name (Publisher) at the crate root, with deprecated aliases for EmbeddedPublisher during migration.
- dead-code · `packages/core/nros-cpp/include/nros/node.hpp:50` · P3 · The `#ifdef NROS_RMW_CYCLONEDDS/XRCE/ZENOH_CFFI/UORB` extern "C" register-function declarations (lines 50-61) are dead — never called anywhere in nros-cpp include/ or src/ — and directly contradict the Phase 128.C.1 comment in the same file (line ~753: 'No #ifdef NROS_RMW_* chain here') describing the RMW_INIT_ENTRIES section-walker that replaced them.
  · fix: Delete the four #ifdef extern declaration blocks; the linker-section walker in nros_cpp_init is the sole registration path. → **issue #227**
- rmw-parity · `packages/core/nros-rmw-cffi/src/lib.rs:537` · P3 · The backend vtable inverts upstream rmw's service-client model: the REQUIRED slot `call_raw` is a blocking one-shot request+reply round-trip, while the non-blocking split matching rmw.h's rmw_send_request/rmw_take_response (`send_request_raw`/`try_recv_reply_raw`, Phase 130.4) is OPTIONAL — and book/src/design/rmw-vs-upstream.md, which documents every other deliberate divergence (drive_io vs rmw_wait, no graph cache, event tiers), never mentions this blocking-vs-non-blocking inversion (call_raw appears only in its return-code table, line 700).
  · fix: Add a 'Services: blocking call_raw vs upstream send/take split' section to rmw-vs-upstream.md stating the rationale and what a backend that omits the optional split loses (async client dispatch), or flip the required/optional roles at the next vtable ABI bump.
- api-shape · `packages/core/nros-c/include/nros/nros_generated.h:5851` · P3 · domain_id type drifts across surfaces for the same concept: uint8_t in the C/C++ init FFI (nros_support_init, nros::init) vs u32 in the Rust ExecutorConfig/Context, the rmw vtable open(), TopicInfo, and the node-level domain_id_override (nros_generated.h:1181) — upstream rcl/rmw uses size_t throughout; the u8 rung silently truncates any future >255 override path and forces casts at every language boundary.
  · fix: Standardize the FFI init parameters on uint32_t (0-232 range-checked at the Rust resolver, which already errors on >232 per RFC-0045), keeping u8 only where struct packing genuinely matters.
- codegen-template-drift · `packages/cli/rosidl-codegen/templates/message_cpp.hpp.jinja:96` · P3 · C++ message constants are emitted at namespace scope as `{{message_name}}_{{constant.name}}` (e.g. pkg::msg::Msg_FOO), while the standard counterpart rosidl_generator_cpp emits them as static members of the message struct (pkg::msg::Msg::FOO) — rclcpp code being ported that references Msg::CONSTANT fails to compile, and the C emitter's #define {{constant_prefix}}_{{name}} (message_c.h.jinja:28) means the same constant has three unrelated spellings across the three emitters.
  · fix: Move the constexpr constants inside the generated struct (static constexpr members), matching rosidl_generator_cpp; keep the namespace-scope names as deprecated aliases for one release.
- api-shape · `packages/core/nros-cpp/include/nros/node.hpp:508` · P3 · Callback-group reshape vs rclcpp: create_callback_group(const char* name) takes a free-form static string mapped to a SchedContext (RFC-0047) where rclcpp::Node::create_callback_group takes a typed CallbackGroupType (MutuallyExclusive/Reentrant); and group association uses new create_timer_in/create_subscription_in/create_publisher_in method variants instead of rclcpp's SubscriptionOptions::callback_group field — even though nano-ros already has an rclcpp-shaped SubscriptionOptions struct (options.hpp) that would be the standard home for the group.
  · fix: Add a `CallbackGroup group` (or group-name) field to SubscriptionOptions/ServiceOptions and route the existing create_subscription overloads through it, keeping the _in variants as thin conveniences; document the string-name vs CallbackGroupType semantic mapping in RFC-0047 and rclcpp_compat.hpp. → **issue #227**

## Classification vs baseline (2026-07-16 quick)

- carried-over: the C1 lifecycle-autostart P3 class (now joined by the
  parameter.hpp seq-engine P2 — same thin-wrapper rule, bigger instance);
  the esp32 phantom board id (one page survived #209's fix → #220).
- resolved-since-last: everything the 07-16 run filed (#205–#212 all closed
  by the day's sessions); the setup.bash rot; the CLI CLAUDE.md.
- new: everything else above — notably the first C5/C6/C7/E5 findings from
  the six maintainer checks added 2026-07-17.

## Coverage

Per-lane coverage notes (sampled vs skipped) are recorded in the workflow
journal (wf_a4d5ab17-b4d). Known gaps this run: full A1 byte-diff of the
cmake/zephyr generator twins; the 420-fn unsafe surface beyond the sampled
hot files; nros-cpp src/ internals; E5 matrix cross-read produced
carve-out-doc drift notes (see #222's sibling P3) rather than empty-cell
findings — a dedicated matrix pass is worth a targeted `/audit deep E`
after the fixture-freshness fix lands.

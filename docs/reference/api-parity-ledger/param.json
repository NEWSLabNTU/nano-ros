{
  "_doc": [
    "Schema: see docs/reference/api-parity-ledger/SCHEMA.md — the one home for what",
    "a row means. Do NOT copy it back here; issue 1095 is what that cost."
  ],
  "c:add_parameter": {
    "verdict": "declined",
    "disposition": "absent",
    "provides": [
      "nros_parameter_declare_bool",
      "nros_parameter_declare_integer",
      "nros_parameter_declare_double",
      "nros_parameter_declare_string",
      "nros_parameter_declare_bool_array",
      "nros_parameter_declare_integer_array",
      "nros_parameter_declare_double_array",
      "nros_parameter_declare_string_array",
      "nros_parameter_declare_byte_array"
    ],
    "why": "RE-VERDICTED 2026-08-26 (phase-379 W5), was `rename`. rclc declares a parameter with `rclc_add_parameter(server, name, TYPE)` and gives it a value in a second `rclc_parameter_set_*` call. Ours is `nros_parameter_declare_<type>(server, name, DEFAULT)` -- one call, which is rclcpp's and rclrs's shape, so the FOLD is deliberate. The two are not one call under two names: rclc's third argument is a TYPE (`rclc_parameter_type_t`, confirmed in the recorded rclc surface), ours is a VALUE. So no target name exists for this row to carry -- which is exactly why it sat blocked, and a row that endorses our shape and can name no destination is a DECLINE, not a rename. What a user loses: the two-step idiom; a ported rclc node rewrites `rclc_add_parameter(s, \"x\", RCLC_PARAMETER_INT)` + `rclc_parameter_set_int(s, \"x\", 5)` as one `nros_parameter_declare_integer(s, \"x\", 5)`. The reader's other complaint -- that nothing here is spelled `add` -- is really the `param` vs `parameter` PREFIX question, and that is a DIFFERENT row: `c:parameter_server_t` owns it. Ours half: the `c:parameter_declare_*` family.",
    "rename": {
      "status": "resolved",
      "resolution": "re-verdicted `declined` -- the declare/set fold means rclc's call and ours take different arguments, so no rename target exists; the prefix question is c:parameter_server_t's, not this row's"
    }
  },
  "c:add_parameter_constraint_double": {
    "verdict": "divergence",
    "disposition": "absent",
    "provides": [
      "nros_executor_add_param_constraint_double",
      "nros_executor_add_param_constraint_double_on",
      "nros_executor_get_param_double_range",
      "nros_executor_get_param_double_range_on"
    ],
    "why": "rclc attaches a `[from, to, step]` range to a double parameter and REJECTS sets outside it; ours does both, through `ParameterServer::apply` — the same predicate a remote `ros2 param set` meets — and additionally refuses a range the parameter's CURRENT value does not satisfy, which would advertise a bound no set could produce. RE-VERDICTED phase-417 W4.a. was `gap`, and the capability now SHIPS — under our spelling, not rclc's. Ours is `nros_executor_*_param_*` because the store is the EXECUTOR's and keyed by NODE (phase-426 W1), where rclc's verbs take an `rclc_parameter_server_t*`: an image composes several nodes onto one executor, and a C caller holds the `nros_node_t` it passed to `nros_executor_node_init`, not a per-node server object. Deliberately NOT added to the legacy `nros_parameter_server_t` family, which is disjoint from the store the six `rcl_interfaces` servers read (issue 0793) — a descriptor surface there would be a second answer to 'what is this parameter's range'. Whether that family is retired or re-pointed is phase-417 W2.a's decision and nothing here depends on it."
  },
  "c:add_parameter_constraint_integer": {
    "verdict": "divergence",
    "disposition": "absent",
    "provides": [
      "nros_executor_add_param_constraint_integer",
      "nros_executor_add_param_constraint_integer_on",
      "nros_executor_get_param_integer_range",
      "nros_executor_get_param_integer_range_on"
    ],
    "why": "the integer form; see `c:add_parameter_constraint_double`. RE-VERDICTED phase-417 W4.a. was `gap`, and the capability now SHIPS — under our spelling, not rclc's. Ours is `nros_executor_*_param_*` because the store is the EXECUTOR's and keyed by NODE (phase-426 W1), where rclc's verbs take an `rclc_parameter_server_t*`: an image composes several nodes onto one executor, and a C caller holds the `nros_node_t` it passed to `nros_executor_node_init`, not a per-node server object. Deliberately NOT added to the legacy `nros_parameter_server_t` family, which is disjoint from the store the six `rcl_interfaces` servers read (issue 0793) — a descriptor surface there would be a second answer to 'what is this parameter's range'. Whether that family is retired or re-pointed is phase-417 W2.a's decision and nothing here depends on it."
  },
  "c:add_parameter_description": {
    "verdict": "divergence",
    "disposition": "absent",
    "provides": [
      "nros_executor_add_param_description",
      "nros_executor_add_param_description_on",
      "nros_executor_describe_param",
      "nros_executor_describe_param_on"
    ],
    "why": "rclc attaches a description and an additional-constraints string to a declared parameter; ours are `nros_executor_add_param_description[_on]`, and `nros_executor_describe_param[_on]` reads them back. Both texts live in the descriptor `~/describe_parameters` serves, so `ros2 param describe` prints what a C caller attached instead of the empty descriptor it used to answer with for everything C declared. RE-VERDICTED phase-417 W4.a. was `gap`, and the capability now SHIPS — under our spelling, not rclc's. Ours is `nros_executor_*_param_*` because the store is the EXECUTOR's and keyed by NODE (phase-426 W1), where rclc's verbs take an `rclc_parameter_server_t*`: an image composes several nodes onto one executor, and a C caller holds the `nros_node_t` it passed to `nros_executor_node_init`, not a per-node server object. Deliberately NOT added to the legacy `nros_parameter_server_t` family, which is disjoint from the store the six `rcl_interfaces` servers read (issue 0793) — a descriptor surface there would be a second answer to 'what is this parameter's range'. Whether that family is retired or re-pointed is phase-417 W2.a's decision and nothing here depends on it."
  },
  "c:bool_array_t": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "a parsed `bool[]` sequence, heap-allocated by the YAML parser. `rcl_yaml_param_parser` exists to parse a `--params-file` off a FILESYSTEM at node start and hand rcl a tree of overrides. A device image has no filesystem and no YAML parser; parameter defaults are baked into the image at build time (RFC-0045) and changed afterwards over `~/set_parameters`. What a user loses is `--params-file` and everything that reads one."
  },
  "c:byte_array_t": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "a parsed `byte[]` sequence, heap-allocated by the YAML parser. `rcl_yaml_param_parser` exists to parse a `--params-file` off a FILESYSTEM at node start and hand rcl a tree of overrides. A device image has no filesystem and no YAML parser; parameter defaults are baked into the image at build time (RFC-0045) and changed afterwards over `~/set_parameters`. What a user loses is `--params-file` and everything that reads one."
  },
  "c:delete_parameter": {
    "verdict": "divergence",
    "disposition": "absent",
    "provides": [
      "nros_executor_delete_param",
      "nros_executor_delete_param_on"
    ],
    "why": "undeclare at runtime. The slot is freed for a later declaration, per node, which is what `ParameterServer::remove` does one language over. The row's own reasoning held: nothing about static storage forbade it. RE-VERDICTED phase-417 W4.a. was `gap`, and the capability now SHIPS — under our spelling, not rclc's. Ours is `nros_executor_*_param_*` because the store is the EXECUTOR's and keyed by NODE (phase-426 W1), where rclc's verbs take an `rclc_parameter_server_t*`: an image composes several nodes onto one executor, and a C caller holds the `nros_node_t` it passed to `nros_executor_node_init`, not a per-node server object. Deliberately NOT added to the legacy `nros_parameter_server_t` family, which is disjoint from the store the six `rcl_interfaces` servers read (issue 0793) — a descriptor surface there would be a second answer to 'what is this parameter's range'. Whether that family is retired or re-pointed is phase-417 W2.a's decision and nothing here depends on it."
  },
  "c:double_array_t": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "a parsed `double[]` sequence, heap-allocated by the YAML parser. `rcl_yaml_param_parser` exists to parse a `--params-file` off a FILESYSTEM at node start and hand rcl a tree of overrides. A device image has no filesystem and no YAML parser; parameter defaults are baked into the image at build time (RFC-0045) and changed afterwards over `~/set_parameters`. What a user loses is `--params-file` and everything that reads one."
  },
  "c:executor_add_param_*": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "Descriptor ATTACHMENT on a declared parameter — rclc's `rclc_add_parameter_description` and `rclc_add_parameter_constraint_{double,integer}`. phase-417 W4.a. OURS-ONLY because the STORE is the executor's and keyed by NODE (phase-426 W1), where rclc's corresponding verbs take an `rclc_parameter_server_t*`: an image composes several nodes onto one executor and a C caller holds the `nros_node_t` it passed to `nros_executor_node_init`. Every member carries the `_on` / primary pair the rest of the `nros_executor_*_param_*` family does, and every one forwards to `nros_params::ParameterServer` and keeps no state. The upstream half each answers is ledgered `divergence` on its own theirs-only row."
  },
  "c:executor_add_parameter_server": {
    "verdict": "divergence",
    "why": "rclc attaches a CALLER-OWNED `rclc_parameter_server_t` to the executor. Ours is `nros_executor_register_parameter_services(executor)`, which takes no server because the executor owns the only one there is -- static entity storage, no allocator to make a second. Ledgered as an extension in the service stage. Note which of our TWO C parameter stores this is. `nros_parameter_server_t` (`nros/parameter.h`) is a standalone store over caller-supplied storage and is purely node-local -- `parameter.rs` calls it \"the legacy `nros_parameter_server_t` API\" in its own comment. The store the six `rcl_interfaces/srv/*` servers read is a DIFFERENT object, the `nros_params::ParameterServer` the executor owns, reached only through `nros_executor_*_param_*`. Nothing joins them. What a user additionally loses is rclc's on-modification callback, which this call is where you pass: see `c:executor_add_parameter_server_with_context`."
  },
  "c:executor_add_parameter_server_with_context": {
    "verdict": "divergence",
    "disposition": "absent",
    "provides": [
      "nros_executor_register_parameter_services",
      "nros_executor_set_param_callback",
      "nros_executor_set_param_callback_on",
      "nros_executor_remove_param_callback"
    ],
    "why": "RE-VERDICTED phase-417 W4.a. was `gap`, and what this row said MATTERED is now closed. BOTH rclc forms take the on-modification callback and ours took none, so a `ros2 param set` reaching the executor-owned store notified nobody: the C callback that existed, `nros_parameter_server_set_callback`, is on the legacy standalone store, which is not the object the services write to, so it fired for nobody. `nros_executor_set_param_callback[_on]` registers the SAME `nros_parameter_callback_t` on the store `ros2 param set` reaches — one typedef, two stores, no third spelling — and it carries rclcpp's accept/reject contract: `false` refuses the write. It runs AFTER the store's read-only / type / range rules, so a callback never sees a write those already refused. Registration is bounded (`nros_params::MAX_ON_SET_CALLBACKS`, 4) and a full registry answers `NROS_RET_FULL` rather than evicting somebody else's hook; `nros_executor_remove_param_callback` takes the token back. The SHAPE still differs and that is what `absent` records: rclc's is one call that creates the server and the callback together over an `rclc_parameter_server_t*`, ours is `nros_executor_register_parameter_services` plus a registration on the executor, because the store is the executor's and keyed by node. RFC-0036 line 91 ('No parameter callbacks; parameters are read/write only') is now wrong in a third direction: C has one on BOTH stores, C++ has one, and Rust has one."
  },
  "c:executor_allow_undeclared_parameters": {
    "verdict": "divergence",
    "why": "the C spelling of `rust:Executor::allow_undeclared_parameters`, and it exists for the same reason the Rust one does: upstream's `allow_undeclared_parameters` is a NODE OPTION given at construction (rclcpp `NodeOptions`, rclrs `allow_undeclared`), and there is no `NodeOptions` object here to carry it -- a node is a row in the executor's fixed table, so the option is a call on the executor. The SEMANTICS are upstream's: per node, off by default (issue 1151). phase-426 W5 added it because C could not reach the policy AT ALL: `nros_executor_set_param_*` on an undeclared name was a permanent `NROS_RET_NOT_FOUND` with no opt-in, so a rule that Rust and a remote `ros2 param set` both honour was unreachable from the C API that shares the store. What a user loses: it cannot be given at node-construction time, so it must be set before `nros_executor_register_parameter_services` starts answering."
  },
  "c:executor_allow_undeclared_parameters_on": {
    "verdict": "divergence",
    "why": "the node-naming half of `c:executor_allow_undeclared_parameters`; see `c:executor_declare_param_*_on` for why the node is an argument here and a receiver upstream."
  },
  "c:executor_declare_param_*": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "the service-backed parameter API: it operates on the `nros_params::ParameterServer` the EXECUTOR owns, which is the store `nros_executor_register_parameter_services` exposes to `ros2 param`. rclc has no equivalent spelling because rclc hands the executor a caller-owned server instead (`c:executor_add_parameter_server`); with static entity storage and no allocator there is one server and the executor holds it, so the executor is the handle. Gated on the `param-services` Cargo feature, which needs `alloc`. SCALARS ONLY -- bool/integer/double/string -- where the local `c:parameter_declare_*` family covers all ten `rcl_interfaces` types, so no C caller can publish an array parameter to `ros2 param`. This spelling means the executor's PRIMARY node; `c:executor_declare_param_*_on` names another. Phase 379 W3; phase-426 W1/W5."
  },
  "c:executor_declare_param_*_on": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "phase-426 W5 -- the node-naming half of `c:executor_declare_param_*`, and the C mirror of `rust:Executor::declare_parameter_on`. The parameter STORE is one fixed table owned by the EXECUTOR, not a container per node: there is no allocator to make a second, so a node cannot own its own. Upstream keys parameters by making the node the RECEIVER (`rclcpp::Node::declare_parameter`, rclc's `rclc_parameter_server_t` bound to a node); ours are on the executor and the node is an ARGUMENT, because an image composes several nodes onto one executor (RFC-0047) and the shared table has to be told whose parameter it is. The `_on` suffix is the same disambiguation the entity builders already use (`node_mut(id)` / `_on(...)`, phase 104.C) and the same pair Rust ships, so C and Rust name one relationship one way. It is a NEW NAME rather than a widened signature deliberately: RFC-0089 permits a signature change only where the compiler forces the edit, and the existing spellings are not wrong -- they mean the primary node and will go on meaning it -- so re-signing them would cost every C caller an edit that buys nothing. What a user loses: a ported node writes `nros_executor_declare_param_integer_on(&exec, &node, name, value)` where rclcpp writes `node->declare_parameter(name, value)`. Takes the `nros_node_t` rather than its slot index, so a node that is uninitialised or bound to another executor is refused instead of silently meaning slot 0."
  },
  "c:executor_delete_param*": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "Undeclare — rclc's `rclc_delete_parameter`, rclcpp's `undeclare_parameter`. phase-417 W4.a. OURS-ONLY because the STORE is the executor's and keyed by NODE (phase-426 W1), where rclc's corresponding verbs take an `rclc_parameter_server_t*`: an image composes several nodes onto one executor and a C caller holds the `nros_node_t` it passed to `nros_executor_node_init`. Every member carries the `_on` / primary pair the rest of the `nros_executor_*_param_*` family does, and every one forwards to `nros_params::ParameterServer` and keeps no state. The upstream half each answers is ledgered `divergence` on its own theirs-only row."
  },
  "c:executor_describe_param*": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "Descriptor READBACK — rclcpp's `describe_parameter`. Every out-param is NULLable and the two texts land in caller-owned buffers of stated capacity, TRUNCATING with `NROS_RET_FULL` rather than refusing: a prefix of a description is still usable where a shortened VALUE would be a wrong answer. There is no descriptor STRUCT on this boundary, deliberately — one would carry either a pointer into the store or an inline buffer, and the second would put `NROS_MAX_PARAM_DESCRIPTION_LEN` into a public ABI. phase-417 W4.a. OURS-ONLY because the STORE is the executor's and keyed by NODE (phase-426 W1), where rclc's corresponding verbs take an `rclc_parameter_server_t*`: an image composes several nodes onto one executor and a C caller holds the `nros_node_t` it passed to `nros_executor_node_init`. Every member carries the `_on` / primary pair the rest of the `nros_executor_*_param_*` family does, and every one forwards to `nros_params::ParameterServer` and keeps no state. The upstream half each answers is ledgered `divergence` on its own theirs-only row."
  },
  "c:executor_get_param_*": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "the read half; see `c:executor_declare_param_*`."
  },
  "c:executor_get_param_*_on": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "the read half of the per-node family; see `c:executor_declare_param_*_on`."
  },
  "c:executor_has_param_on": {
    "verdict": "divergence",
    "why": "the existence predicate of the per-node family; see `c:executor_declare_param_*_on`. Upstream's correspondent is `rclcpp::Node::has_parameter`, whose receiver IS the node. A node this executor does not own answers `false`, never the primary node's answer -- defaulting the node is how a sibling's parameter reads as this one's."
  },
  "c:executor_list_params*": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "Prefix-filtered name listing — rclcpp's `list_parameters`, `~/list_parameters`'s local form. Names land in a caller-owned RECTANGLE (`max_names` rows of `name_stride`) and `*out_count` always reports the TOTAL that matched, so `max_names = 0` counts: the only way a list of strings crosses this boundary with no allocator. phase-417 W4.a. OURS-ONLY because the STORE is the executor's and keyed by NODE (phase-426 W1), where rclc's corresponding verbs take an `rclc_parameter_server_t*`: an image composes several nodes onto one executor and a C caller holds the `nros_node_t` it passed to `nros_executor_node_init`. Every member carries the `_on` / primary pair the rest of the `nros_executor_*_param_*` family does, and every one forwards to `nros_params::ParameterServer` and keeps no state. The upstream half each answers is ledgered `divergence` on its own theirs-only row."
  },
  "c:executor_remove_param_callback": {
    "verdict": "divergence",
    "why": "Unregister an on-set hook — rclcpp's `remove_on_set_parameters_callback`. Takes the `uint16_t` token the registration returned, because with no allocator there is no owning handle whose destruction could unregister. See `c:executor_add_parameter_server_with_context` for the registration half. phase-417 W4.a. OURS-ONLY because the STORE is the executor's and keyed by NODE (phase-426 W1), where rclc's corresponding verbs take an `rclc_parameter_server_t*`: an image composes several nodes onto one executor and a C caller holds the `nros_node_t` it passed to `nros_executor_node_init`. Every member carries the `_on` / primary pair the rest of the `nros_executor_*_param_*` family does, and every one forwards to `nros_params::ParameterServer` and keeps no state. The upstream half each answers is ledgered `divergence` on its own theirs-only row."
  },
  "c:executor_set_param_*": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "the write half; see `c:executor_declare_param_*`."
  },
  "c:executor_set_param_*_on": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "the write half of the per-node family; see `c:executor_declare_param_*_on`. Routes through `Executor::set_parameter_on`, hence `ParameterServer::apply`, so a C set gets exactly the read-only / type / range / undeclared rules a remote `ros2 param set` gets (phase-426 W2)."
  },
  "c:int64_array_t": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "a parsed `int64[]` sequence, heap-allocated by the YAML parser. `rcl_yaml_param_parser` exists to parse a `--params-file` off a FILESYSTEM at node start and hand rcl a tree of overrides. A device image has no filesystem and no YAML parser; parameter defaults are baked into the image at build time (RFC-0045) and changed afterwards over `~/set_parameters`. What a user loses is `--params-file` and everything that reads one."
  },
  "c:node_params_t": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "one node's slice of a parsed parameter file. `rcl_yaml_param_parser` exists to parse a `--params-file` off a FILESYSTEM at node start and hand rcl a tree of overrides. A device image has no filesystem and no YAML parser; parameter defaults are baked into the image at build time (RFC-0045) and changed afterwards over `~/set_parameters`. What a user loses is `--params-file` and everything that reads one."
  },
  "c:param_array_t": {
    "verdict": "rename",
    "why": "the deprecated `nros_param_*` spelling of `c:parameter_array_t`. DEPRECATED ALIAS, phase-379 W5 (2026-08-27). The live spelling is `nros_parameter_array_t`; this one survived one release as a plain `typedef` in `nros/parameter.h`; phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05) retired all four typedef aliases with the forwarders, so the old type name is an error now. It compiles SILENTLY -- C has no portable way to attach a deprecation attribute to a typedef (MSVC rejects `__declspec(deprecated)` there and `[[deprecated]]` on one is C23), so unlike the function forwarders this alias is a grace period rather than a warning, and this row is where the deprecation is actually recorded. It aliases the TYPE NAME only: `struct nros_param_array_t` no longer names anything, because the tag moved with the type. Removal is no longer the follow-up; it happened in W-B5. Substantive verdict moved to `c:parameter_array_t`.",
    "rename": {
      "status": "landed",
      "category": "A",
      "resolution": "LANDED 2026-08-27, commit 92402ece0 (phase-379 W5), which renamed the whole C parameter family in one edit. `nros/parameter.h` declares only the `nros_parameter_*` spelling and no in-tree caller names the old one. This row is the RETAINED ALIAS, not open work.",
      "residue": "NONE since phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05), which retired the four typedef aliases beside the forwarders. The type-identity assertion that stood in `param_name_aliases.c` went with it — that file was removed, trimmed to its live half and renamed `packages/api/nros-c/tests/compile/param_entry_points.c`. What this row predicted came true and the changelog has to say so (W-B6): these four vanished without ever having warned, because C cannot portably deprecate a typedef."
    }
  },
  "c:param_callback_t": {
    "verdict": "rename",
    "why": "the deprecated `nros_param_*` spelling of `c:parameter_callback_t`. DEPRECATED ALIAS, phase-379 W5 (2026-08-27). The live spelling is `nros_parameter_callback_t`; this one survived one release as a plain `typedef` in `nros/parameter.h`; phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05) retired all four typedef aliases with the forwarders, so the old type name is an error now. It compiles SILENTLY -- C has no portable way to attach a deprecation attribute to a typedef (MSVC rejects `__declspec(deprecated)` there and `[[deprecated]]` on one is C23), so unlike the function forwarders this alias is a grace period rather than a warning, and this row is where the deprecation is actually recorded. It aliases the TYPE NAME only: `struct nros_param_callback_t` no longer names anything, because the tag moved with the type. Removal is no longer the follow-up; it happened in W-B5. Substantive verdict moved to `c:parameter_callback_t`.",
    "rename": {
      "status": "landed",
      "category": "A",
      "resolution": "LANDED 2026-08-27, commit 92402ece0 (phase-379 W5), which renamed the whole C parameter family in one edit. `nros/parameter.h` declares only the `nros_parameter_*` spelling and no in-tree caller names the old one. This row is the RETAINED ALIAS, not open work.",
      "residue": "NONE since phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05), which retired the four typedef aliases beside the forwarders. The type-identity assertion that stood in `param_name_aliases.c` went with it — that file was removed, trimmed to its live half and renamed `packages/api/nros-c/tests/compile/param_entry_points.c`. What this row predicted came true and the changelog has to say so (W-B6): these four vanished without ever having warned, because C cannot portably deprecate a typedef."
    }
  },
  "c:param_declare_*": {
    "verdict": "rename",
    "bucket": "ours-only",
    "why": "the deprecated `nros_param_*` spelling of `c:parameter_declare_*`. DEPRECATED ALIAS, phase-379 W5 (2026-08-27). The live spelling is `nros_parameter_declare_*`; this one survived one release as a `NROS_DEPRECATED_MSG` `static inline` forwarder in `nros/parameter.h`, so an out-of-tree C node that still named it kept compiling and got a diagnostic naming its replacement; phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05) RETIRED it, so `nros/parameter.h` no longer declares the old spelling and a node that still names it gets an undeclared-identifier error instead. Recorded rather than left implicit for the same reason as `rust:BoardConfig::zenoh_locator` in boot.json: a deprecated item is still API a reader can find, and this one teaches the reader the abbreviation the rename existed to remove. While it stood it was source compatibility only -- the forwarder had no external linkage, so `nros_parameter_declare_*` is the sole exported symbol and an object file built against the old one had to be recompiled. Removal is no longer the follow-up; it happened in W-B5. Substantive verdict moved to `c:parameter_declare_*`.",
    "rename": {
      "status": "landed",
      "category": "A",
      "resolution": "LANDED 2026-08-27, commit 92402ece0 (phase-379 W5), which renamed the whole C parameter family in one edit. `nros/parameter.h` declares only the `nros_parameter_*` spelling and no in-tree caller names the old one. This row is the RETAINED ALIAS, not open work.",
      "residue": "NONE since phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05). The `NROS_DEPRECATED_MSG` `static inline` forwarder was retained ON PURPOSE for one release, to retire with every other deprecated alias in the tree as ONE batch on a version boundary (phase-379 W7 step 4); that batch was W-B5, and it took both probes with it. `param_deprecation_probe.c` — the expected-failure compile under `-Werror=deprecated-declarations` that proved the forwarder still WARNED — was deleted, because with the identifier gone it would have kept passing while asserting nothing. `param_name_aliases.c` was removed the same way, trimmed to its live half and renamed `packages/api/nros-c/tests/compile/param_entry_points.c`, which pins the `nros_parameter_*` signatures alone on the `just check c` lane (`just/check/lanes.just`)."
    }
  },
  "c:param_get_*": {
    "verdict": "rename",
    "bucket": "ours-only",
    "why": "the deprecated `nros_param_*` spelling of `c:parameter_get_*`. DEPRECATED ALIAS, phase-379 W5 (2026-08-27). The live spelling is `nros_parameter_get_*`; this one survived one release as a `NROS_DEPRECATED_MSG` `static inline` forwarder in `nros/parameter.h`, so an out-of-tree C node that still named it kept compiling and got a diagnostic naming its replacement; phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05) RETIRED it, so `nros/parameter.h` no longer declares the old spelling and a node that still names it gets an undeclared-identifier error instead. Recorded rather than left implicit for the same reason as `rust:BoardConfig::zenoh_locator` in boot.json: a deprecated item is still API a reader can find, and this one teaches the reader the abbreviation the rename existed to remove. While it stood it was source compatibility only -- the forwarder had no external linkage, so `nros_parameter_get_*` is the sole exported symbol and an object file built against the old one had to be recompiled. Removal is no longer the follow-up; it happened in W-B5. Substantive verdict moved to `c:parameter_get_*`.",
    "rename": {
      "status": "landed",
      "category": "A",
      "resolution": "LANDED 2026-08-27, commit 92402ece0 (phase-379 W5), which renamed the whole C parameter family in one edit. `nros/parameter.h` declares only the `nros_parameter_*` spelling and no in-tree caller names the old one. This row is the RETAINED ALIAS, not open work.",
      "residue": "NONE since phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05). The `NROS_DEPRECATED_MSG` `static inline` forwarder was retained ON PURPOSE for one release, to retire with every other deprecated alias in the tree as ONE batch on a version boundary (phase-379 W7 step 4); that batch was W-B5, and it took both probes with it. `param_deprecation_probe.c` — the expected-failure compile under `-Werror=deprecated-declarations` that proved the forwarder still WARNED — was deleted, because with the identifier gone it would have kept passing while asserting nothing. `param_name_aliases.c` was removed the same way, trimmed to its live half and renamed `packages/api/nros-c/tests/compile/param_entry_points.c`, which pins the `nros_parameter_*` signatures alone on the `just check c` lane (`just/check/lanes.just`)."
    }
  },
  "c:param_get_type": {
    "verdict": "rename",
    "why": "the deprecated `nros_param_*` spelling of `c:parameter_get_type`. DEPRECATED ALIAS, phase-379 W5 (2026-08-27). The live spelling is `nros_parameter_get_type`; this one survived one release as a `NROS_DEPRECATED_MSG` `static inline` forwarder in `nros/parameter.h`, so an out-of-tree C node that still named it kept compiling and got a diagnostic naming its replacement; phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05) RETIRED it, so `nros/parameter.h` no longer declares the old spelling and a node that still names it gets an undeclared-identifier error instead. Recorded rather than left implicit for the same reason as `rust:BoardConfig::zenoh_locator` in boot.json: a deprecated item is still API a reader can find, and this one teaches the reader the abbreviation the rename existed to remove. While it stood it was source compatibility only -- the forwarder had no external linkage, so `nros_parameter_get_type` is the sole exported symbol and an object file built against the old one had to be recompiled. Removal is no longer the follow-up; it happened in W-B5. Substantive verdict moved to `c:parameter_get_type`.",
    "rename": {
      "status": "landed",
      "category": "A",
      "resolution": "LANDED 2026-08-27, commit 92402ece0 (phase-379 W5), which renamed the whole C parameter family in one edit. `nros/parameter.h` declares only the `nros_parameter_*` spelling and no in-tree caller names the old one. This row is the RETAINED ALIAS, not open work.",
      "residue": "NONE since phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05). The `NROS_DEPRECATED_MSG` `static inline` forwarder was retained ON PURPOSE for one release, to retire with every other deprecated alias in the tree as ONE batch on a version boundary (phase-379 W7 step 4); that batch was W-B5, and it took both probes with it. `param_deprecation_probe.c` — the expected-failure compile under `-Werror=deprecated-declarations` that proved the forwarder still WARNED — was deleted, because with the identifier gone it would have kept passing while asserting nothing. `param_name_aliases.c` was removed the same way, trimmed to its live half and renamed `packages/api/nros-c/tests/compile/param_entry_points.c`, which pins the `nros_parameter_*` signatures alone on the `just check c` lane (`just/check/lanes.just`)."
    }
  },
  "c:param_has": {
    "verdict": "rename",
    "why": "the deprecated `nros_param_*` spelling of `c:parameter_has`. DEPRECATED ALIAS, phase-379 W5 (2026-08-27). The live spelling is `nros_parameter_has`; this one survived one release as a `NROS_DEPRECATED_MSG` `static inline` forwarder in `nros/parameter.h`, so an out-of-tree C node that still named it kept compiling and got a diagnostic naming its replacement; phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05) RETIRED it, so `nros/parameter.h` no longer declares the old spelling and a node that still names it gets an undeclared-identifier error instead. Recorded rather than left implicit for the same reason as `rust:BoardConfig::zenoh_locator` in boot.json: a deprecated item is still API a reader can find, and this one teaches the reader the abbreviation the rename existed to remove. While it stood it was source compatibility only -- the forwarder had no external linkage, so `nros_parameter_has` is the sole exported symbol and an object file built against the old one had to be recompiled. Removal is no longer the follow-up; it happened in W-B5. Substantive verdict moved to `c:parameter_has`.",
    "rename": {
      "status": "landed",
      "category": "A",
      "resolution": "LANDED 2026-08-27, commit 92402ece0 (phase-379 W5), which renamed the whole C parameter family in one edit. `nros/parameter.h` declares only the `nros_parameter_*` spelling and no in-tree caller names the old one. This row is the RETAINED ALIAS, not open work.",
      "residue": "NONE since phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05). The `NROS_DEPRECATED_MSG` `static inline` forwarder was retained ON PURPOSE for one release, to retire with every other deprecated alias in the tree as ONE batch on a version boundary (phase-379 W7 step 4); that batch was W-B5, and it took both probes with it. `param_deprecation_probe.c` — the expected-failure compile under `-Werror=deprecated-declarations` that proved the forwarder still WARNED — was deleted, because with the identifier gone it would have kept passing while asserting nothing. `param_name_aliases.c` was removed the same way, trimmed to its live half and renamed `packages/api/nros-c/tests/compile/param_entry_points.c`, which pins the `nros_parameter_*` signatures alone on the `just check c` lane (`just/check/lanes.just`)."
    }
  },
  "c:param_server_fini": {
    "verdict": "rename",
    "why": "the deprecated `nros_param_*` spelling of `c:parameter_server_fini`. DEPRECATED ALIAS, phase-379 W5 (2026-08-27). The live spelling is `nros_parameter_server_fini`; this one survived one release as a `NROS_DEPRECATED_MSG` `static inline` forwarder in `nros/parameter.h`, so an out-of-tree C node that still named it kept compiling and got a diagnostic naming its replacement; phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05) RETIRED it, so `nros/parameter.h` no longer declares the old spelling and a node that still names it gets an undeclared-identifier error instead. Recorded rather than left implicit for the same reason as `rust:BoardConfig::zenoh_locator` in boot.json: a deprecated item is still API a reader can find, and this one teaches the reader the abbreviation the rename existed to remove. While it stood it was source compatibility only -- the forwarder had no external linkage, so `nros_parameter_server_fini` is the sole exported symbol and an object file built against the old one had to be recompiled. Removal is no longer the follow-up; it happened in W-B5. Substantive verdict moved to `c:parameter_server_fini`.",
    "rename": {
      "status": "landed",
      "category": "A",
      "resolution": "LANDED 2026-08-27, commit 92402ece0 (phase-379 W5), which renamed the whole C parameter family in one edit. `nros/parameter.h` declares only the `nros_parameter_*` spelling and no in-tree caller names the old one. This row is the RETAINED ALIAS, not open work.",
      "residue": "NONE since phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05). The `NROS_DEPRECATED_MSG` `static inline` forwarder was retained ON PURPOSE for one release, to retire with every other deprecated alias in the tree as ONE batch on a version boundary (phase-379 W7 step 4); that batch was W-B5, and it took both probes with it. `param_deprecation_probe.c` — the expected-failure compile under `-Werror=deprecated-declarations` that proved the forwarder still WARNED — was deleted, because with the identifier gone it would have kept passing while asserting nothing. `param_name_aliases.c` was removed the same way, trimmed to its live half and renamed `packages/api/nros-c/tests/compile/param_entry_points.c`, which pins the `nros_parameter_*` signatures alone on the `just check c` lane (`just/check/lanes.just`)."
    }
  },
  "c:param_server_get_count": {
    "verdict": "rename",
    "why": "the deprecated `nros_param_*` spelling of `c:parameter_server_get_count`. DEPRECATED ALIAS, phase-379 W5 (2026-08-27). The live spelling is `nros_parameter_server_get_count`; this one survived one release as a `NROS_DEPRECATED_MSG` `static inline` forwarder in `nros/parameter.h`, so an out-of-tree C node that still named it kept compiling and got a diagnostic naming its replacement; phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05) RETIRED it, so `nros/parameter.h` no longer declares the old spelling and a node that still names it gets an undeclared-identifier error instead. Recorded rather than left implicit for the same reason as `rust:BoardConfig::zenoh_locator` in boot.json: a deprecated item is still API a reader can find, and this one teaches the reader the abbreviation the rename existed to remove. While it stood it was source compatibility only -- the forwarder had no external linkage, so `nros_parameter_server_get_count` is the sole exported symbol and an object file built against the old one had to be recompiled. Removal is no longer the follow-up; it happened in W-B5. Substantive verdict moved to `c:parameter_server_get_count`.",
    "rename": {
      "status": "landed",
      "category": "A",
      "resolution": "LANDED 2026-08-27, commit 92402ece0 (phase-379 W5), which renamed the whole C parameter family in one edit. `nros/parameter.h` declares only the `nros_parameter_*` spelling and no in-tree caller names the old one. This row is the RETAINED ALIAS, not open work.",
      "residue": "NONE since phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05). The `NROS_DEPRECATED_MSG` `static inline` forwarder was retained ON PURPOSE for one release, to retire with every other deprecated alias in the tree as ONE batch on a version boundary (phase-379 W7 step 4); that batch was W-B5, and it took both probes with it. `param_deprecation_probe.c` — the expected-failure compile under `-Werror=deprecated-declarations` that proved the forwarder still WARNED — was deleted, because with the identifier gone it would have kept passing while asserting nothing. `param_name_aliases.c` was removed the same way, trimmed to its live half and renamed `packages/api/nros-c/tests/compile/param_entry_points.c`, which pins the `nros_parameter_*` signatures alone on the `just check c` lane (`just/check/lanes.just`)."
    }
  },
  "c:param_server_get_zero_initialized": {
    "verdict": "rename",
    "why": "the deprecated `nros_param_*` spelling of `c:parameter_server_get_zero_initialized`. DEPRECATED ALIAS, phase-379 W5 (2026-08-27). The live spelling is `nros_parameter_server_get_zero_initialized`; this one survived one release as a `NROS_DEPRECATED_MSG` `static inline` forwarder in `nros/parameter.h`, so an out-of-tree C node that still named it kept compiling and got a diagnostic naming its replacement; phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05) RETIRED it, so `nros/parameter.h` no longer declares the old spelling and a node that still names it gets an undeclared-identifier error instead. Recorded rather than left implicit for the same reason as `rust:BoardConfig::zenoh_locator` in boot.json: a deprecated item is still API a reader can find, and this one teaches the reader the abbreviation the rename existed to remove. While it stood it was source compatibility only -- the forwarder had no external linkage, so `nros_parameter_server_get_zero_initialized` is the sole exported symbol and an object file built against the old one had to be recompiled. Removal is no longer the follow-up; it happened in W-B5. Substantive verdict moved to `c:parameter_server_get_zero_initialized`.",
    "rename": {
      "status": "landed",
      "category": "A",
      "resolution": "LANDED 2026-08-27, commit 92402ece0 (phase-379 W5), which renamed the whole C parameter family in one edit. `nros/parameter.h` declares only the `nros_parameter_*` spelling and no in-tree caller names the old one. This row is the RETAINED ALIAS, not open work.",
      "residue": "NONE since phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05). The `NROS_DEPRECATED_MSG` `static inline` forwarder was retained ON PURPOSE for one release, to retire with every other deprecated alias in the tree as ONE batch on a version boundary (phase-379 W7 step 4); that batch was W-B5, and it took both probes with it. `param_deprecation_probe.c` — the expected-failure compile under `-Werror=deprecated-declarations` that proved the forwarder still WARNED — was deleted, because with the identifier gone it would have kept passing while asserting nothing. `param_name_aliases.c` was removed the same way, trimmed to its live half and renamed `packages/api/nros-c/tests/compile/param_entry_points.c`, which pins the `nros_parameter_*` signatures alone on the `just check c` lane (`just/check/lanes.just`)."
    }
  },
  "c:param_server_init": {
    "verdict": "rename",
    "why": "the deprecated `nros_param_*` spelling of `c:parameter_server_init`. DEPRECATED ALIAS, phase-379 W5 (2026-08-27). The live spelling is `nros_parameter_server_init`; this one survived one release as a `NROS_DEPRECATED_MSG` `static inline` forwarder in `nros/parameter.h`, so an out-of-tree C node that still named it kept compiling and got a diagnostic naming its replacement; phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05) RETIRED it, so `nros/parameter.h` no longer declares the old spelling and a node that still names it gets an undeclared-identifier error instead. Recorded rather than left implicit for the same reason as `rust:BoardConfig::zenoh_locator` in boot.json: a deprecated item is still API a reader can find, and this one teaches the reader the abbreviation the rename existed to remove. While it stood it was source compatibility only -- the forwarder had no external linkage, so `nros_parameter_server_init` is the sole exported symbol and an object file built against the old one had to be recompiled. Removal is no longer the follow-up; it happened in W-B5. Substantive verdict moved to `c:parameter_server_init`.",
    "rename": {
      "status": "landed",
      "category": "A",
      "resolution": "LANDED 2026-08-27, commit 92402ece0 (phase-379 W5), which renamed the whole C parameter family in one edit. `nros/parameter.h` declares only the `nros_parameter_*` spelling and no in-tree caller names the old one. This row is the RETAINED ALIAS, not open work.",
      "residue": "NONE since phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05). The `NROS_DEPRECATED_MSG` `static inline` forwarder was retained ON PURPOSE for one release, to retire with every other deprecated alias in the tree as ONE batch on a version boundary (phase-379 W7 step 4); that batch was W-B5, and it took both probes with it. `param_deprecation_probe.c` — the expected-failure compile under `-Werror=deprecated-declarations` that proved the forwarder still WARNED — was deleted, because with the identifier gone it would have kept passing while asserting nothing. `param_name_aliases.c` was removed the same way, trimmed to its live half and renamed `packages/api/nros-c/tests/compile/param_entry_points.c`, which pins the `nros_parameter_*` signatures alone on the `just check c` lane (`just/check/lanes.just`)."
    }
  },
  "c:param_server_set_callback": {
    "verdict": "rename",
    "why": "the deprecated `nros_param_*` spelling of `c:parameter_server_set_callback`. DEPRECATED ALIAS, phase-379 W5 (2026-08-27). The live spelling is `nros_parameter_server_set_callback`; this one survived one release as a `NROS_DEPRECATED_MSG` `static inline` forwarder in `nros/parameter.h`, so an out-of-tree C node that still named it kept compiling and got a diagnostic naming its replacement; phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05) RETIRED it, so `nros/parameter.h` no longer declares the old spelling and a node that still names it gets an undeclared-identifier error instead. Recorded rather than left implicit for the same reason as `rust:BoardConfig::zenoh_locator` in boot.json: a deprecated item is still API a reader can find, and this one teaches the reader the abbreviation the rename existed to remove. While it stood it was source compatibility only -- the forwarder had no external linkage, so `nros_parameter_server_set_callback` is the sole exported symbol and an object file built against the old one had to be recompiled. Removal is no longer the follow-up; it happened in W-B5. Substantive verdict moved to `c:parameter_server_set_callback`.",
    "rename": {
      "status": "landed",
      "category": "A",
      "resolution": "LANDED 2026-08-27, commit 92402ece0 (phase-379 W5), which renamed the whole C parameter family in one edit. `nros/parameter.h` declares only the `nros_parameter_*` spelling and no in-tree caller names the old one. This row is the RETAINED ALIAS, not open work.",
      "residue": "NONE since phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05). The `NROS_DEPRECATED_MSG` `static inline` forwarder was retained ON PURPOSE for one release, to retire with every other deprecated alias in the tree as ONE batch on a version boundary (phase-379 W7 step 4); that batch was W-B5, and it took both probes with it. `param_deprecation_probe.c` — the expected-failure compile under `-Werror=deprecated-declarations` that proved the forwarder still WARNED — was deleted, because with the identifier gone it would have kept passing while asserting nothing. `param_name_aliases.c` was removed the same way, trimmed to its live half and renamed `packages/api/nros-c/tests/compile/param_entry_points.c`, which pins the `nros_parameter_*` signatures alone on the `just check c` lane (`just/check/lanes.just`)."
    }
  },
  "c:param_server_state_t": {
    "verdict": "rename",
    "why": "the deprecated `nros_param_*` spelling of `c:parameter_server_state_t`. DEPRECATED ALIAS, phase-379 W5 (2026-08-27). The live spelling is `nros_parameter_server_state_t`; this one survived one release as a plain `typedef` in `nros/parameter.h`; phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05) retired all four typedef aliases with the forwarders, so the old type name is an error now. It compiles SILENTLY -- C has no portable way to attach a deprecation attribute to a typedef (MSVC rejects `__declspec(deprecated)` there and `[[deprecated]]` on one is C23), so unlike the function forwarders this alias is a grace period rather than a warning, and this row is where the deprecation is actually recorded. It aliases the TYPE NAME only: `struct nros_param_server_state_t` no longer names anything, because the tag moved with the type. Removal is no longer the follow-up; it happened in W-B5. Substantive verdict moved to `c:parameter_server_state_t`.",
    "rename": {
      "status": "landed",
      "category": "A",
      "resolution": "LANDED 2026-08-27, commit 92402ece0 (phase-379 W5), which renamed the whole C parameter family in one edit. `nros/parameter.h` declares only the `nros_parameter_*` spelling and no in-tree caller names the old one. This row is the RETAINED ALIAS, not open work.",
      "residue": "NONE since phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05), which retired the four typedef aliases beside the forwarders. The type-identity assertion that stood in `param_name_aliases.c` went with it — that file was removed, trimmed to its live half and renamed `packages/api/nros-c/tests/compile/param_entry_points.c`. What this row predicted came true and the changelog has to say so (W-B6): these four vanished without ever having warned, because C cannot portably deprecate a typedef."
    }
  },
  "c:param_server_t": {
    "verdict": "rename",
    "why": "the deprecated `nros_param_*` spelling of `c:parameter_server_t`. DEPRECATED ALIAS, phase-379 W5 (2026-08-27). The live spelling is `nros_parameter_server_t`; this one survived one release as a plain `typedef` in `nros/parameter.h`; phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05) retired all four typedef aliases with the forwarders, so the old type name is an error now. It compiles SILENTLY -- C has no portable way to attach a deprecation attribute to a typedef (MSVC rejects `__declspec(deprecated)` there and `[[deprecated]]` on one is C23), so unlike the function forwarders this alias is a grace period rather than a warning, and this row is where the deprecation is actually recorded. It aliases the TYPE NAME only: `struct nros_param_server_t` no longer names anything, because the tag moved with the type. Removal is no longer the follow-up; it happened in W-B5. Substantive verdict moved to `c:parameter_server_t`.",
    "rename": {
      "status": "landed",
      "category": "A",
      "resolution": "LANDED 2026-08-27, commit 92402ece0 (phase-379 W5), which renamed the whole C parameter family in one edit. `nros/parameter.h` declares only the `nros_parameter_*` spelling and no in-tree caller names the old one. This row is the RETAINED ALIAS, not open work.",
      "residue": "NONE since phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05), which retired the four typedef aliases beside the forwarders. The type-identity assertion that stood in `param_name_aliases.c` went with it — that file was removed, trimmed to its live half and renamed `packages/api/nros-c/tests/compile/param_entry_points.c`. What this row predicted came true and the changelog has to say so (W-B6): these four vanished without ever having warned, because C cannot portably deprecate a typedef."
    }
  },
  "c:param_set_*": {
    "verdict": "rename",
    "bucket": "ours-only",
    "why": "the deprecated `nros_param_*` spelling of `c:parameter_set_*`. DEPRECATED ALIAS, phase-379 W5 (2026-08-27). The live spelling is `nros_parameter_set_*`; this one survived one release as a `NROS_DEPRECATED_MSG` `static inline` forwarder in `nros/parameter.h`, so an out-of-tree C node that still named it kept compiling and got a diagnostic naming its replacement; phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05) RETIRED it, so `nros/parameter.h` no longer declares the old spelling and a node that still names it gets an undeclared-identifier error instead. Recorded rather than left implicit for the same reason as `rust:BoardConfig::zenoh_locator` in boot.json: a deprecated item is still API a reader can find, and this one teaches the reader the abbreviation the rename existed to remove. While it stood it was source compatibility only -- the forwarder had no external linkage, so `nros_parameter_set_*` is the sole exported symbol and an object file built against the old one had to be recompiled. Removal is no longer the follow-up; it happened in W-B5. Substantive verdict moved to `c:parameter_set_*`.",
    "rename": {
      "status": "landed",
      "category": "A",
      "resolution": "LANDED 2026-08-27, commit 92402ece0 (phase-379 W5), which renamed the whole C parameter family in one edit. `nros/parameter.h` declares only the `nros_parameter_*` spelling and no in-tree caller names the old one. This row is the RETAINED ALIAS, not open work.",
      "residue": "NONE since phase-417 W-B5 (stage 6 step B, commit 4dc5caee9, 2026-09-05). The `NROS_DEPRECATED_MSG` `static inline` forwarder was retained ON PURPOSE for one release, to retire with every other deprecated alias in the tree as ONE batch on a version boundary (phase-379 W7 step 4); that batch was W-B5, and it took both probes with it. `param_deprecation_probe.c` — the expected-failure compile under `-Werror=deprecated-declarations` that proved the forwarder still WARNED — was deleted, because with the identifier gone it would have kept passing while asserting nothing. `param_name_aliases.c` was removed the same way, trimmed to its live half and renamed `packages/api/nros-c/tests/compile/param_entry_points.c`, which pins the `nros_parameter_*` signatures alone on the `just check c` lane (`just/check/lanes.just`)."
    }
  },
  "c:parameter_array_t": {
    "verdict": "extension",
    "why": "LANDED 2026-08-27 (phase-379 W5): the C parameter family is now spelled `nros_parameter_*`. Renamed here from `c:param_array_t`, which survives as the deprecated alias row. pointer + length for the six array parameter types. The pointer is BORROWED -- the server records the caller's buffer verbatim and never copies (issue 0340, documented at `nros/parameter.h`'s \"Array parameter pointer identity\") -- because there is no allocator to own the elements. rcl's parsed arrays (`c:double_array_t` and friends) own their memory."
  },
  "c:parameter_declare_*": {
    "verdict": "extension",
    "bucket": "ours-only",
    "why": "LANDED 2026-08-27 (phase-379 W5): the C parameter family is now spelled `nros_parameter_*`. Renamed here from `c:param_declare_*`, which survives as the deprecated alias row. declare-with-default, one entry point per `rcl_interfaces` type. Two things are ours-only here. The FOLD of rclc's `add` + `set` into one call is deliberate and matches rclcpp/rclrs -- see `c:add_parameter`. The six ARRAY types (`byte_array`, `bool_array`, `integer_array`, `double_array`, `string_array`) have no rclc counterpart at all: rclc's parameter server handles bool/int/double scalars only. Scenario: a controller whose parameter is a calibration vector or an MPC weight matrix, stored by borrowed pointer (`c:parameter_array_t`) because there is no heap. Note which of our TWO C parameter stores this is. `nros_parameter_server_t` (`nros/parameter.h`) is a standalone store over caller-supplied storage and is purely node-local -- `parameter.rs` calls it \"the legacy `nros_parameter_server_t` API\" in its own comment. The store the six `rcl_interfaces/srv/*` servers read is a DIFFERENT object, the `nros_params::ParameterServer` the executor owns, reached only through `nros_executor_*_param_*`. Nothing joins them. So an array declared here is invisible to `ros2 param get`: `nros_executor_declare_param_*` covers the four SCALARS only, and there is no C entry point that puts an array in the store the services read. Phase 379 W3.\n\nphase-428 W5 (2026-09-06) — ledgering a recorded sweep finding. RANKED-24, with the cited helper CORRECTED. The finding pointed at `packages/api/nros-c/src/util.rs:18` (`copy_cstr_into`), which is the ENTITY-name truncator with 36 call sites; the PARAMETER path uses a SECOND, private spelling of the same silent contract — `copy_cstr_to_buffer`, `packages/api/nros-c/src/parameter.rs:170`. Both truncate to the buffer, NUL-terminate, and return a length with no overflow signal.\n\nUPSTREAM'S CONTRACT: rcl/rclc reject an over-long name rather than storing a different one — a declare either takes the name you gave it or fails.\n\nOURS: `NROS_MAX_PARAM_NAME_LEN` is 64 and one byte is the NUL, so a name of 64 bytes or more is stored truncated to 63 and `declare` returns `NROS_RET_OK`. Lookup goes through `find_parameter` -> `cstr_eq_buffer`, which walks the caller's FULL string against the stored buffer and mismatches at index 63. The parameter therefore exists, is counted, occupies a slot, and can never be found by the name it was declared with — not by `has`, `get`, `set`, or a re-`declare`, which does not even return `NROS_RET_ALREADY_EXISTS`.\n\nHOW A USER FINDS OUT: SILENTLY WRONG. OK on declare; not-found on every later access to a name the API just accepted.\n\nDisposition ADOPT — the correct idiom is already in the crate: `write_cstr_out` (`packages/api/nros-c/src/node.rs:824`) returns `NROS_RET_FULL` rather than truncating. The ENTITY-name half of the same class (topic/service/type names truncated against 256-byte buffers, entity created on a DIFFERENT key, `NROS_RET_OK` returned) is recorded at `c:publisher_init`. Re-verified by hand against the current worktree before this row was written.\n\nDISPOSITION ADOPT, recorded in the field, and it is about the TRUNCATION only — the `extension` verdict above (the fold of rclc's add+set, and the six array types rclc has no counterpart for) is untouched. Rejecting an over-long name is what upstream does and what `write_cstr_out` already does in this crate.",
    "disposition": "adopt"
  },
  "c:parameter_get_*": {
    "verdict": "extension",
    "bucket": "ours-only",
    "why": "LANDED 2026-08-27 (phase-379 W5): the C parameter family is now spelled `nros_parameter_*`. Renamed here from `c:param_get_*`, which survives as the deprecated alias row. the getters for `c:parameter_declare_*`; scalars are rclc's `rclc_parameter_get_*` renamed (see `c:parameter_get_bool`) and the array forms are ours-only. An array get returns the BORROWED pointer unchanged, which `nros/parameter.hpp` depends on -- issue 0340."
  },
  "c:parameter_get_int": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "rclc spells it `rclc_parameter_get_int`; ours is `nros_parameter_get_integer`. See `c:parameter_server_t` for the `param`/`parameter` split. Ours also spells the 64-bit integer `integer` where rclc says `int`, matching `rcl_interfaces` and our own `ParameterType::Integer`. RE-VERDICTED 2026-08-27 (phase-379 W5), was `rename`. The `param` -> `parameter` half of this row LANDED: ours is `nros_parameter_get_integer` now. What is left is the only thing this row can still be about, and it is a difference we KEEP: rclc abbreviates the 64-bit integer to `int`, and we spell it `integer` because that is what `rcl_interfaces` calls the type and what our own `ParameterType::Integer`, `nros_parameter_type_t::NROS_PARAMETER_INTEGER` and the C++/Rust surfaces call it. Renaming ours to `int` would make the C surface disagree with our other two languages and with the message definitions, to match one abbreviation. So there is no destination left for this row to carry, which -- exactly as for `c:add_parameter` -- makes it a DECLINE rather than a rename. What a user loses: nothing but the spelling; `rclc_parameter_get_int(s, \"x\", &v)` is `nros_parameter_get_integer(s, \"x\", &v)`. Ours: `c:parameter_get_integer`.",
    "rename": {
      "status": "resolved",
      "resolution": "the `param` -> `parameter` prefix half landed in phase-379 W5; the remaining `int` vs `integer` difference is one we decline to make, so no rename target is left and the row was re-verdicted `declined`"
    }
  },
  "c:parameter_get_type": {
    "verdict": "extension",
    "why": "LANDED 2026-08-27 (phase-379 W5): the C parameter family is now spelled `nros_parameter_*`. Renamed here from `c:param_get_type`, which survives as the deprecated alias row. OUR LANGUAGES DISAGREE: C and Rust can ask a parameter its type -- `nros_parameter_get_type` here, `ParameterServer::get_type` there -- and C++ cannot: `nros::ParameterServer` wraps `nros_parameter_has` and `nros_parameter_server_get_count` and skips this one, so a C++ node has no type accessor at all. Ours-only against rclc because rclc has no type accessor either; rclcpp's nearest is `Node::get_parameter_types`, which returns a vector, where a single-name accessor returning `NROS_PARAMETER_NOT_SET` for absent needs no allocation."
  },
  "c:parameter_has": {
    "verdict": "extension",
    "why": "LANDED 2026-08-27 (phase-379 W5): the C parameter family is now spelled `nros_parameter_*`. Renamed here from `c:param_has`, which survives as the deprecated alias row. an existence predicate, so a C node can branch instead of treating `NROS_RET_NOT_FOUND` as fatal. rclc has none; rclcpp spells it `Node::has_parameter`, which we do have in C++ (`ParameterServer::has_parameter`) and Rust (`ParameterServer::has`)."
  },
  "c:parameter_options_t": {
    "verdict": "divergence",
    "why": "`rclc_parameter_options_t` carries `notify_changed_over_dds`, `allow_undeclared_parameters`, `low_mem_mode` and `max_params` as RUNTIME fields. All four are compile-time here: the table size is the `capacity` argument to `nros_parameter_server_init` over caller storage, undeclared access is not offered, and there is no `/parameter_events` publisher to toggle (see `cpp:ParameterEventHandler`). Same `compile-time-options` collapse as the node stage's `c:node_options_*`. LANDED 2026-08-27 (phase-379 W5): the name half is done -- ours is `nros_parameter_server_init`. What remains is the caller-owned-storage divergence, which is the verdict."
  },
  "c:parameter_server_fini": {
    "verdict": "rename",
    "why": "rclc's `rclc_parameter_server_fini`; ours is `nros_parameter_server_fini`. See `c:parameter_server_t`. LANDED 2026-08-27 (phase-379 W5). Ours is now spelled the same way, so this row and its ours-only half have COLLAPSED -- the correlator reports the pair as `systematic` under the `handle-owns-node` rule (rclc's fini also takes the node; ours does not, which is that rule, not a naming difference) and neither side needs a verdict to explain it. The row is kept for the history. The old `nros_param_*` spelling still exists as a deprecated forwarder, recorded at `c:param_server_fini`.",
    "rename": {
      "status": "landed",
      "category": "A",
      "resolution": "LANDED 2026-08-27 (92402ece0). This is the LIVE spelling, not an alias — it reads as part of the alias family only because it sorts beside `c:param_server_fini`. Ours now spells rclc's name, so the pair correlates `systematic` under `handle-owns-node` and neither half needs a verdict. A phase-379 W7 STEP 1 collapse candidate, not open work; the retained forwarder is rowed separately at `c:param_server_fini`."
    }
  },
  "c:parameter_server_get_count": {
    "verdict": "extension",
    "why": "LANDED 2026-08-27 (phase-379 W5): the C parameter family is now spelled `nros_parameter_*`. Renamed here from `c:param_server_get_count`, which survives as the deprecated alias row. how many parameters are declared. rclc exposes the count as a struct field on its server; ours is behind an accessor because `nros_parameter_server_t`'s storage fields are not part of the documented ABI. rclcpp's nearest is `list_parameters().names.size()`."
  },
  "c:parameter_server_get_zero_initialized": {
    "verdict": "extension",
    "why": "LANDED 2026-08-27 (phase-379 W5): the C parameter family is now spelled `nros_parameter_*`. Renamed here from `c:param_server_get_zero_initialized`, which survives as the deprecated alias row. rcl's zero-init idiom under our naming; see the pubsub stage's `c:publisher_get_zero_initialized`. rclc has no equivalent because `rclc_parameter_server_init_default` allocates."
  },
  "c:parameter_server_init": {
    "verdict": "divergence",
    "why": "LANDED 2026-08-27 (phase-379 W5): the C parameter family is now spelled `nros_parameter_*`. Renamed here from `c:param_server_init`, which survives as the deprecated alias row. the ours-only half of `c:parameter_server_init_default` -- caller-owned storage array plus an explicit capacity, because there is no allocator."
  },
  "c:parameter_server_init_default": {
    "verdict": "divergence",
    "provides": [
      "nros_parameter_server_init",
      "nros_executor_register_parameter_services"
    ],
    "why": "rclc's `rclc_parameter_server_init_default(server, node)` allocates the parameter table from `rcl_get_default_allocator()` and creates the six services in the same call. Ours is `nros_parameter_server_init(server, storage, capacity)`: the caller owns the `nros_parameter_t[]` and the count is an argument, because there is no allocator to size a table from. The service half is a separate explicit call (`c:executor_register_parameter_services`, service stage). The NAME should still change -- see `c:parameter_server_t`. LANDED 2026-08-27 (phase-379 W5): the name half is done -- ours is `nros_parameter_server_init`. What remains is the caller-owned-storage divergence, which is the verdict."
  },
  "c:parameter_server_init_with_option": {
    "verdict": "divergence",
    "why": "the options-taking form of `c:parameter_server_init_default`; see also `c:parameter_options_t`."
  },
  "c:parameter_server_set_callback": {
    "verdict": "extension",
    "why": "LANDED 2026-08-27 (phase-379 W5): the C parameter family is now spelled `nros_parameter_*`. Renamed here from `c:param_server_set_callback`, which survives as the deprecated alias row. OUR LANGUAGES DISAGREE: C has a parameter-change callback and neither C++ nor Rust does -- `nros::ParameterServer` wraps every other C entry point and skips this one, and `nros_params::ParameterServer` has no callback field at all (`grep -c callback server.rs` is 0). It is an accept/reject callback invoked before a set is applied -- the `bool` return VETOES the change, which is exactly rclcpp's `add_on_set_parameters_callback` contract. Scenario: a device that must refuse a control-loop period outside its actuator's safe band before it takes effect, with no allocator for rclcpp's handle-per-registration. rclc passes the same callback to `rclc_executor_add_parameter_server` instead, which is why this shows as ours-only. TWO CAVEATS. (1) It is registered on the LEGACY standalone server, not on the executor-owned store the parameter services write to, so it does NOT fire for `ros2 param set` -- see `c:executor_add_parameter_server_with_context`. (2) RFC-0036 line 91 (\"No parameter callbacks\") predates it and should be corrected. Phase 379 W3."
  },
  "c:parameter_server_state_t": {
    "verdict": "extension",
    "why": "LANDED 2026-08-27 (phase-379 W5): the C parameter family is now spelled `nros_parameter_*`. Renamed here from `c:param_server_state_t`, which survives as the deprecated alias row. explicit entity lifecycle so double-init and use-after-fini return a code instead of being undefined; see the pubsub stage's `c:publisher_state_t`."
  },
  "c:parameter_set_*": {
    "verdict": "extension",
    "bucket": "ours-only",
    "why": "LANDED 2026-08-27 (phase-379 W5): the C parameter family is now spelled `nros_parameter_*`. Renamed here from `c:param_set_*`, which survives as the deprecated alias row. the setters for `c:parameter_declare_*`; see `c:parameter_get_*`."
  },
  "c:parameter_set_int": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "the setter half; see `c:parameter_get_int`. RE-VERDICTED 2026-08-27 (phase-379 W5), was `rename`. The `param` -> `parameter` half of this row LANDED: ours is `nros_parameter_set_integer` now. What is left is the only thing this row can still be about, and it is a difference we KEEP: rclc abbreviates the 64-bit integer to `int`, and we spell it `integer` because that is what `rcl_interfaces` calls the type and what our own `ParameterType::Integer`, `nros_parameter_type_t::NROS_PARAMETER_INTEGER` and the C++/Rust surfaces call it. Renaming ours to `int` would make the C surface disagree with our other two languages and with the message definitions, to match one abbreviation. So there is no destination left for this row to carry, which -- exactly as for `c:add_parameter` -- makes it a DECLINE rather than a rename. What a user loses: nothing but the spelling; `rclc_parameter_set_int(s, \"x\", &v)` is `nros_parameter_set_integer(s, \"x\", &v)`. Ours: `c:parameter_set_integer`.",
    "rename": {
      "status": "resolved",
      "resolution": "the `param` -> `parameter` prefix half landed in phase-379 W5; the remaining `int` vs `integer` difference is one we decline to make, so no rename target is left and the row was re-verdicted `declined`"
    }
  },
  "c:parameter_t": {
    "verdict": "extension",
    "why": "the record a declared parameter occupies: name, type and value inline, no indirection. rclc keeps the same fields in `Parameter` from `rcl_interfaces`, which is a generated message with heap strings; ours is a fixed-size struct in the caller's storage array. Named with the full word where the functions around it say `param` -- see `c:parameter_server_t`."
  },
  "c:parameter_value_t": {
    "verdict": "extension",
    "why": "the value union inside `c:parameter_t`. `rcl_interfaces/msg/ParameterValue` carries every type as a separate populated field plus a discriminator; a union sized by its largest member is the no-allocator form of the same thing."
  },
  "c:params_t": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "the whole parsed parameter tree: node names plus per-node parameter lists. `rcl_yaml_param_parser` exists to parse a `--params-file` off a FILESYSTEM at node start and hand rcl a tree of overrides. A device image has no filesystem and no YAML parser; parameter defaults are baked into the image at build time (RFC-0045) and changed afterwards over `~/set_parameters`. What a user loses is `--params-file` and everything that reads one."
  },
  "c:set_parameter_read_only": {
    "verdict": "divergence",
    "disposition": "absent",
    "provides": [
      "nros_executor_set_param_read_only",
      "nros_executor_set_param_read_only_on"
    ],
    "why": "rclc marks a declared parameter read-only so `ros2 param set` is refused. Ours does, through the descriptor the store already checks on every write, so a local `nros_executor_set_param_*` is refused exactly as the wire is. RE-VERDICTED phase-417 W4.a. was `gap`, and the capability now SHIPS — under our spelling, not rclc's. Ours is `nros_executor_*_param_*` because the store is the EXECUTOR's and keyed by NODE (phase-426 W1), where rclc's verbs take an `rclc_parameter_server_t*`: an image composes several nodes onto one executor, and a C caller holds the `nros_node_t` it passed to `nros_executor_node_init`, not a per-node server object. Deliberately NOT added to the legacy `nros_parameter_server_t` family, which is disjoint from the store the six `rcl_interfaces` servers read (issue 0793) — a descriptor surface there would be a second answer to 'what is this parameter's range'. Whether that family is retired or re-pointed is phase-417 W2.a's decision and nothing here depends on it."
  },
  "c:variant_t": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "the tagged union a parsed YAML scalar or sequence lands in. `rcl_yaml_param_parser` exists to parse a `--params-file` off a FILESYSTEM at node start and hand rcl a tree of overrides. A device image has no filesystem and no YAML parser; parameter defaults are baked into the image at build time (RFC-0045) and changed afterwards over `~/set_parameters`. What a user loses is `--params-file` and everything that reads one."
  },
  "cpp:AsyncParametersClient": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "the CLIENT side of the parameter services -- a service client speaking `rcl_interfaces/srv/*` on someone else's behalf. Ours registers the SERVER side (`nros_executor_register_parameter_services`) and ships no client: a device sets its own parameters from baked config (RFC-0045) and is the thing a desktop tool queries, not the thing that queries. See the param stage for the server half."
  },
  "cpp:LifecycleNode::*parameter*": {
    "verdict": "gap",
    "bucket": "theirs-only",
    "why": "`rclcpp_lifecycle::LifecycleNode` repeats `Node`'s entire parameter surface verbatim. Every row has the same answer as its `cpp:Node::` twin -- a lifecycle node declares and reads parameters exactly like an ordinary one here, and a lifecycle node's parameters are not lifecycle-gated in rclcpp either. See the `cpp:Node::*` rows in this shard."
  },
  "cpp:Node::add_on_set_parameters_callback": {
    "verdict": "divergence",
    "disposition": "refuse-loud",
    "why": "upstream takes a `std::function` over a `std::vector<rclcpp::Parameter>` returning a `SetParametersResult`, and hands back an owning `ParameterCallbackHandle` shared_ptr whose destruction unregisters. With no allocator ours is a plain function pointer plus a `void*` context seeing ONE write at a time as scalars, answering `bool` — upstream's `successful` — and `out_handle` receives a `uint16_t` token because there is nothing to own. The registry is bounded (`nros_params::MAX_ON_SET_CALLBACKS`, 4) and a full one answers `Result::FULL` rather than evicting somebody else's hook. The hook runs AFTER the store's read-only / type / range rules and on every write that reaches the store, local or over `~/set_parameters`, because `apply` is the one seam. RE-VERDICTED phase-417 W4.a. was `gap`, and the capability now SHIPS on `rclcpp::Node`, forwarding to the executor's one `nros_params::ParameterServer` and holding no state of its own. `refuse-loud` rather than `adopt`: the NAME exists, so a ported file reaches it, and the SIGNATURE is what refuses — the compiler names the mismatch at the call site and there is no shape in which this compiles and silently differs."
  },
  "cpp:Node::declare_parameters": {
    "verdict": "divergence",
    "disposition": "adopt-bounded",
    "why": "SHIPPED phase-417 W4.a. The map-taking bulk form, hosted only, because upstream's argument IS a `std::map<std::string, T>`. `prefix` is joined with `.` exactly as upstream does. The envelope: upstream returns `std::vector<T>`, the values IN EFFECT after each declaration, and ours returns `nros::Result`. They differ only where a launch seed already declared a name — the case rclcpp's return exists for — and a caller who needs it reads back with `get_parameter<T>`. A ported file that ignores the return compiles and behaves identically; one that binds it does not compile, which is the loud half. The row previously said `ComponentNode` 'would not have it either'; that type is gone (phase-427 W4) and the reason it gave (the map needs an allocator) is what the `NROS_CPP_STD` gate is for, not a reason to decline."
  },
  "cpp:Node::describe_parameter": {
    "verdict": "divergence",
    "disposition": "refuse-loud",
    "why": "upstream returns an `rcl_interfaces::msg::ParameterDescriptor` BY VALUE, whose strings own their storage. Ours fills a caller-owned `rclcpp::ParameterDescriptor` whose two `const char*` point into a caller-owned text buffer the call splits in half: `describe_parameter(name, out, text, text_len)`. There is no other shape without an allocator, and across the FFI there is no descriptor struct at all — text in as a borrowed `const char*`, out into a `char*` of stated capacity, scalars as NULLable out-params. A struct with an inline buffer would put `NROS_MAX_PARAM_DESCRIPTION_LEN` into the ABI and make a build knob a layout, which `check-cpp-capability-layout` forbids. RE-VERDICTED phase-417 W4.a. was `gap`, and the capability now SHIPS on `rclcpp::Node`, forwarding to the executor's one `nros_params::ParameterServer` and holding no state of its own. `refuse-loud` rather than `adopt`: the NAME exists, so a ported file reaches it, and the SIGNATURE is what refuses — the compiler names the mismatch at the call site and there is no shape in which this compiles and silently differs."
  },
  "cpp:Node::describe_parameters": {
    "verdict": "declined",
    "disposition": "absent",
    "provides": [
      "nros::Node::get_parameter",
      "nros::Node::set_parameter",
      "nros::Node::describe_parameter",
      "nros::Node::set_parameters_atomically"
    ],
    "why": "RE-VERDICTED phase-417 W4.a. was `gap`. the vector form of `describe_parameter`, which ships one name at a time into caller storage (phase-417 W4.a). Every one of these is a vector of `rclcpp::Parameter` — a generated ROS message value object ledgered `declined` at `cpp:Parameter` — and a `std::vector` needs an allocator the freestanding lane does not have. The singular forms ship and reach the same store; the plural forms are declined because their ARGUMENT TYPE is, not because the capability is missing."
  },
  "cpp:Node::get_node_parameters_interface": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "see the node stage's `cpp:Node::get_node_base_interface` -- rclcpp's ~10 `node_interfaces::Node*Interface` slices exist so a component can be handed the part it needs; our components are declared, not constructed with an injected node (RFC-0044)."
  },
  "cpp:Node::get_parameter_type": {
    "verdict": "extension",
    "why": "The SINGULAR type query. rclcpp has only the vector-taking `get_parameter_types`, which needs an allocator for both its argument and its return; ours ships as `get_parameter_types(names, count, out)` over caller storage (`cpp:Node::get_parameter_types`) and this is the one-name form underneath it. phase-417 W4.a. C and Rust have had the singular form all along (`nros_parameter_get_type`, `ParameterServer::get_type`), so this is the row that stops C++ being the odd one out — which is what stage 4 is."
  },
  "cpp:Node::get_parameter_types": {
    "verdict": "divergence",
    "disposition": "refuse-loud",
    "why": "upstream takes a `std::vector<std::string>` and returns a `std::vector<rclcpp::ParameterType>`; both need an allocator, and the freestanding lane has neither. Ours is `get_parameter_types(names, count, out)` over caller storage, plus the singular `get_parameter_type(name)`. An undeclared name answers `PARAMETER_NOT_SET`, as upstream's service does. RE-VERDICTED phase-417 W4.a. was `gap`, and the capability now SHIPS on `rclcpp::Node`, forwarding to the executor's one `nros_params::ParameterServer` and holding no state of its own. `refuse-loud` rather than `adopt`: the NAME exists, so a ported file reaches it, and the SIGNATURE is what refuses — the compiler names the mismatch at the call site and there is no shape in which this compiles and silently differs."
  },
  "cpp:Node::get_parameters": {
    "verdict": "declined",
    "disposition": "absent",
    "provides": [
      "nros::Node::get_parameter",
      "nros::Node::set_parameter",
      "nros::Node::describe_parameter",
      "nros::Node::set_parameters_atomically"
    ],
    "why": "RE-VERDICTED phase-417 W4.a. was `gap`. the read half: upstream returns `std::vector<rclcpp::Parameter>` for a vector of names. Every one of these is a vector of `rclcpp::Parameter` — a generated ROS message value object ledgered `declined` at `cpp:Parameter` — and a `std::vector` needs an allocator the freestanding lane does not have. The singular forms ship and reach the same store; the plural forms are declined because their ARGUMENT TYPE is, not because the capability is missing."
  },
  "cpp:Node::list_parameters": {
    "verdict": "divergence",
    "disposition": "refuse-loud",
    "why": "upstream returns an `rcl_interfaces::msg::ListParametersResult` and takes a vector of prefixes plus a `depth`. Ours writes into a caller-owned RECTANGLE (`max_names` rows of `name_stride` bytes) and always reports the TOTAL that matched, so `max_names = 0` counts — the only way a list of strings crosses this boundary with no allocator. ONE prefix and no `depth`: our names carry no separator semantics to recurse on. RE-VERDICTED phase-417 W4.a. was `gap`, and the capability now SHIPS on `rclcpp::Node`, forwarding to the executor's one `nros_params::ParameterServer` and holding no state of its own. `refuse-loud` rather than `adopt`: the NAME exists, so a ported file reaches it, and the SIGNATURE is what refuses — the compiler names the mismatch at the call site and there is no shape in which this compiles and silently differs."
  },
  "cpp:Node::parameters": {
    "verdict": "extension",
    "disposition": "absent",
    "why": "REMOVED, phase-426 W4 (2026-09-07). It was an ours-only accessor on the compat shim's `rclcpp::Node` handing back a reference to the node's own `nros::ParameterServer`, documented as \"the escape hatch to the node-owned store\" for \"the C-API helpers that take an `nros_parameter_server_t*`\" -- and no such helper exists: `nros_executor_register_parameter_services` takes the EXECUTOR, which `cpp:ParameterServer` had already recorded. With the node-local store deleted there is nothing to return and nothing that took it, so the name is gone rather than returning a reference to a store the services do not read. A node's parameters are reached through `declare_parameter` / `get_parameter` / `set_parameter`, which now name the one store. rclcpp declares no `parameters()` either, so removing it moves the ported surface TOWARD upstream. The row is kept, not deleted, so the removal is legible: a reader who finds `parameters()` in a pre-W4 port learns where it went."
  },
  "cpp:Node::remove_on_set_parameters_callback": {
    "verdict": "divergence",
    "disposition": "refuse-loud",
    "why": "the unregister half. Upstream takes the owning handle; ours takes the `rclcpp::ParameterCallbackHandle` token (a `uint16_t`) its registration returned. See `cpp:Node::add_on_set_parameters_callback`. RE-VERDICTED phase-417 W4.a. was `gap`, and the capability now SHIPS on `rclcpp::Node`, forwarding to the executor's one `nros_params::ParameterServer` and holding no state of its own. `refuse-loud` rather than `adopt`: the NAME exists, so a ported file reaches it, and the SIGNATURE is what refuses — the compiler names the mismatch at the call site and there is no shape in which this compiles and silently differs."
  },
  "cpp:Node::set_parameters": {
    "verdict": "declined",
    "disposition": "absent",
    "provides": [
      "nros::Node::get_parameter",
      "nros::Node::set_parameter",
      "nros::Node::describe_parameter",
      "nros::Node::set_parameters_atomically"
    ],
    "why": "RE-VERDICTED phase-417 W4.a. was `gap`. the non-atomic write half: upstream takes `std::vector<rclcpp::Parameter>` and returns a `std::vector<SetParametersResult>`, one verdict per element. `Node::set_parameters_atomically` DOES ship (phase-417 W4.a) over `rclcpp::ParameterWrite`, so the capability is reachable; what is declined is the per-element-result vector, which needs `SetParametersResult`. Every one of these is a vector of `rclcpp::Parameter` — a generated ROS message value object ledgered `declined` at `cpp:Parameter` — and a `std::vector` needs an allocator the freestanding lane does not have. The singular forms ship and reach the same store; the plural forms are declined because their ARGUMENT TYPE is, not because the capability is missing."
  },
  "cpp:Node::set_parameters_atomically": {
    "verdict": "divergence",
    "disposition": "refuse-loud",
    "why": "we SERVED `~/set_parameters_atomically` and offered it in no language, so the only way to reach our own atomic set was over the wire. Now `set_parameters_atomically(writes, count)` over `rclcpp::ParameterWrite` — a name plus a tagged value, which is one element of upstream's `std::vector<rclcpp::Parameter>` flattened into scalars, because `rclcpp::Parameter` is a generated message we do not have (`cpp:Parameter`) and the vector needs an allocator. Check-all-then-apply-all lives in the store (`ParameterServer::apply_atomically`), which is the same call the service handler makes, so the wire and the local call cannot disagree about what 'atomic' means. RE-VERDICTED phase-417 W4.a. was `gap`, and the capability now SHIPS on `rclcpp::Node`, forwarding to the executor's one `nros_params::ParameterServer` and holding no state of its own. `refuse-loud` rather than `adopt`: the NAME exists, so a ported file reaches it, and the SIGNATURE is what refuses — the compiler names the mismatch at the call site and there is no shape in which this compiles and silently differs."
  },
  "cpp:Node::undeclare_parameter": {
    "verdict": "divergence",
    "disposition": "adopt-bounded",
    "why": "SHIPPED phase-417 W4.a. `rclcpp::Node::undeclare_parameter(name)` forwards to `ParameterServer::remove` through `nros_cpp_node_undeclare_param`, on the one store, so the slot is freed for a later declaration and a sibling node's identical name is untouched. The envelope: upstream returns `void` and THROWS `ParameterNotDeclaredException`; ours returns `nros::Result` and answers `NOT_FOUND`, which is RFC-0089's error-channel rule (there are no exceptions here), not a choice made for this verb. A ported call site compiles either way; one that relies on the throw to skip the rest of a block does not."
  },
  "cpp:NodeOptions::allow_undeclared_parameters": {
    "verdict": "divergence",
    "disposition": "refuse-loud",
    "why": "`rclcpp::NodeOptions::allow_undeclared_parameters` -- REFUSE-LOUD, per-CONCEPT with the rest of the option family (`cpp:NodeOptions` carries the reasoning; this row carries the SITE).\n\nSITE: `packages/api/nros-cpp/include/nros/options.hpp:278` (setter) and `:282` (getter). Each is a `template <typename T = void>` whose body is `static_assert(detail::refuse<T>::value, NROS_RCLCPP_REFUSE_NODE_OPTIONS)` -- dependent on `T` so it fires on USE, not on include, and a caller writes the chain exactly as upstream does. Probe: `packages/api/nros-cpp/tests/compile/ros2_refuse_node_options_probe.cpp`.\n\nHOW A USER FINDS OUT: COMPILE ERROR at the call carrying the constraint and the alternative. For this member: the parameter store is fixed-capacity and `declare_parameter<T>(name, default)` is the only way a name enters it -- an undeclared name has no storage to land in.\n\nphase-428 Q1 (2026-09-06): this member used to INHERIT its disposition from `cpp:NodeOptions` and had no row of its own, which is how a live refusal stayed invisible to the ledger -- a refusal is a declaration, it correlates `same`, and `--check` stopped asking. A refusal site now gets its own row so `--require-disposition` can see it."
  },
  "cpp:NodeOptions::append_parameter_override": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "phase-417 stage 0 — visible only because the rclcpp adapter (then `rclcpp_compat.hpp`; deleted at stage 6 step A, each declaration moved to the header owning its concept) became a translation unit (issue 1020). The shim ships this TYPE as a name, so its members stop inheriting \"we do not have this type\" and become their own claim. Not a new gap: a gap INSIDE a type we ship is a different statement from one about a type we do not. This member is NOT declared here at all: `packages/api/nros-cpp/include/nros/options.hpp` refuses the ten option setter/getter pairs a tutorial chains (`arguments`, `use_intra_process_comms`, `enable_rosout`, ...) with a dependent `static_assert` carrying `NROS_RCLCPP_REFUSE_NODE_OPTIONS`, and declares nothing else on the type. So a ported call to THIS one gets `no member named` -- honest, and it names neither the constraint nor the alternative. phase-428 Q1 (2026-09-06): this row used to say it was a `= delete` overload; it never was, and the disposition is corrected from refuse-loud to absent. The refusal that does exist is per-CONCEPT and `cpp:NodeOptions` carries it."
  },
  "cpp:NodeOptions::automatically_declare_parameters_from_overrides": {
    "verdict": "divergence",
    "disposition": "refuse-loud",
    "why": "`rclcpp::NodeOptions::automatically_declare_parameters_from_overrides` -- REFUSE-LOUD, per-CONCEPT with the rest of the option family (`cpp:NodeOptions` carries the reasoning; this row carries the SITE).\n\nSITE: `packages/api/nros-cpp/include/nros/options.hpp:288` (setter) and `:292` (getter). Each is a `template <typename T = void>` whose body is `static_assert(detail::refuse<T>::value, NROS_RCLCPP_REFUSE_NODE_OPTIONS)` -- dependent on `T` so it fires on USE, not on include, and a caller writes the chain exactly as upstream does. Probe: `packages/api/nros-cpp/tests/compile/ros2_refuse_node_options_probe.cpp`.\n\nHOW A USER FINDS OUT: COMPILE ERROR at the call carrying the constraint and the alternative. For this member: overrides arrive as launcher-projected environment, and `declare_parameter<T>` reads them at the declaration; there is no override table to walk.\n\nphase-428 Q1 (2026-09-06): this member used to INHERIT its disposition from `cpp:NodeOptions` and had no row of its own, which is how a live refusal stayed invisible to the ledger -- a refusal is a declaration, it correlates `same`, and `--check` stopped asking. A refusal site now gets its own row so `--require-disposition` can see it."
  },
  "cpp:NodeOptions::parameter_event_qos": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "phase-417 stage 0 — visible only because the rclcpp adapter (then `rclcpp_compat.hpp`; deleted at stage 6 step A, each declaration moved to the header owning its concept) became a translation unit (issue 1020). The shim ships this TYPE as a name, so its members stop inheriting \"we do not have this type\" and become their own claim. Not a new gap: a gap INSIDE a type we ship is a different statement from one about a type we do not. This member is NOT declared here at all: `packages/api/nros-cpp/include/nros/options.hpp` refuses the ten option setter/getter pairs a tutorial chains (`arguments`, `use_intra_process_comms`, `enable_rosout`, ...) with a dependent `static_assert` carrying `NROS_RCLCPP_REFUSE_NODE_OPTIONS`, and declares nothing else on the type. So a ported call to THIS one gets `no member named` -- honest, and it names neither the constraint nor the alternative. phase-428 Q1 (2026-09-06): this row used to say it was a `= delete` overload; it never was, and the disposition is corrected from refuse-loud to absent. The refusal that does exist is per-CONCEPT and `cpp:NodeOptions` carries it."
  },
  "cpp:NodeOptions::parameter_overrides": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "phase-417 stage 0 — visible only because the rclcpp adapter (then `rclcpp_compat.hpp`; deleted at stage 6 step A, each declaration moved to the header owning its concept) became a translation unit (issue 1020). The shim ships this TYPE as a name, so its members stop inheriting \"we do not have this type\" and become their own claim. Not a new gap: a gap INSIDE a type we ship is a different statement from one about a type we do not. This member is NOT declared here at all: `packages/api/nros-cpp/include/nros/options.hpp` refuses the ten option setter/getter pairs a tutorial chains (`arguments`, `use_intra_process_comms`, `enable_rosout`, ...) with a dependent `static_assert` carrying `NROS_RCLCPP_REFUSE_NODE_OPTIONS`, and declares nothing else on the type. So a ported call to THIS one gets `no member named` -- honest, and it names neither the constraint nor the alternative. phase-428 Q1 (2026-09-06): this row used to say it was a `= delete` overload; it never was, and the disposition is corrected from refuse-loud to absent. The refusal that does exist is per-CONCEPT and `cpp:NodeOptions` carries it."
  },
  "cpp:OnSetParametersCallbackType": {
    "verdict": "divergence",
    "why": "Upstream's is a `std::function<SetParametersResult(const std::vector<Parameter>&)>`. Ours is a plain function pointer plus a `void*` context over one `rclcpp::ParameterWrite`, answering `bool` — upstream's `successful`. The allocator is the constraint for the closure and the vector, and `<functional>` is absent from the freestanding lane entirely. phase-417 W4.a."
  },
  "cpp:Parameter": {
    "verdict": "declined",
    "disposition": "absent",
    "provides": [
      "nros::ParameterServer::get_parameter",
      "nros::ParameterServer::set_parameter"
    ],
    "why": "rclcpp's parameter VALUE OBJECT: a name plus a type-tagged value, copied around by value and the currency of `set_parameters`, `get_parameters` and the on-set callback. Every accessor on it (`as_string`, `as_double_array`, `value_to_string`, `to_parameter_msg`) returns an owned `std::string` or `std::vector`, so the type cannot exist without an allocator. Our C++ passes values through the typed template methods instead (`ParameterServer::get_parameter<T>(name, out&)`), which is the out-param form of the same read. What a user loses: a heterogeneous parameter list. Note C and Rust both DO have the record type (`nros_parameter_t`, `nros_params::Parameter`) -- theirs holds the value inline rather than owning it."
  },
  "cpp:ParameterCallbackHandle": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "the owning handle `ParameterEventHandler::add_parameter_callback` returns; dropping it unregisters. the `/parameter_events` family. We publish no `/parameter_events` topic at all -- `QOS_PROFILE_PARAMETER_EVENTS` is defined in `nros-rmw/src/traits.rs` and has no publisher behind it -- so there is nothing for an event handler to subscribe to. Bringing it back needs a `std::function`-shaped callback registry and a shared_ptr handle per registration, neither of which exists without an allocator. What a user loses: reacting to ANOTHER node's parameter change. Phase 379 W3."
  },
  "cpp:ParameterCallbackHandle::*": {
    "verdict": "declined",
    "bucket": "theirs-only",
    "disposition": "absent",
    "provides": [
      "nros::Node::add_on_set_parameters_callback",
      "nros::Node::remove_on_set_parameters_callback"
    ],
    "why": "The `make_shared` / `make_unique` factories rclcpp generates for its owning callback handle. Ours is `rclcpp::ParameterCallbackHandle = uint16_t`, a plain token: with no allocator there is nothing to own and nothing whose destruction could unregister, so `remove_on_set_parameters_callback(handle)` is explicit. A factory for an integer would be a name with nothing behind it. phase-417 W4.a."
  },
  "cpp:ParameterDescriptor": {
    "verdict": "divergence",
    "why": "Upstream's is `rcl_interfaces::msg::ParameterDescriptor`, a generated ROS message whose strings OWN their storage. Ours BORROWS its text: the `const char*` are read during the call that consumes the descriptor — the store copies into its own slot — and never retained, so a descriptor is a string-literal aggregate on the stack with no allocator anywhere. Reading one back is the mirror of that and takes a caller-owned text buffer (`Node::describe_parameter`). The platform constraint is the allocator; there is no shape in which a returned descriptor owns its strings without one. phase-417 W4.a."
  },
  "cpp:ParameterEventCallbackHandle": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "the whole-event form of `cpp:ParameterCallbackHandle`. the `/parameter_events` family. We publish no `/parameter_events` topic at all -- `QOS_PROFILE_PARAMETER_EVENTS` is defined in `nros-rmw/src/traits.rs` and has no publisher behind it -- so there is nothing for an event handler to subscribe to. Bringing it back needs a `std::function`-shaped callback registry and a shared_ptr handle per registration, neither of which exists without an allocator. What a user loses: reacting to ANOTHER node's parameter change. Phase 379 W3."
  },
  "cpp:ParameterEventHandler": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "the `/parameter_events` family. We publish no `/parameter_events` topic at all -- `QOS_PROFILE_PARAMETER_EVENTS` is defined in `nros-rmw/src/traits.rs` and has no publisher behind it -- so there is nothing for an event handler to subscribe to. Bringing it back needs a `std::function`-shaped callback registry and a shared_ptr handle per registration, neither of which exists without an allocator. What a user loses: reacting to ANOTHER node's parameter change. Phase 379 W3."
  },
  "cpp:ParameterEventsQoS::ParameterEventsQoS": {
    "verdict": "declined",
    "disposition": "adopt",
    "why": "the QoS preset for `/parameter_events`. We define the profile (`QOS_PROFILE_PARAMETER_EVENTS`, reachable from Rust as `QoSProfile::parameter_events_default`) and publish nothing on the topic. the `/parameter_events` family. We publish no `/parameter_events` topic at all -- `QOS_PROFILE_PARAMETER_EVENTS` is defined in `nros-rmw/src/traits.rs` and has no publisher behind it -- so there is nothing for an event handler to subscribe to. Bringing it back needs a `std::function`-shaped callback registry and a shared_ptr handle per registration, neither of which exists without an allocator. What a user loses: reacting to ANOTHER node's parameter change. Phase 379 W3. COLLAPSE CHECK 2026-09-04 (phase-417): the TYPE now ships (`rclcpp::ParameterEventsQoS`, phase-417 W3.f, ADOPT), so this correlates `same` on the PORTED surface. THE DECLINE RECORDED HERE IS ABOUT THE TOPIC AND IT STANDS: nano-ros publishes no `/parameter_events`, so there is still nothing for an event handler to subscribe to and a user still cannot react to another node's parameter change."
  },
  "cpp:ParameterMap": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "`std::unordered_map<std::string, std::vector<Parameter>>` -- what `parameter_map_from_yaml_file` produces. Needs an allocator and `cpp:Parameter`. `rcl_yaml_param_parser` exists to parse a `--params-file` off a FILESYSTEM at node start and hand rcl a tree of overrides. A device image has no filesystem and no YAML parser; parameter defaults are baked into the image at build time (RFC-0045) and changed afterwards over `~/set_parameters`. What a user loses is `--params-file` and everything that reads one."
  },
  "cpp:ParameterServer": {
    "verdict": "divergence",
    "why": "RETIRED 2026-09-11 (phase-426 W4, second half): `nros::ParameterServer<Capacity, SeqSlots, SeqPoolBytes>` IS DELETED. What follows is the reasoning that produced the row and then retired it; the class no longer exists, so nothing here is a claim about today's surface. It was a fixed-capacity, node-local typed parameter store, storage inline, no heap, over the LEGACY caller-storage C `nros_parameter_server_t` -- which means its parameters were NOT what `ros2 param` sees: the six `rcl_interfaces/srv/*` servers read a DIFFERENT object, the `nros_params::ParameterServer` the executor owns, and nothing joined them. rclcpp has no such type because rclcpp's parameters live ON the node and the node allocates. THREE FACTS DECIDED THE RETIREMENT, all measured. (1) NO CONSUMER. After the first half of W4 deleted both node-owned C++ stores, `grep -rn 'ParameterServer<' packages examples book docs` found the class constructed in exactly one place in the tree: `examples/native/cpp/parameters`, the example that documented it. A caller-owned store with no executor is a capability we had no caller for. (2) IT WAS THE DEFECT, SHIPPED. An example is COPIED OUT (RFC-0026), so the shipped answer to \"how do I use parameters in C++\" was the store `ros2 param get` cannot read -- which is the whole of what phase-426 exists to remove. (3) IT IS WRAPPER-SIDE BEHAVIOUR. RFC-0019/0020 and RFC-0089 §\"Parameters\" put storage, typing, the read-only and range rules and the node keying in Rust and leave the C++ API a call-shape adapter; a ~600-line header with a name table and a bump-allocated element pool is the second implementation that rule forbids. Disposition is ABSENT (RFC-0089's fourth), which is available here because upstream never had the name -- the alias rule only binds where rclcpp HAS a name and we lack it. The capability moved rather than went: `nros::Seq<T, N>` survives as a VALUE (see `cpp:Seq`) and now declares and reads through the node API onto the executor's store, so a freestanding sequence parameter is visible to `ros2 param get` for the first time -- measured 2026-09-11, `ros2 param get /cpp_parameters mpc_weights` -> `array('d', [4.0, 5.0, 6.0, 7.0])`. The `raw()` accessor went with the class and nothing mourned it: it was documented for \"C-API helpers (e.g. ROS 2 service registration)\" that do not exist -- `nros_executor_register_parameter_services` takes the executor, never a standalone store. The C `nros_parameter_*` family it wrapped is UNCHANGED and still shipped; what changed is that no example demonstrates it (`check-example-parameter-stores`). Phase 379 W3, retired phase-426 W4."
  },
  "cpp:ParameterService": {
    "verdict": "divergence",
    "why": "rclcpp wraps the parameter SERVERS in an object a node owns; ours is a registration call on the executor, because the executor owns the one parameter store there is. See `c:executor_register_parameter_services` and the service stage's `cpp:ParameterService::ParameterService`."
  },
  "cpp:ParameterTypeException": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "thrown when a `Parameter` is read as the wrong type. nros-cpp is built `-fno-exceptions` (RFC-0018); every failure here is an `nros::Result` carrying an `nros_ret_t`, and a type mismatch is `NROS_RET_INVALID_ARGUMENT`."
  },
  "cpp:ParameterValue": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "the value half of `cpp:Parameter`, with the same allocator problem: `get<std::string>()` and `to_value_msg()` both hand back owned storage. Rust's `ParameterValue` is the no-allocator equivalent (heapless `String`/`Vec`, accessors that return borrows); C++ has no counterpart."
  },
  "cpp:ParameterWrite": {
    "verdict": "divergence",
    "why": "The proposed write an on-set-parameters callback sees, and the element type of `set_parameters_atomically`. Upstream passes a `std::vector<rclcpp::Parameter>`; `rclcpp::Parameter` is a generated message value object ledgered `declined` (`cpp:Parameter`) and the vector needs an allocator, so ours is one element of that vector flattened into scalars a freestanding TU can read. `#[repr(C)]` in `params_shim.rs` and emitted by cbindgen, so it is GENERATED rather than hand-mirrored — `check-ffi-struct-mirrors`' class cannot arise from it. phase-417 W4.a."
  },
  "cpp:Seq": {
    "verdict": "divergence",
    "why": "`nros::Seq<T, N>` -- a fixed-capacity inline sequence, the `no_std` stand-in for `std::vector<T>` at the parameter surface, so a ported rclcpp node that declares a `std::vector<double>` weight matrix (RFC-0044 Q3, phase 242.3) compiles with no heap. Constraint: no allocator. Over-capacity is rejected or flagged (`overflowed()`), never UB. Under `NROS_CPP_STD` it converts to and from `std::vector<T>` so hosted source is unchanged. rclcpp needs no counterpart because `std::vector` is always available to it. Element types are `double`/`int64_t`/`bool` only -- there is no `Seq` path for the `byte_array` or `string_array` parameter types the C layer underneath does support (`nros_parameter_declare_byte_array`, `nros_parameter_declare_string_array`), so C++ reaches 8 of the 10 `rcl_interfaces` types. PHASE-426 W4 (2026-09-11) -- WHAT IT IS A VALUE FOR CHANGED, and this is the reason the type outlived the class it shipped with. `nros::ParameterServer<Cap>` is deleted (see that row); `Seq` was its element-carrying value and is now the node API's: `node.declare_parameter(name, Seq<double, 8>{...})` / `get_parameter(name, seq)` / `set_parameter(name, seq)` route through `nros_cpp_node_{declare,get,set}_param_{double,integer,bool}_array` onto the executor's store, so a sequence parameter is visible to `ros2 param get` like every scalar. Three things follow. The ELEMENTS are owned by the store (a `heapless::Vec` in its slot), not by an inline bump pool in the header, so there is no borrow for the caller to outlive -- `ParameterServer`'s `seq_pool_` left with the class. The overloads sit OUTSIDE `NROS_CPP_STD`, deliberately: `Seq` is the array form a `-nostdinc++` node has, because `std::vector<T>` reaches the same FFI only under that macro. And a read into a too-small `Seq` is refused (`ErrorCode::Full`) rather than truncated, matching the `std::vector` read's two-call length probe -- a short weight matrix is a plausible wrong answer, which is worse than an error. `Seq<bool, N>` exists where `std::vector<bool>` cannot: its storage is a plain `bool[N]`, so it has a `data()`. Phase 379 W3."
  },
  "cpp:SyncParametersClient": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "the CLIENT side of the parameter services -- a service client speaking `rcl_interfaces/srv/*` on someone else's behalf. Ours registers the SERVER side (`nros_executor_register_parameter_services`) and ships no client: a device sets its own parameters from baked config (RFC-0045) and is the thing a desktop tool queries, not the thing that queries. See the param stage for the server half. The sync form additionally blocks on its own spin, which RFC-0021 forbids."
  },
  "cpp:_to_json_dict_entry": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "rclcpp's JSON formatter for a parameter value, used by `Parameter::value_to_string`. It builds a `std::string` per entry; with no allocator there is nothing to build it in, and an image has no consumer for JSON. See `cpp:Parameter`."
  },
  "cpp:declared_param_exempt": {
    "verdict": "extension",
    "why": "the names no contract has to declare -- `use_sim_time`, `start_type_description_service` and `qos_overrides.*` -- which are exactly the names play_launch exempts when it holds a launch file to the same contract. See `cpp:declared_param_node`."
  },
  "cpp:declared_param_node": {
    "verdict": "extension",
    "why": "phase-446 W6. The declaring node for a namespace + name in the generated `declared_params` table (`nros/declared_params.hpp`), or `nullptr` when the node's launch contract has no `params:`. rclcpp has no counterpart: its parameters are declared nowhere outside the code, so there is nothing to check a declaration against. Ours are declared in the launch contract because the parameter store is a fixed table sized from that contract at build time (no allocator to grow it, phase-446 W4), and `ComponentNode::declare_parameter` uses this lookup to refuse, at boot, a declaration the contract does not make. `constexpr`, so `declared_params.cpp` asserts its answers against a rendered fixture."
  },
  "cpp:declared_param_type": {
    "verdict": "extension",
    "why": "the declared type (the `rcl_interfaces/msg/ParameterType` code) of one parameter on a declaring node, or `DECLARED_PARAM_UNDECLARED`. See `cpp:declared_param_node`."
  },
  "cpp:declared_param_type_name": {
    "verdict": "extension",
    "why": "the contract's spelling of a type code, for the boot diagnostic that names the declared and the passed type. See `cpp:declared_param_node`."
  },
  "cpp:parameter_descriptor": {
    "verdict": "extension",
    "why": "A `ParameterDescriptor` with nothing set. C++14 has no designated initialisers, so `auto d = rclcpp::parameter_descriptor(); d.read_only = true;` is how a caller builds one field at a time — and this header must parse at C++14 (PX4 modules build `-std=gnu++14 -Werror`). Upstream has no correspondent because its descriptor is a generated message with a default constructor. phase-417 W4.a."
  },
  "cpp:parameter_map_from": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "converts an `rcl_params_t` into a `cpp:ParameterMap`. `rcl_yaml_param_parser` exists to parse a `--params-file` off a FILESYSTEM at node start and hand rcl a tree of overrides. A device image has no filesystem and no YAML parser; parameter defaults are baked into the image at build time (RFC-0045) and changed afterwards over `~/set_parameters`. What a user loses is `--params-file` and everything that reads one."
  },
  "cpp:parameter_map_from_yaml_file": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "reads a params file off disk. `rcl_yaml_param_parser` exists to parse a `--params-file` off a FILESYSTEM at node start and hand rcl a tree of overrides. A device image has no filesystem and no YAML parser; parameter defaults are baked into the image at build time (RFC-0045) and changed afterwards over `~/set_parameters`. What a user loses is `--params-file` and everything that reads one."
  },
  "cpp:parameter_value_from": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "converts one `rcl_variant_t` into a `cpp:ParameterValue`. `rcl_yaml_param_parser` exists to parse a `--params-file` off a FILESYSTEM at node start and hand rcl a tree of overrides. A device image has no filesystem and no YAML parser; parameter defaults are baked into the image at build time (RFC-0045) and changed afterwards over `~/set_parameters`. What a user loses is `--params-file` and everything that reads one."
  },
  "rust:AvailableValues": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "the input to rclrs's initial-value discriminator: the set of candidate values a declaration may choose from (override, prior value, default). rclrs's parameter OVERRIDE machinery: a value supplied outside the code (`--ros-args -p`, a params file, a prior declaration) that the declaration then has to reconcile with. Overrides are resolved BEFORE the image runs here -- the launch resolver bakes them into the SystemModel and the generated entry seeds them into the executor store before any component constructor runs (RFC-0045, issue 0745) -- so by declare time there is no second value to reconcile against. Constraint: no runtime env, no argv."
  },
  "rust:DeclarationError": {
    "verdict": "divergence",
    "disposition": "adopt-bounded",
    "why": "SHIPPED phase-417 W4.a. `nros_params::DeclarationError`, returned by `ParameterServer::try_declare` / `try_declare_with_descriptor` and exported from the `nros` facade. `declare` returned a bare `bool` for 'the name was taken', 'the table was full' and 'the default is outside its range' alike; the `bool` spellings now forward to these, so there is one declare path. The envelope: OUR VARIANT SET IS A SUBSET of rclrs's, and the difference is structural rather than an omission. Ours are `AlreadyDeclared`, `InvalidRange`, `InitialValueOutOfRange` (all three rclrs's), plus `StorageFull` and `NameTooLong`, which rclrs has no correspondent for because its store grows and ours is the storage the caller placed. rclrs's `NoValueAvailable`, `OverrideValueTypeMismatch` and `PriorValueTypeMismatch` are about a launch OVERRIDE arriving before the declaration and disagreeing with it; nano-ros seeds overrides through the same `ParameterServer::apply` a remote set uses, so a disagreeing override is a `SetParameterResult` on the write that carried it and cannot reach a declaration."
  },
  "rust:DeclarationError::*": {
    "verdict": "extension",
    "bucket": "ours-only",
    "why": "Whatever the derive gives the enum. The type itself is `rust:DeclarationError`. phase-417 W4.a."
  },
  "rust:Executor::*parameter*": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "The parameter surface C and C++ forward to, and the Rust half of stage 4. OURS-ONLY BY RECEIVER: rclrs puts these on `Node`, and our store belongs to the EXECUTOR because an image composes several nodes onto one executor and there is no allocator to give each node a table (phase-426 W1). So each verb carries an `_on` spelling naming the node and a bare one meaning the primary — the pair every other parameter verb on the executor already had. Every member forwards to `nros_params::ParameterServer` and holds no state; the wrappers forward HERE, which is what keeps all three languages on one store. phase-417 W4.a."
  },
  "rust:Executor::*params*": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "See `rust:Executor::*parameter*` — the same family, for the members whose name is plural. phase-417 W4.a."
  },
  "rust:Executor::allow_undeclared_parameters": {
    "verdict": "divergence",
    "why": "upstream's `allow_undeclared_parameters` (rclcpp `NodeOptions`, rclrs `allow_undeclared`) is a NODE OPTION set at construction. We have no `NodeOptions` object to carry it -- a node here is a row in the executor's fixed table, not a constructed object with an options struct -- so it is a call on the executor instead. The SEMANTICS are upstream's, per node and off by default (issue 1151): with it on, a remote set may declare a name the node never declared; with it off the set is refused with rclrs's reason. This spelling means the executor's primary node; `allow_undeclared_parameters_on` names another. What a user loses: it cannot be given at node-construction time, so it must be set before the six services start answering."
  },
  "rust:Executor::allow_undeclared_parameters_on": {
    "verdict": "divergence",
    "why": "the node-naming half of `rust:Executor::allow_undeclared_parameters`. phase-426 W1 -- the parameter STORE is one fixed table owned by the EXECUTOR, not a container per node: there is no allocator to make a second, so a node cannot own its own. Upstream's parameter methods are on the node object itself (`rclcpp::Node::declare_parameter`, rclrs's `Node::declare`), which is how upstream keys them; ours are on the executor and the node is an ARGUMENT, because an image composes several nodes onto one executor (RFC-0047) and the shared table has to be told whose parameter it is. The `_on(NodeId, ...)` spelling is the same disambiguation the entity builders already use (`node_mut(id)` / `_on(...)`, phase 104.C). What a user loses: a ported node writes `executor.declare_parameter_on(node_id, name, value)` where rclcpp writes `node->declare_parameter(name, value)`."
  },
  "rust:Executor::declare_parameter": {
    "verdict": "divergence",
    "why": "an `Executor::register_*` entry point: the imperative half of what the component model declares. The executor owns the static entity table, so registration is a call on it rather than a method on a node the entity outlives. part of the IMPERATIVE Rust API, which coexists with the declarative component model and is the rclrs-shaped one: `NodeHandle` (exported from `nros` and IN THE PRELUDE, behind `rmw-cffi`) hands back concrete entities you hold and call. rclrs's counterpart exists and correlates by concept but not by name, because ours carries the storage strategy in the type -- `Embedded*` means the entity lives in the executor's static table rather than behind an `Arc`, which is the no-allocator constraint made visible in the name. Whether the two Rust APIs should both be public, and which one `nros::` should lead with, is issue 0784. See issue 0793 for the parameter stores these reach."
  },
  "rust:Executor::declare_parameter_on": {
    "verdict": "divergence",
    "why": "the node-naming half of `rust:Executor::declare_parameter`. phase-426 W1 -- the parameter STORE is one fixed table owned by the EXECUTOR, not a container per node: there is no allocator to make a second, so a node cannot own its own. Upstream's parameter methods are on the node object itself (`rclcpp::Node::declare_parameter`, rclrs's `Node::declare`), which is how upstream keys them; ours are on the executor and the node is an ARGUMENT, because an image composes several nodes onto one executor (RFC-0047) and the shared table has to be told whose parameter it is. The `_on(NodeId, ...)` spelling is the same disambiguation the entity builders already use (`node_mut(id)` / `_on(...)`, phase 104.C). What a user loses: a ported node writes `executor.declare_parameter_on(node_id, name, value)` where rclcpp writes `node->declare_parameter(name, value)`."
  },
  "rust:Executor::declare_parameter_with_descriptor": {
    "verdict": "divergence",
    "why": "an `Executor::register_*` entry point: the imperative half of what the component model declares. The executor owns the static entity table, so registration is a call on it rather than a method on a node the entity outlives. part of the IMPERATIVE Rust API, which coexists with the declarative component model and is the rclrs-shaped one: `NodeHandle` (exported from `nros` and IN THE PRELUDE, behind `rmw-cffi`) hands back concrete entities you hold and call. rclrs's counterpart exists and correlates by concept but not by name, because ours carries the storage strategy in the type -- `Embedded*` means the entity lives in the executor's static table rather than behind an `Arc`, which is the no-allocator constraint made visible in the name. Whether the two Rust APIs should both be public, and which one `nros::` should lead with, is issue 0784. See issue 0793 for the parameter stores these reach."
  },
  "rust:Executor::declare_parameter_with_descriptor_on": {
    "verdict": "divergence",
    "why": "the node-naming half of `rust:Executor::declare_parameter_with_descriptor`. phase-426 W1 -- the parameter STORE is one fixed table owned by the EXECUTOR, not a container per node: there is no allocator to make a second, so a node cannot own its own. Upstream's parameter methods are on the node object itself (`rclcpp::Node::declare_parameter`, rclrs's `Node::declare`), which is how upstream keys them; ours are on the executor and the node is an ARGUMENT, because an image composes several nodes onto one executor (RFC-0047) and the shared table has to be told whose parameter it is. The `_on(NodeId, ...)` spelling is the same disambiguation the entity builders already use (`node_mut(id)` / `_on(...)`, phase 104.C). What a user loses: a ported node writes `executor.declare_parameter_on(node_id, name, value)` where rclcpp writes `node->declare_parameter(name, value)`."
  },
  "rust:Executor::get_parameter": {
    "verdict": "divergence",
    "why": "an `Executor::register_*` entry point: the imperative half of what the component model declares. The executor owns the static entity table, so registration is a call on it rather than a method on a node the entity outlives. part of the IMPERATIVE Rust API, which coexists with the declarative component model and is the rclrs-shaped one: `NodeHandle` (exported from `nros` and IN THE PRELUDE, behind `rmw-cffi`) hands back concrete entities you hold and call. rclrs's counterpart exists and correlates by concept but not by name, because ours carries the storage strategy in the type -- `Embedded*` means the entity lives in the executor's static table rather than behind an `Arc`, which is the no-allocator constraint made visible in the name. Whether the two Rust APIs should both be public, and which one `nros::` should lead with, is issue 0784. See issue 0793 for the parameter stores these reach."
  },
  "rust:Executor::get_parameter_integer": {
    "verdict": "divergence",
    "why": "an `Executor::register_*` entry point: the imperative half of what the component model declares. The executor owns the static entity table, so registration is a call on it rather than a method on a node the entity outlives. part of the IMPERATIVE Rust API, which coexists with the declarative component model and is the rclrs-shaped one: `NodeHandle` (exported from `nros` and IN THE PRELUDE, behind `rmw-cffi`) hands back concrete entities you hold and call. rclrs's counterpart exists and correlates by concept but not by name, because ours carries the storage strategy in the type -- `Embedded*` means the entity lives in the executor's static table rather than behind an `Arc`, which is the no-allocator constraint made visible in the name. Whether the two Rust APIs should both be public, and which one `nros::` should lead with, is issue 0784. See issue 0793 for the parameter stores these reach."
  },
  "rust:Executor::get_parameter_on": {
    "verdict": "divergence",
    "why": "the node-naming half of `rust:Executor::get_parameter`. phase-426 W1 -- the parameter STORE is one fixed table owned by the EXECUTOR, not a container per node: there is no allocator to make a second, so a node cannot own its own. Upstream's parameter methods are on the node object itself (`rclcpp::Node::declare_parameter`, rclrs's `Node::declare`), which is how upstream keys them; ours are on the executor and the node is an ARGUMENT, because an image composes several nodes onto one executor (RFC-0047) and the shared table has to be told whose parameter it is. The `_on(NodeId, ...)` spelling is the same disambiguation the entity builders already use (`node_mut(id)` / `_on(...)`, phase 104.C). What a user loses: a ported node writes `executor.declare_parameter_on(node_id, name, value)` where rclcpp writes `node->declare_parameter(name, value)`."
  },
  "rust:Executor::parameter": {
    "verdict": "divergence",
    "why": "an `Executor::register_*` entry point: the imperative half of what the component model declares. The executor owns the static entity table, so registration is a call on it rather than a method on a node the entity outlives. part of the IMPERATIVE Rust API, which coexists with the declarative component model and is the rclrs-shaped one: `NodeHandle` (exported from `nros` and IN THE PRELUDE, behind `rmw-cffi`) hands back concrete entities you hold and call. rclrs's counterpart exists and correlates by concept but not by name, because ours carries the storage strategy in the type -- `Embedded*` means the entity lives in the executor's static table rather than behind an `Arc`, which is the no-allocator constraint made visible in the name. Whether the two Rust APIs should both be public, and which one `nros::` should lead with, is issue 0784. See issue 0793 for the parameter stores these reach."
  },
  "rust:Executor::parameter_on": {
    "verdict": "divergence",
    "why": "the node-naming half of `rust:Executor::parameter`. phase-426 W1 -- the parameter STORE is one fixed table owned by the EXECUTOR, not a container per node: there is no allocator to make a second, so a node cannot own its own. Upstream's parameter methods are on the node object itself (`rclcpp::Node::declare_parameter`, rclrs's `Node::declare`), which is how upstream keys them; ours are on the executor and the node is an ARGUMENT, because an image composes several nodes onto one executor (RFC-0047) and the shared table has to be told whose parameter it is. The `_on(NodeId, ...)` spelling is the same disambiguation the entity builders already use (`node_mut(id)` / `_on(...)`, phase 104.C). What a user loses: a ported node writes `executor.declare_parameter_on(node_id, name, value)` where rclcpp writes `node->declare_parameter(name, value)`."
  },
  "rust:Executor::set_parameter": {
    "verdict": "divergence",
    "why": "phase-426 W2 -- THE writer, and it was missing: `SetParameters` and `SetParametersAtomically` were registered services with no store-side writer any wrapper could reach (`grep -c 'fn set_parameter'` in `nros-node` was 0), so the C facade wrote the slot through `ParameterServer::set` directly. It routes through `ParameterServer::apply`, so an in-image set gets exactly the rules a remote `ros2 param set` gets -- read-only, type, min/max/step, and issue 1151's refusal of an undeclared name. rclcpp's `Node::set_parameter` is the correspondent; the receiver differs for the reason below. phase-426 W1 -- the parameter STORE is one fixed table owned by the EXECUTOR, not a container per node: there is no allocator to make a second, so a node cannot own its own. Upstream's parameter methods are on the node object itself (`rclcpp::Node::declare_parameter`, rclrs's `Node::declare`), which is how upstream keys them; ours are on the executor and the node is an ARGUMENT, because an image composes several nodes onto one executor (RFC-0047) and the shared table has to be told whose parameter it is. The `_on(NodeId, ...)` spelling is the same disambiguation the entity builders already use (`node_mut(id)` / `_on(...)`, phase 104.C). What a user loses: a ported node writes `executor.declare_parameter_on(node_id, name, value)` where rclcpp writes `node->declare_parameter(name, value)`. part of the IMPERATIVE Rust API, which coexists with the declarative component model and is the rclrs-shaped one. Whether the two Rust APIs should both be public, and which one `nros::` should lead with, is issue 0784."
  },
  "rust:Executor::set_parameter_on": {
    "verdict": "divergence",
    "why": "the node-naming half of `rust:Executor::set_parameter`. phase-426 W1 -- the parameter STORE is one fixed table owned by the EXECUTOR, not a container per node: there is no allocator to make a second, so a node cannot own its own. Upstream's parameter methods are on the node object itself (`rclcpp::Node::declare_parameter`, rclrs's `Node::declare`), which is how upstream keys them; ours are on the executor and the node is an ARGUMENT, because an image composes several nodes onto one executor (RFC-0047) and the shared table has to be told whose parameter it is. The `_on(NodeId, ...)` spelling is the same disambiguation the entity builders already use (`node_mut(id)` / `_on(...)`, phase 104.C). What a user loses: a ported node writes `executor.declare_parameter_on(node_id, name, value)` where rclcpp writes `node->declare_parameter(name, value)`."
  },
  "rust:FloatingPointRange*": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "The floating-point half; see `rust:IntegerRange*`. phase-417 W4.a."
  },
  "rust:IntegerRange*": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "The two halves of `ParameterRange`, exported from the `nros` facade since phase-417 W4.a. rclrs's `ParameterRange<T>` is ONE generic type over `i64` and `f64`; ours is an enum over two concrete structs, because `nros-params` is `no_std` and a monomorphised range inside a `#[repr]`-stable descriptor slot would multiply the slot by the number of instantiations. `new` / `is_valid` / `contains` are the predicates the store runs on every declare and every set (min, max AND step -- step since issue 1150), so a range means the same thing to a local set and to a remote `ros2 param set`. The aggregate rclrs builds out of them is `rust:ParameterRanges`."
  },
  "rust:MandatoryParameter::set": {
    "verdict": "divergence",
    "why": "ours is `set(&mut self, T)`, rclrs's is `set(&self, U)`. rclrs can take `&self` because the value lives behind the node's `Arc<Mutex<...>>`, so a handle is a shared reference into shared state; ours holds `&'a mut ParameterServer` directly. Constraint: no allocator and no `Arc` -- with one executor per RTOS task (RFC-0002) the store is owned, not shared, so mutation is an exclusive borrow the compiler checks rather than a lock taken at runtime. The value-type difference is the same distinction: ours takes `T` exactly, rclrs takes any `U: Into<T>`."
  },
  "rust:Node::declare_parameter": {
    "verdict": "divergence",
    "why": "rclrs declares a parameter on a live `Arc<Node>` and hands back a typed handle. Ours is `DeclaredNode::declare_parameter_for_name[_with_default]`, called during `register`. the Rust API is a DECLARATIVE COMPONENT MODEL, not rclrs's handle model (RFC-0043/0044). A user implements `Node`/`ExecutableNode` for a type, declares entities in `register`, and receives callbacks; rclrs holds an `Arc<Node>` and calls methods on it. The constraint is static entity storage plus an executor that owns dispatch: with no allocator the entity set has to be known at declaration time."
  },
  "rust:Node::use_undeclared_parameters": {
    "verdict": "divergence",
    "disposition": "absent",
    "provides": [
      "nros::Executor::use_undeclared_parameters",
      "nros::Executor::use_undeclared_parameters_on"
    ],
    "why": "RE-VERDICTED phase-417 W4.a. was `gap`, and the capability SHIPS: `Executor::use_undeclared_parameters[_on]` hands out the handle, which now writes as well as reads. The RECEIVER is what diverges and it is structural — the store belongs to the EXECUTOR, because an image composes several nodes onto one executor and there is no allocator to give each node a table (phase-426 W1). rclrs's `Node` owns its own. So the `_on` spelling names the node and the bare one means the primary, which is the same pair every other parameter verb on the executor carries. `absent`, because the name does not exist on a node type."
  },
  "rust:OnSetParameterFn": {
    "verdict": "divergence",
    "why": "An accept/reject hook. rclrs takes a closure; this crate is `no_std` with no allocator so there is nothing to box a `FnMut` into, and this is a `fn` pointer plus an `OnSetContext`. `false` REFUSES the write, which is rclcpp's contract and the one C's legacy `nros_parameter_server_set_callback` already had on the wrong store. phase-417 W4.a."
  },
  "rust:OnSetParameterHandle*": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "The token `remove_on_set_parameters_callback` takes. rclcpp returns an owning `ParameterCallbackHandle` shared_ptr whose destruction unregisters; with no allocator there is nothing to own, so this is an opaque `u16` index into the bounded registry and unregistering is explicit. `raw` / `from_raw` exist for the FFI that must hand it to C. phase-417 W4.a."
  },
  "rust:OptionalParameter::set": {
    "verdict": "divergence",
    "why": "same as `rust:MandatoryParameter::set`, with `Option<T>` because `None` is how a value is cleared here -- see `rust:OptionalParameter::unset`."
  },
  "rust:OptionalParameter::unset": {
    "verdict": "declined",
    "disposition": "adopt",
    "why": "rclrs spells clearing an optional parameter `unset()`; ours is `set(None)`, which stores `ParameterValue::NotSet` -- the same operation, one word shorter to implement and one call longer to read. What a user loses is only the spelling."
  },
  "rust:Parameter": {
    "verdict": "extension",
    "why": "the stored record -- name plus `ParameterValue` -- returned by `ParameterServer::iter()` and `get_parameter()`, and what the `~/list_parameters` / `~/describe_parameters` handlers walk. rclrs never materialises one because its store is a map behind the node handle. Ours must, because the handlers iterate the static array without allocating an intermediate collection. C has the same record as `nros_parameter_t` and C++ has none. This row covers `Parameter::new`/`bool`/`integer`/`double`/`string`/`name`/`param_type`, which are constructors and accessors over those two fields."
  },
  "rust:ParameterBuilder::default_from_iter": {
    "verdict": "declined",
    "disposition": "adopt-bounded",
    "why": "builds an array default from any `IntoIterator<Item = T>`. Ours takes the value directly, and an array value is a `heapless::Vec` bounded by `MAX_ARRAY_LEN`; collecting an unbounded iterator into a fixed capacity has to fail somewhere, which is what `ParameterVariant::try_to_parameter_value` reports (issue 0323). No allocator."
  },
  "rust:ParameterBuilder::default_string_array": {
    "verdict": "declined",
    "disposition": "adopt-bounded",
    "why": "the `&[&str]` convenience for the same thing; see `rust:ParameterBuilder::default_from_iter`. A string array here is `Vec<String<MAX_STRING_VALUE_LEN>, MAX_ARRAY_LEN>` -- two compile-time bounds, not a borrowed slice of unbounded strs."
  },
  "rust:ParameterBuilder::discard_mismatching_prior_value": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "handles re-declaring a parameter whose existing value has the wrong type. Ours cannot reach that state: `ParameterServer::declare` refuses a duplicate name outright and returns `false`. rclrs's parameter OVERRIDE machinery: a value supplied outside the code (`--ros-args -p`, a params file, a prior declaration) that the declaration then has to reconcile with. Overrides are resolved BEFORE the image runs here -- the launch resolver bakes them into the SystemModel and the generated entry seeds them into the executor store before any component constructor runs (RFC-0045, issue 0745) -- so by declare time there is no second value to reconcile against. Constraint: no runtime env, no argv."
  },
  "rust:ParameterBuilder::discriminate": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "supplies a custom choice function in place of `rust:default_initial_value_discriminator`. rclrs's parameter OVERRIDE machinery: a value supplied outside the code (`--ros-args -p`, a params file, a prior declaration) that the declaration then has to reconcile with. Overrides are resolved BEFORE the image runs here -- the launch resolver bakes them into the SystemModel and the generated entry seeds them into the executor store before any component constructor runs (RFC-0045, issue 0745) -- so by declare time there is no second value to reconcile against. Constraint: no runtime env, no argv."
  },
  "rust:ParameterBuilder::float_range": {
    "verdict": "extension",
    "why": "the floating-point form; see `rust:ParameterBuilder::integer_range`."
  },
  "rust:ParameterBuilder::ignore_override": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "declares that a command-line or params-file override must NOT win. rclrs's parameter OVERRIDE machinery: a value supplied outside the code (`--ros-args -p`, a params file, a prior declaration) that the declaration then has to reconcile with. Overrides are resolved BEFORE the image runs here -- the launch resolver bakes them into the SystemModel and the generated entry seeds them into the executor store before any component constructor runs (RFC-0045, issue 0745) -- so by declare time there is no second value to reconcile against. Constraint: no runtime env, no argv."
  },
  "rust:ParameterBuilder::integer_range": {
    "verdict": "extension",
    "why": "min/max/step as three scalars. rclrs's `range()` takes a `T::Range` VALUE, and we have that method too (it matched) -- but a facade user cannot construct the value to pass it, because `ParameterRange`/`IntegerRange`/`FloatingPointRange` are not re-exported (see `rust:ParameterRange`). So the scalar form is currently the only reachable one. If the facade gap is closed this stops earning its place."
  },
  "rust:ParameterBuilder::mandatory": {
    "verdict": "rename",
    "disposition": "adopt",
    "why": "phase-428 W6 remainder (2026-09-06) — ledgered because the correlator's return-type comparison became a VERDICT. Return type differs in ONE name and nothing else: rclrs returns `Result<MandatoryParameter<T>, DeclarationError>`, ours (`packages/core/nros-params/src/typed.rs:200`) `Result<MandatoryParameter<'a, 's, T>, ParameterError>`. Same shape, same `Result` (so the discard is already lint-covered by core's own `#[must_use]`), same success type modulo our two lifetimes on a caller-owned store (phase-382). The only real difference is that our error enum is called `ParameterError` where upstream's is `DeclarationError` — a rename we chose and never wrote down, which is exactly what this ledger is for. RFC-0036 forbids recording a preference as a divergence, so the honest verdict is `rename`, and the disposition is ADOPT: nothing about an RTOS motivates the different word. Covers `optional` and `read_only`, which carry pointer rows."
  },
  "rust:ParameterBuilder::new": {
    "verdict": "divergence",
    "why": "rclrs's builder is produced by `Node::declare_parameter(name)` and borrows the node; ours is `ParameterBuilder::new(&mut ParameterServer, name)` and borrows the SERVER, because there is no `Arc<Node>` holding parameters. the Rust API is a DECLARATIVE COMPONENT MODEL, not rclrs's handle model (RFC-0043/0044). A user implements `Node`/`ExecutableNode` for a type, declares entities in `register`, and receives callbacks; rclrs holds an `Arc<Node>` and calls methods on it. The constraint is static entity storage plus an executor that owns dispatch: with no allocator the entity set has to be known at declaration time."
  },
  "rust:ParameterBuilder::optional": {
    "verdict": "rename",
    "disposition": "adopt",
    "why": "phase-428 W6 remainder (2026-09-06) — ledgered because the correlator's return-type comparison became a VERDICT. see `rust:ParameterBuilder::mandatory` — `ParameterError` where rclrs names `DeclarationError`. (`typed.rs:222`.)"
  },
  "rust:ParameterBuilder::range": {
    "verdict": "divergence",
    "disposition": "adopt",
    "why": "phase-428 W6 remainder (2026-09-06) — ledgered because the correlator's return-type comparison became a VERDICT. NOT the error-name rename its three siblings are. rclrs's `ParameterBuilder::range` returns `Self` — an infallible builder step — while ours (`packages/core/nros-params/src/typed.rs:157`) returns `Result<Self, ParameterError>`, because a range is validated against the declared parameter TYPE at the point it is set rather than at `mandatory()`. That is a real behavioural difference and it is LOUD in the right direction: a ported `.range(a..=b).mandatory()` fails to compile (no `mandatory` on a `Result`), and `Result` is `#[must_use]`, so the value cannot be dropped silently either. ADOPT is recorded as the direction — validating at the terminal would restore upstream's chain — but nothing is silently wrong today, so this is a record, not a work item."
  },
  "rust:ParameterBuilder::read_only": {
    "verdict": "rename",
    "disposition": "adopt",
    "why": "phase-428 W6 remainder (2026-09-06) — ledgered because the correlator's return-type comparison became a VERDICT. see `rust:ParameterBuilder::mandatory` — `ParameterError` where rclrs names `DeclarationError`. (`typed.rs:169`.)"
  },
  "rust:ParameterDefault": {
    "verdict": "extension",
    "why": "the parameter default a COMPONENT records at declaration, so `nros sync` and the launch resolver can read a package's parameters out of source without running the image (RFC-0043). Values are `MetadataString` because the record has to be `const`-constructible in a static table -- `Double` is a string for the same reason. machinery `nros::node!` expands into. Public because the macro resolves it through `::nros::`, not because a user writes it. rclrs has no counterpart because rclrs has no codegen step. Issue 0784: this should be `#[doc(hidden)]`. Covers `ParameterDefault::for_type` and `::parameter_type`."
  },
  "rust:ParameterDescriptor": {
    "verdict": "divergence",
    "why": "the per-parameter metadata `rcl_interfaces/msg/ParameterDescriptor` carries -- description, read-only, dynamic typing, range -- as ONE inline value stored beside the parameter. rclrs splits the same information across `ParameterOptions`, `ParameterRanges` and `AvailableValues`, which are owned/`Arc` shaped. Constraint: no allocator -- the descriptor lives in the server's fixed slot array, so the description is a `heapless::String` and the range an inline enum, and both are sized at compile time. `validate_range` is called on every set -- direct `ParameterServer::set`, the typed API, and both `SetParameters` / `SetParametersAtomically` handlers through `ParameterServer::apply` / `check_apply` -- and it enforces min, max AND step (`ParameterRange::contains`, the one predicate for both arms; rclrs `rclrs/src/parameter/range.rs` semantics: either endpoint accepted, off-lattice interior refused, 100-ULP tolerance for doubles), which is where `SetParameterResult::OutOfRange` comes from. Until issue 1150 (fixed 2026-09-06) this sentence was FALSE: `contains()` checked only min/max while `DescribeParameters` published the step. Declare also refuses an ill-formed range (`min > max`, `step < 0`) or a default its own range rejects (`SetParameterResult::InvalidRange`), as upstream. This row covers `ParameterDescriptor::*`. See `rust:ParameterOptions`."
  },
  "rust:ParameterError": {
    "verdict": "extension",
    "why": "RE-VERDICTED 2026-08-26 (phase-379 W5), was `rename`. The ours-only half of `rust:ParameterValueError`, now `declined`: one error type (`packages/core/nros-params/src/typed.rs:18`) spanning what rclrs splits into `ParameterValueError` and `DeclarationError`, plus `StorageFull` and `StringConversion` -- the two failures a fixed-capacity `heapless` store has and an allocating one does not. The blocker recorded here, that this contradicted `rust:SetParameterResult`, was NOT a real contradiction: that row writes its destination as `Result<(), ParameterError>` meaning 'the surviving error type, spelled as it is today', and its change is the REMOVAL of `SetParameterResult` (and of `is_success` and the panicking `From` impl), which says nothing about what the survivor is called. The two rows compose -- collapse first, and there is then one error type, this one.",
    "rename": {
      "status": "resolved",
      "resolution": "re-verdicted `extension` -- follows its theirs-half rust:ParameterValueError, now declined; the rust:SetParameterResult 'contradiction' was shorthand for 'the surviving error type', not a naming claim"
    }
  },
  "rust:ParameterKind": {
    "verdict": "declined",
    "disposition": "absent",
    "provides": [
      "nros::ParameterType",
      "nros::ParameterDescriptor::with_dynamic_typing"
    ],
    "why": "rclrs's type discriminant, which is `rcl_interfaces`'s nine types PLUS a tenth, `Dynamic`, for a parameter allowed to change type. Ours is `ParameterType` (see `rust:ParameterType`), which mirrors `rcl_interfaces/msg/ParameterType` exactly and carries the dynamic-typing intent as a descriptor FLAG (`ParameterDescriptor::with_dynamic_typing`) rather than a tenth variant, so the enum stays byte-identical to what goes on the wire. What a user loses: a type-level distinction between a fixed and a dynamic parameter. AMENDED 2026-09-05 (phase-425): the NAME half of this pair is now `their-rename` on `rust:ParameterType` -- `rcl_interfaces/msg/ParameterType`, rclcpp and our own C all use our word, so rclrs is the outlier on the spelling. This row stays `declined` because it is not only a spelling: rclrs's enum carries a tenth variant, `Dynamic`, that we genuinely do not have, and a `their-rename` may not absorb a shape difference."
  },
  "rust:ParameterOptions": {
    "verdict": "divergence",
    "why": "rclrs's per-parameter options bundle (description, constraints, ranges, override handling), built and passed at declare time. Ours is `ParameterDescriptor`, stored INLINE beside the parameter in the server's fixed array rather than passed as an owned bundle -- no allocator, so the description is a `heapless::String` and the range an inline enum. See `rust:ParameterDescriptor`."
  },
  "rust:ParameterRange::*": {
    "verdict": "extension",
    "bucket": "ours-only",
    "why": "`is_valid` and `contains` — the predicates `ParameterServer` runs on every declare and every set. rclrs's `ParameterRange` carries the bounds and leaves the checking to the node; ours is where min, max AND step are enforced (step included since issue 1150), so the range means the same thing to a local set and to a remote `ros2 param set`. phase-417 W4.a. (the type itself correlates and needs no row; these two methods are ours)."
  },
  "rust:ParameterRanges": {
    "verdict": "declined",
    "disposition": "absent",
    "provides": [
      "nros::ParameterRange",
      "nros::IntegerRange",
      "nros::FloatingPointRange"
    ],
    "why": "RE-VERDICTED phase-417 W4.a. was `gap`. rclrs's aggregate of every range on one parameter. Ours is a single `Option<ParameterRange>` on the descriptor, and for a parameter that is not a shortfall: `ParameterRange` is an enum over `IntegerRange` / `FloatingPointRange`, a parameter has ONE type, and `rcl_interfaces/msg/ParameterDescriptor` carries the two range sequences precisely so that at most one is non-empty — which is exactly what `to_rcl_descriptor` writes. The aggregate would name a state the wire format cannot carry. All three types are exported from the `nros` facade since W4.a, which is the half of the old row that WAS a defect."
  },
  "rust:ParameterServer": {
    "verdict": "divergence",
    "why": "the parameter store as a STANDALONE object. rclrs has no such type because in rclrs the `Arc<Node>` is the store: declaring goes through the node handle and the values live behind its mutex. Ours is a fixed table of slots the six `rcl_interfaces/srv/*` handlers read directly. Constraints: no allocator (capacity is a slot count fixed before the first declare, defaulting to the compile-time Kconfig-derived `MAX_PARAMETERS`), and static entity storage under the component model (RFC-0043/0044) -- there is no `Arc<Node>` to hang parameters off. Phase 382 W2' made the slots CALLER-OWNED: the server borrows a `ParameterTable` instead of owning the array, so it is 24 bytes and `new()` became `new_in(table)`. This row covers every `ParameterServer::*` member; `remove`/`unset` are worth naming because they are capabilities neither C nor C++ has (see `c:delete_parameter`)."
  },
  "rust:ParameterStorage": {
    "verdict": "divergence",
    "why": "backing storage for a `ParameterServer`, which rclrs has no counterpart to because rclrs allocates -- its parameters live behind the `Arc<Node>`'s mutex and grow on demand. Constraint: no allocator, so the slots must be memory the CALLER placed (a `static`, a struct field, or the executor backing once phase-382 W3' carves it) and lent for the server's life. `N` defaults to `MAX_PARAMETERS`, the Kconfig-derived constant, and is not a cap. This row covers every `ParameterStorage::*` member; `init_in_place` is worth naming because the type is enormous and scales with `N` (285,184 bytes at 32 slots, 2,281,472 at 256 -- `ParameterValue` is sized by its `StringArray` variant), so `Box::new(ParameterStorage::new())` builds the whole value on the stack first and overruns an RTOS thread stack (issue 0756). It moved here from `ParameterServer` in phase-382 W2', with the bulk it guards."
  },
  "rust:ParameterTable": {
    "verdict": "divergence",
    "why": "the borrowed view of a `ParameterStorage` that a `ParameterServer` holds -- `&'s mut [Option<ParameterEntry>]` behind a newtype. rclrs has no counterpart for the same reason as `rust:ParameterStorage`: it allocates, so nothing there has to name the storage separately from the store. Constraint: no allocator plus a private slot layout. The newtype exists so a consumer that must NAME the storage (`nros-node`, which will carve it out of the executor backing in phase-382 W3') can do so without `ParameterEntry` becoming public API. Its length is the server's capacity."
  },
  "rust:ParameterType": {
    "verdict": "their-rename",
    "why": "RE-VERDICTED rename -> their-rename 2026-09-05 (phase-425). The row said in prose what its verdict denied: \"a rename rclrs should make, not us\". `rename` means OURS is the one that should change, so the row was self-contradictory, and it is the one item in this class whose misfiling had a MECHANICAL consequence -- `scripts/check-prelude-tiers.py` keys prelude membership on `extension`, so an earlier attempt to move this row into that bucket would have evicted `ParameterType` from `nros::prelude`, a name every ported parameter node needs (`packages/api/nros/src/lib.rs`). `their-rename` says the correspondent EXISTS, which is exactly the fact the prelude rule asks for, so the name stays exported and the gate now says so on purpose rather than by omission. Pair: `rust:ParameterKind`, which stays `declined` -- rclrs's enum also carries a tenth `Dynamic` variant that we replace with `ParameterDescriptor::with_dynamic_typing`, and that is a SHAPE claim this verdict does not cover. No code change.\n\nORIGINAL ROW: the ours-only half of `rust:ParameterKind`. Ours is the better name of the two -- it is what `rcl_interfaces/msg/ParameterType`, rclcpp and our own C all call it -- so this pair is a rename rclrs should make, not us; recorded here because the campaign records both halves.",
    "their_rename": {
      "ours": "`nros::ParameterType` (`packages/core/nros-params/src/types.rs`), re-exported from `nros` and from `nros::prelude`",
      "majority": [
        "`rcl_interfaces/msg/ParameterType` -- the wire definition of the nine type constants, which both sides mirror",
        "rclcpp: `ParameterType` (recorded surface; also `ParameterTypeException`, `get_parameter_types`)",
        "our own C `nros_parameter_type_t`"
      ],
      "outlier": "rclrs's `ParameterKind` -- the only recorded ROS 2 client library that renames the discriminant away from the message's own word",
      "pair": "rust:ParameterKind"
    }
  },
  "rust:ParameterValue::*": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "typed accessors (`as_bool`, `as_string`, `as_double_array`, ...) and constructors (`from_bool`, `from_string`, ...) on the value enum. rclrs converts through `From`/`TryFrom` impls that hand back owned `String`/`Vec`; ours cannot, because the value is stored in a `heapless::String<MAX_STRING_VALUE_LEN>` / `heapless::Vec<_, MAX_ARRAY_LEN>` inside the server's fixed slot and there is no allocator to copy it out of. So the readers return BORROWS (`Option<&str>`, `Option<&[u8]>`) and the writers are fallible on capacity (`from_string` returns `Option<Self>`). Constraint: no allocator, compile-time capacity. Same reason `rust:ParameterVariant::*` exists."
  },
  "rust:ParameterValueError": {
    "verdict": "declined",
    "disposition": "absent",
    "provides": [
      "nros::ParameterError"
    ],
    "why": "RE-VERDICTED 2026-08-26 (phase-379 W5), was `rename`, and the old reason was wrong on two counts. rclrs's set-time failure enum is exactly `OutOfRange`, `TypeMismatch`, `ReadOnly` (recorded surface: rclrs 0.7.0, `rclrs/src/parameter.rs`). It is one of rclrs's TWO parameter error enums -- the other is `DeclarationError` (`AlreadyDeclared`, `NoValueAvailable`, `OverrideValueTypeMismatch`, `PriorValueTypeMismatch`, `InitialValueOutOfRange`, `InvalidRange`). Ours is ONE `ParameterError` (`packages/core/nros-params/src/typed.rs:18`) whose seven variants span both: those three, plus `InvalidRange` -- which is rclrs's DECLARATION error, and ours is raised by `ParameterBuilder::integer_range`/`float_range` -- plus `NotFound`, plus `StorageFull` and `StringConversion`, the two failures a fixed-capacity `heapless` store has and an allocating one does not. So (a) it is NOT 'the same concept under a different name': our declare path already returns it (`ParameterBuilder::mandatory`/`optional`/`read_only` -> `Result<_, ParameterError>`), and a type called `ParameterValueError` would report a full store and a malformed range as 'value' errors. And (b) it is not 'no platform reason': two of the four extra variants are the capacity constraint made visible, the same one `rust:ParameterValue::*` and `rust:ParameterVariant::*` record. Taking rclrs's narrow name would also fight `rust:DeclarationError`, the open gap for the other half of rclrs's split. What a user loses: that split -- a type-level distinction between a declare-time and a set-time failure -- which is recorded as the `rust:DeclarationError` gap, not here. Ours half: `rust:ParameterError`.",
    "rename": {
      "status": "resolved",
      "resolution": "re-verdicted `declined` -- ours is not a renamed ParameterValueError but a union of rclrs's ParameterValueError and DeclarationError plus two heapless-capacity variants; the missing split is the rust:DeclarationError gap"
    }
  },
  "rust:ParameterVariant::*": {
    "verdict": "divergence",
    "bucket": "ours-only",
    "why": "`to_parameter_value` / `from_parameter_value` / `try_to_parameter_value` -- the conversion trait between a Rust type and a stored `ParameterValue`. rclrs's `ParameterVariant` declares only `kind()` and does its conversions through `From`. The third method is the constraint made visible: `to_parameter_value` cannot report that a `String` exceeded `MAX_STRING_VALUE_LEN` or a `Vec` exceeded `MAX_ARRAY_LEN`, and the hosted impls used to `unwrap_or_default()` -- turning an over-long string into `NotSet` (a TYPE change) and an oversized vec into an EMPTY array, both indistinguishable from a caller who meant it (issue 0323). Fixed capacity forces a fallible conversion; an allocator would not. See `rust:ParameterValue::*`."
  },
  "rust:ParameterVariant::kind": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "RE-VERDICTED 2026-08-26 (phase-379 W5), was `rename`. rclrs names the trait method that reports a variant's type `kind()`, returning `ParameterKind`; ours is `parameter_type()`, returning `ParameterType` (both are associated fns with no receiver -- ours at `packages/core/nros-params/src/types.rs:426`). The rename justified itself entirely by pointing at `rust:ParameterKind` -- whose verdict is `declined`, because ours is the better name of the two: it is what `rcl_interfaces/msg/ParameterType`, rclcpp and our own C (`nros_parameter_type_t`) all call it. The stated reason therefore resolves AGAINST the rename. Renaming the method alone would buy no rclrs parity either -- a `kind()` that returns a `ParameterType` still does not match rclrs's `kind() -> ParameterKind` -- while costing the one consistency we do have, that the accessor is named for the type it returns. What a user loses: the `kind()` spelling. Ours half: `rust:ParameterVariant::parameter_type`.",
    "rename": {
      "status": "resolved",
      "resolution": "re-verdicted `declined` -- the rename's only argument was rust:ParameterKind, which is itself declined; the method is named after the type we keep"
    }
  },
  "rust:ParameterVariant::parameter_type": {
    "verdict": "extension",
    "why": "RE-VERDICTED 2026-08-26 (phase-379 W5), was `rename`. The ours-only half of `rust:ParameterVariant::kind`, now `declined`: the accessor is named for the type it returns, and that type (`ParameterType`, see `rust:ParameterKind`) is the one we keep. Recorded so the pair is visible from both sides; it carries no decision of its own.",
    "rename": {
      "status": "resolved",
      "resolution": "re-verdicted `extension` -- follows its theirs-half rust:ParameterVariant::kind, now declined"
    }
  },
  "rust:Parameters": {
    "verdict": "rename",
    "disposition": "adopt-bounded",
    "provides": [
      "nros::UndeclaredParameters",
      "nros::Executor::use_undeclared_parameters"
    ],
    "why": "RE-VERDICTED phase-417 W4.a. was `gap`, and two of its three complaints are closed: the type IS exported from the `nros` facade now, and it reads AND WRITES (`set_bool` / `set_integer` / `set_double` / `set_string`, through `ParameterServer::set_or_declare` — not `apply`, because `allow_undeclared_parameters` governs what a REMOTE set may create and a caller who reached for this type has opted in by naming it, which is rclrs's split too). What remains is the NAME: rclrs calls it `Parameters`, ours is `UndeclaredParameters`. `rename` and not `divergence` because no platform constraint picks either spelling. The envelope: a ported rclrs file rarely NAMES the type — it binds what `use_undeclared_parameters()` returns — so the drop-in cost is a `use` line. Renaming it inside `nros::`, where `Parameter` is already the store's value type, is a stage-6 decision rather than this item's."
  },
  "rust:QoSProfile::parameters_default": {
    "verdict": "rename",
    "why": "Ours is `QoSProfile::parameters_default()` (`nros-rmw/src/traits.rs:957`); rclrs spells it `parameter_services_default` (`rclrs/src/qos.rs:327`). EXACTLY ONE name differs — rclrs's sibling `parameter_events_default` already matches ours (`traits.rs:962`) — so the rename also makes our own pair self-consistent. Three sites: the definition, `spin.rs:7038`, and the assertion at `traits.rs:3305`.\n\nTHREE CLAIMS IN THE PREVIOUS TEXT WERE WRONG, corrected 2026-09-04 and verified:\n1. \"It is also DEAD\" — false. `Executor::register_parameter_services` builds all six servers with `QoSProfile::parameters_default()` at `spin.rs:7038`; issue 0793 wired it there precisely because the constant had no caller and every parameter server ran on a depth-10 queue. service.json's `rust:QoSProfile::parameter_services_default` already carried this correction; param.json never got it.\n2. \"the transient-local profile this returns\" — false. `QOS_PROFILE_PARAMETERS` is Reliable + **Volatile** + KeepLast(1000) (`traits.rs:886-891`). service.json corrected this on 2026-08-25 against `rmw/qos_profiles.h` and said so; this row held the pre-correction text.\n3. \"The owning type's own `QoSProfile`->`QoSProfile` rename landed\" — a self-identical rename, which is not a statement. The real one was `QosSettings`->`QoSProfile`, spelled correctly three times in timer.json. A rename sweep rewrote the FROM side of a sentence about that rename, destroying the only thing it recorded.\n\nTwo sibling rows still hold errors 1 and 2: `cpp:ParametersQoS::ParametersQoS` here, and `cpp:ParametersQoS` in qos.json. A 2026-08-25 correction landed in one shard and three siblings kept the retracted claim — the ledger's own fix-the-CLASS rule failing inside the ledger.",
    "rename": {
      "from": "QoSProfile::parameters_default",
      "to": "QoSProfile::parameter_services_default",
      "status": "settled-pending-owner",
      "category": "A",
      "resolution": "OPEN, unblocked, ~3 lines. Whether to leave a `#[deprecated]` forwarder is the owner's call: phase-379 W7's \"NOTHING for Rust\" rule is about TRAIT methods and this is an inherent const fn, so the `BoardConfig::zenoh_locator` precedent applies instead."
    }
  },
  "rust:RmwParameterConversionError": {
    "verdict": "rename",
    "disposition": "adopt-bounded",
    "provides": [
      "nros::ValueConversionError"
    ],
    "why": "RE-VERDICTED phase-417 W4.a. was `gap`, and the complaint it was filed under is closed: the `nros` facade exports the type now, so a user can match on WHY a wire value was rejected rather than reading the sentence `reason()` put in the `SetParametersResult`. What remains is the NAME — rclrs's `RmwParameterConversionError` against our `ValueConversionError`, which is where the conversion actually happens (`nros-node`'s `parameter_services`, between an `rcl_interfaces` value and the store's). `rename` because no platform constraint picks either spelling; the export is what a porting user needed."
  },
  "rust:SetParameterResult": {
    "verdict": "rename",
    "why": "OUR LANGUAGES DISAGREE, inside one language: this enum and `ParameterError` name the same five failures (`ReadOnly`, `TypeMismatch`, `OutOfRange`, `NotFound`, `StorageFull`), differing only in that this one also has a `Success` variant instead of being a `Result`. There is a `From<SetParameterResult> for ParameterError` that `panic!`s on the two cases it does not cover. `ParameterServer`'s inherent setters return this one; the typed `MandatoryParameter`/`OptionalParameter` wrappers return the other. rclrs has one spelling, `Result<(), ParameterValueError>`. Ours should collapse to `Result<(), ParameterError>`. Covers `SetParameterResult::is_success`, which exists only because this is not a `Result`. Phase 379 W3.",
    "rename": {
      "to": null,
      "category": "C",
      "status": "not-a-rename",
      "actual_change": "REMOVAL — collapses to Result<(), _>, takes is_success() and a From impl with it. Public Rust API break"
    }
  },
  "rust:UndeclaredParameters*": {
    "verdict": "rename",
    "bucket": "ours-only",
    "why": "Our spelling of rclrs's `Parameters` — the handle that reads and writes parameters a node never declared. See `rust:Parameters` for the naming half; the capability, the facade export and the WRITE half all ship as of phase-417 W4.a. The setters go through `ParameterServer::set_or_declare` rather than `apply`, because `allow_undeclared_parameters` governs what a REMOTE set may create and a caller who reached for this type has opted in by naming it — which is rclrs's split too."
  },
  "rust:ValueConversionError*": {
    "verdict": "rename",
    "bucket": "ours-only",
    "why": "Our spelling of rclrs's `RmwParameterConversionError`, exported from the `nros` facade since phase-417 W4.a. -- see `rust:RmwParameterConversionError` for the naming half. `reason()` is the sentence the `~/set_parameters` handler puts into the `SetParametersResult`, so a local caller and a remote one read the same words."
  },
  "rust:default_initial_value_discriminator": {
    "verdict": "declined",
    "disposition": "absent",
    "why": "the default choice function over `rust:AvailableValues`. rclrs's parameter OVERRIDE machinery: a value supplied outside the code (`--ros-args -p`, a params file, a prior declaration) that the declaration then has to reconcile with. Overrides are resolved BEFORE the image runs here -- the launch resolver bakes them into the SystemModel and the generated entry seeds them into the executor store before any component constructor runs (RFC-0045, issue 0745) -- so by declare time there is no second value to reconcile against. Constraint: no runtime env, no argv."
  }
}

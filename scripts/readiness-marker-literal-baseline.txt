# Issue 0481 — readiness greps naming an ambiguous literal, predating the gate.
# Keyed file<TAB>literal<TAB>count, NOT by line: a line-keyed baseline broke
# the moment a doc comment shifted four sites that had not changed.
# SHRINKING backlog — fix sites, lower the count.
# New code should use ManagedProcess::expect_ready(DemoRole::…, lang, timeout).
#
# EMPTY since issue 0480 (2026-08-10). All 32 remaining sites were mapped to the
# binary they spawn and pointed at that binary's `output::*` constant, so the
# gate now enforces the rule outright with nothing exempted. Three were real
# defects, not ambiguity: zephyr.rs twice and interop_e2e.rs once waited on a
# marker their binary does not print (see the issue). Re-adding a row here means
# admitting a NEW ambiguous literal — fix the site instead.

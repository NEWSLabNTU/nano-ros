# Leaf crates whose Cargo.lock cannot satisfy its own manifest.
#
# EMPTY as of 2026-07-31 — all 26 were pinned. Issue 0359 is closed.
#
# scripts/check-leaf-lockfiles.sh fails if a line here stops drifting, so this
# list can only shrink. Never add a line to silence NEW drift: regenerate the
# lock and review the diff instead.

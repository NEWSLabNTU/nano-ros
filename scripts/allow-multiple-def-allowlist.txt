# Phase 251 — audited allowlist of build files permitted to use the linker flag
# `--allow-multiple-definition`. SINGLE SOURCE OF TRUTH for
# scripts/check-no-allow-multiple-def.sh (and `just check-no-allow-multiple-def`).
#
# The flag is FORBIDDEN by default: it lets two different functions with the same
# name coexist and silently binds callers to whichever copy the linker picks first
# (the #48-class wrong-copy hazard). Every surviving use is a tracked exception
# with a reason + owning issue; the target is ZERO entries.
#
# Format: <repo-relative-path>   # reason (owning issue / wave)
# Only ACTUAL link-flag uses count (a comment that merely names the flag does not).
# Drift fails the gate: a new unlisted use, or a listed file that no longer uses it.

CMakeLists.txt                                   # APPLE cyclonedds force_load tail — phase-251 W2 (#70); drop or macOS-gate
cmake/board/nano-ros-board-riscv64-qemu.cmake    # threadx-riscv64 strong/weak mem override — phase-251 W1 (#70); drop after relink

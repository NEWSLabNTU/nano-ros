/*
 * Host stand-in for <zephyr/posix/sched.h> (phase-460 W6). Forwards to the
 * host's own sched.h for the same reason as the pthread header beside it.
 */
#ifndef NROS_HOST_SHIMS_ZEPHYR_POSIX_SCHED_H
#define NROS_HOST_SHIMS_ZEPHYR_POSIX_SCHED_H

#include <sched.h>

#endif /* NROS_HOST_SHIMS_ZEPHYR_POSIX_SCHED_H */

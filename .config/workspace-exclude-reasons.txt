# Root-workspace `exclude` entries that no manifest fact justifies.
#
# phase-451 W3, issue 1217. `scripts/check-workspace-exclude-list.py` derives a
# reason for an exclusion from the tree itself (own `[workspace]`, a nested
# workspace above it, its own tracked `Cargo.lock`, a pinned `[build] target`,
# a cross-only dependency, or no Rust target at all). The entries below satisfy
# none of those and are excluded anyway, for a reason that is real but is not a
# cargo fact.
#
# This list is a RATCHET: it may only SHRINK. Adding a line is how the unaudited
# list of 2026-07-28 grew back, so a new entry should be a workspace member
# instead, or should gain the manifest fact that justifies it.
#
# Format: <path>  # <reason>
#
# Shrunk 4 -> 2 on 2026-09-11 (issue 1309): `nros-baremetal-common` and
# `nros-board-freertos` are workspace MEMBERS now. The second needed a dead
# `reference-mps2` back-edge removed first — it formed a package cycle, which
# is what had kept the crate out of every workspace and therefore out of every
# lane. Entering the lanes surfaced 8 latent `-D warnings` errors in it.

packages/boards/nros-board-threadx     # package CYCLE, like nros-board-freertos had: it optionally deps nros-board-threadx-{linux,qemu-riscv64}, which dep it back. Unlike freertos, its `reference-*` features still have live `cfg` readers (issue 1309)
packages/boards/nros-board-nuttx       # package CYCLE via the optional nros-board-nuttx-qemu dep; `reference-qemu` has live `cfg` readers in src/lib.rs, so removing it is not the one-line change freertos was (issue 1309)

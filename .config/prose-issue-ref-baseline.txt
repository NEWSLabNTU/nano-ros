# Prose references to an issue id with no file on disk.
#
# A RATCHET, not an allowlist: this file may only shrink. Each row is
# `<path>:<id>`. The legitimate reason to be here is an IN-FLIGHT issue —
# the citing doc is on main, its issue file is in another open PR — where
# deleting the correct citation would be the wrong fix.
#
# Regenerate: python3 scripts/check-prose-issue-refs.py --write-baseline
#
# CLASSIFIED 2026-09-08, because "in flight" is only true of some of these:
#
#   9999  NEGATION — the citation is deliberately unresolvable. Issue 1092
#         describes a mutation test whose control is "a gap reason naming
#         issue 9999, which DOES NOT EXIST". Filing 9999 would destroy the
#         control; deleting the sentence would delete the test's description.
#         The same class `check-doc-recipe-refs` calls NEGATION.
#
#         TWO documents cite it: issue 1092, which describes the test, and
#         issue 1241, which quotes 1092's control. BOTH are archived now
#         (1092 landed resolved in phase-444 W4.a) and `archived/` is not
#         scanned, so the row this note explained is gone. The live tools
#         spell the control as "an id with no file (9999)", which the ref
#         regex does not read as a citation.
#
#   1110  DELIBERATELY WITHDRAWN. The file DID exist, on the phase-431
#         branch only: W6 filed it while the rustdoc lane was red, and
#         phase-431 deletes it because the lane is green and an OPEN issue
#         against a green lane is a false claim in the series whose job is to
#         say what is broken. The FOUR prose citations stay, because what they
#         record — a deny-level rustdoc lint no merge-gating lane ran, the
#         0319/0896 class — is still worth reading, and `refs/issue-ids/1110`
#         is claimed either way.
#
#   1319  IN FLIGHT — filed on `feat/448-w7-w8-arena-model` (PR #951). phase-448
#         names it because it is the one UNDER-size in the arena model, and
#         anyone editing W7's term must read it before lowering that term
#         further. REMOVE THIS ROW when #951 merges.
#
docs/issues/1116-rustdoc-diagnostics-outside-the-published-crate-set.md:1110
docs/roadmap/archived/phase-431-ship-the-nros-binary.md:1110
docs/roadmap/phase-445-board-choice-generates-leaf-config.md:1296
docs/roadmap/phase-445-board-choice-generates-leaf-config.md:1305
docs/roadmap/phase-448-exact-executor-backing-on-every-port.md:1319
just/check/docs.just:1110
scripts/build/rustdoc-set.sh:1110

# Zephyr native_sim test fixtures use NSOS host socket offload.
# This keeps E2E tests on host loopback and avoids zeth/TAP setup.
CONFIG_ETH_NATIVE_POSIX=n
CONFIG_NET_DRIVERS=y
CONFIG_NET_SOCKETS_OFFLOAD=y
CONFIG_NET_NATIVE_OFFLOADED_SOCKETS=y

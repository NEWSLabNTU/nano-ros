/*
 * Copyright (c) 2026, NEWSLab NTU.
 * SPDX-License-Identifier: EPL-2.0 OR BSD-3-Clause
 *
 * Hand-rolled replacement for Cyclone DDS's autogenerated
 * `dds/features.h`. Reference template:
 * `third-party/dds/cyclonedds/src/ddsrt/include/dds/features.h.in`
 *
 * Same approach as `dds/config.h` — pre-bake the feature set for
 * Zephyr so cross-compile skips the host `option(DDS_HAS_...)`
 * tree. Match the `DDS_HAS_*` defines passed via
 * `zephyr_compile_definitions()` in `zephyr/CMakeLists.txt` —
 * those control call-site gating; this header controls API
 * visibility. Keep the two in sync.
 */
#ifndef _DDS_PUBLIC_FEATURES_H_
#define _DDS_PUBLIC_FEATURES_H_

/* Lifespan + deadline-missed events are part of stock RMW-cyclonedds
 * expectations — keep them enabled. */
#define DDS_HAS_LIFESPAN 1
#define DDS_HAS_DEADLINE_MISSED 1

/* Source-specific multicast: off (DDS basics work without it). */
/* #undef DDS_HAS_SSM */

/* Everything else off — matches the zephyr_compile_definitions block
 * in zephyr/CMakeLists.txt under the cyclonedds branch. */
/* #undef DDS_HAS_SECURITY */
/* #undef DDS_HAS_NETWORK_PARTITIONS */
/* #undef DDS_HAS_SSL */
/* #undef DDS_HAS_TYPE_DISCOVERY */
/* #undef DDS_HAS_TOPIC_DISCOVERY */
/* #undef DDS_HAS_SHM */

#endif

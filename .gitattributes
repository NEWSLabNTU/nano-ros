
# issue 0884 — the open-issue index is 100% generated and append-heavy.
# (docs/issues/open.md carried `merge=union` here until 2026-09-03. The file is
# now GITIGNORED — see .gitignore — because the driver does not run on GitHub's
# server-side merge/rebase, which is the only place the conflict mattered.)
